destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''arp-scan'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libpcap
- perl-libwww
update_on:
- alpm: arp-scan
  source: archpkg
