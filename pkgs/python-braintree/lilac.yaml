destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-braintree'')

  '
repo_depends:
- python-requests
update_on:
- alpm: python-braintree
  source: archpkg
