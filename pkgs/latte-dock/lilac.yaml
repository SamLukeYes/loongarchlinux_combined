destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''latte-dock'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- plasma-framework
- knewstuff
- hicolor-icon-theme
repo_makedepends:
- extra-cmake-modules
update_on:
- alpm: latte-dock
  source: archpkg
