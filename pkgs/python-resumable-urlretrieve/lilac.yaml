destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-resumable-urlretrieve'')

  '
repo_checkdepends:
- python-pytest
- python-rangehttpserver
repo_depends:
- python
- python-requests
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-resumable-urlretrieve
  source: archpkg
