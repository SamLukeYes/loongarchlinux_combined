destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''profile-sync-daemon'')

  '
repo_checkdepends: []
repo_depends:
- bash
- findutils
- procps-ng
- rsync
- systemd
repo_makedepends: []
update_on:
- alpm: profile-sync-daemon
  source: archpkg
