destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xfce4-diskperf-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- xfce4-panel
update_on:
- alpm: xfce4-diskperf-plugin
  source: archpkg
