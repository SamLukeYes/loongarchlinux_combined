destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libirman'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- lirc
- glibc
update_on:
- alpm: libirman
  source: archpkg
