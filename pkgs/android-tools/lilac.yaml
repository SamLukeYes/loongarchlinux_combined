destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''android-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb
- android-udev
- pcre2
- brotli
- zstd
- protobuf
update_on:
- alias: protobuf
- alpm: android-tools
  source: archpkg
