destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aws-c-mqtt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- aws-c-http
- aws-c-common
update_on:
- alpm: aws-c-mqtt
  source: archpkg
