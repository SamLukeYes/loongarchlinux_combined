destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ghostwriter'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kxmlgui
- hunspell
- sonnet
- qt5-webengine
update_on:
- alpm: ghostwriter
  source: archpkg
