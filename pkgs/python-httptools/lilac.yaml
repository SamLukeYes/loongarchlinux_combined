destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-httptools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- http-parser
repo_makedepends:
- git
- python-setuptools
- python-wheel
- cython
update_on:
- alias: python
- alpm: python-httptools
  source: archpkg
