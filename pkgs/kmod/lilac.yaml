destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kmod'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- linux: linux-headers
- elfutils: libelf
repo_depends:
- glibc
- zlib
- openssl
- xz
- zstd
repo_makedepends: []
update_on:
- alpm: kmod
  source: archpkg
