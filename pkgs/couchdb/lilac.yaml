destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''couchdb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- icu
- js91
- zlib
repo_makedepends:
- erlang-nox
update_on:
- alias: icu
- alpm: couchdb
  source: archpkg
