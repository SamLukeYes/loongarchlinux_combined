destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''canto-daemon'')

  '
repo_depends:
- python-feedparser
update_on:
- alpm: canto-daemon
  source: archpkg
