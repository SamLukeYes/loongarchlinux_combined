destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-plette'')

  '
repo_checkdepends:
- python-pytest
- python-cerberus
repo_depends:
- python-tomlkit
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-plette
  source: archpkg
