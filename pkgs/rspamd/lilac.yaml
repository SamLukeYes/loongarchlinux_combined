destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rspamd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- file
- glib2
- jemalloc
- openblas
- pcre2
- luajit
- icu
- libsodium
- zlib
- sqlite
- openssl
- glibc
- hyperscan
update_on:
- alias: icu
- alpm: rspamd
  source: archpkg
