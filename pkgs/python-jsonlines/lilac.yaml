destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jsonlines'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-attrs
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-jsonlines
  source: archpkg
