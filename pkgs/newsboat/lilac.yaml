destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''newsboat'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- curl
- hicolor-icon-theme
- json-c
- libxml2
- sqlite
- stfl
repo_makedepends:
- asciidoctor
- rust
- swig
update_on:
- alpm: newsboat
  source: archpkg
