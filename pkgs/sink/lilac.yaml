destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sink'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kcontacts
- kcalendarcore
- kasync
- lmdb
- xapian-core
- curl
- gpgme
repo_makedepends:
- extra-cmake-modules
- flatbuffers
update_on:
- alpm: sink
  source: archpkg
