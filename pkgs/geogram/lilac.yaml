destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''geogram'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glu
- glfw: glfw-x11
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: glfw-x11
  source: alpm
  strip_release: true
- alpm: geogram
  source: archpkg
