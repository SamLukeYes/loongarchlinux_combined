destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nsd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openssl
- libevent
- fstrm
- protobuf-c
- systemd: systemd-libs
repo_makedepends:
- systemd
update_on:
- alpm: nsd
  source: archpkg
