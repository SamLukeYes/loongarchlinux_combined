destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''eslint'')

  '
repo_checkdepends: []
repo_depends:
- acorn
- nodejs
repo_makedepends:
- npm
- rsync
update_on:
- alpm: eslint
  source: archpkg
