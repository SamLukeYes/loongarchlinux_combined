destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rocksdb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bzip2
- jemalloc
- lz4
- snappy
- zlib
- zstd
- liburing
repo_makedepends:
- python
update_on:
- alpm: rocksdb
  source: archpkg
