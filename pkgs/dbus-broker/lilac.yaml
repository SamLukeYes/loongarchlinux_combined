destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dbus-broker'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- systemd: systemd-libs
- expat
- audit
update_on:
- alpm: dbus-broker
  source: archpkg
