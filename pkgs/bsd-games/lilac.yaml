destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bsd-games'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- systemd
- ncurses
update_on:
- alpm: bsd-games
  source: archpkg
