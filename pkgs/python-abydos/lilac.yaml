destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-abydos'')

  '
repo_checkdepends:
- python-pytest
- python-deprecation
- python-nltk
repo_depends:
- python-numpy
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-abydos
  source: archpkg
