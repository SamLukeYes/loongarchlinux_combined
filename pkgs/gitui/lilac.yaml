destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gitui'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- libxcb
- openssl
- zlib
repo_makedepends:
- python
update_on:
- alpm: julia
  provides: libgit2.so
  source: alpm
- alpm: gitui
  source: archpkg
