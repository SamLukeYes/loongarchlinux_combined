destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ristretto'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxfce4ui
- xfconf
- tumbler
- libexif
- file
repo_makedepends:
- python
update_on:
- alpm: ristretto
  source: archpkg
