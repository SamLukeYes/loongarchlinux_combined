destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-mime-types'')

  '
repo_checkdepends: []
repo_depends:
- ruby
- ruby-mime-types-data
repo_makedepends: []
update_on:
- alias: ruby
- alpm: ruby-mime-types
  source: archpkg
