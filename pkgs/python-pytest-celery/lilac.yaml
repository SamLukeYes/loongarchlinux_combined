destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-celery'')

  '
repo_checkdepends: []
repo_depends:
- python-celery
repo_makedepends:
- python-build
- python-installer
- python-flit-core
update_on:
- alpm: python-pytest-celery
  source: archpkg
