destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''schroot'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- e2fsprogs
- boost: boost-libs
- pam
update_on:
- alpm: schroot
  source: archpkg
