destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-django-rest-framework'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-django
repo_depends:
- django: python-django
- python-pytz
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-django-rest-framework
  source: archpkg
