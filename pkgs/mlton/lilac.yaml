destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mlton'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gmp
repo_makedepends:
- git
- mlton
- setconf
update_on:
- alpm: mlton
  source: archpkg
