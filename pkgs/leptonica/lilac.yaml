destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''leptonica'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libjpeg-turbo
- libtiff
- zlib
- openjpeg2
- libwebp
- giflib
- libpng
update_on:
- alpm: leptonica
  source: archpkg
