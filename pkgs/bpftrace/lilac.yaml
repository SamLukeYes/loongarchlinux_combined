destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bpftrace'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- elfutils: libelf
- llvm: llvm-libs
- clang
- zlib
- libpcap
- libbpf
- bcc
update_on:
- alias: clang
- alpm: bpftrace
  source: archpkg
