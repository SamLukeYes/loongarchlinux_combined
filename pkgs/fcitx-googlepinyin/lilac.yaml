destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fcitx-googlepinyin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fcitx
- libgooglepinyin
repo_makedepends:
- cmake
- intltool
update_on:
- alpm: fcitx-googlepinyin
  source: archpkg
