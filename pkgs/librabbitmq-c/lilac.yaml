destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''librabbitmq-c'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- popt
- openssl
repo_makedepends:
- cmake
- docbook-xsl
- doxygen
- xmlto
- ninja
update_on:
- alpm: librabbitmq-c
  source: archpkg
