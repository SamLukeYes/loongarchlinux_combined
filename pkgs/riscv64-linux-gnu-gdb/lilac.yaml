destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''riscv64-linux-gnu-gdb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python
- guile
update_on:
- alias: python
- alpm: guile
  source: alpm
  strip_release: true
- alpm: riscv64-linux-gnu-gdb
  source: archpkg
