destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kross'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kparts
- qt5-script
repo_makedepends:
- extra-cmake-modules
- kdoctools
- qt5-tools
update_on:
- alpm: kross
  source: archpkg
