destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qterminal'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- hicolor-icon-theme
- qtermwidget
- qt5-x11extras
repo_makedepends:
- cmake
- lxqt-build-tools
update_on:
- alpm: qterminal
  source: archpkg
