destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-routes'')

  '
repo_checkdepends:
- python-nose
- python-webob
- python-webtest
repo_depends:
- python-repoze.lru
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-routes
  source: archpkg
