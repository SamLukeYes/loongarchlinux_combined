destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''exiv2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- curl
- zlib
- gettext
- expat
update_on:
- alpm: expat
  provides: libexpat.so
  source: alpm
- alpm: curl
  provides: libcurl.so
  source: alpm
- alpm: exiv2
  source: archpkg
