destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-xmlsec'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-hypothesis
- python-pytest
repo_depends:
- python-lxml
- xmlsec
repo_makedepends:
- git
- python-build
- python-installer
- python-pkgconfig
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-xmlsec
  source: archpkg
