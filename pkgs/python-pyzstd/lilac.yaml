destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyzstd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python: python-tests
repo_depends:
- python
- zstd
repo_makedepends:
- python-build
- python-installer
- python-wheel
- python-setuptools-scm
update_on:
- alias: python
- alpm: python-pyzstd
  source: archpkg
