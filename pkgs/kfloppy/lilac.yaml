destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kfloppy'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kxmlgui
- hicolor-icon-theme
- kcompletion
update_on:
- alpm: kfloppy
  source: archpkg
