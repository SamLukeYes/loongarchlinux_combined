destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''khard'')

  '
repo_depends:
- python-configobj
- python-unidecode
- python-vobject
- python-atomicwrites
- python-ruamel-yaml
update_on:
- alpm: khard
  source: archpkg
