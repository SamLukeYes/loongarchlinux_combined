destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libnetfilter_queue'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libnfnetlink
- libmnl
update_on:
- alpm: libnetfilter_queue
  source: archpkg
