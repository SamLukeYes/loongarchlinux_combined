destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''release-plz'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- git
repo_depends:
- gcc: gcc-libs
- curl
repo_makedepends: []
update_on:
- alpm: release-plz
  source: archpkg
