destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gcovr'')

  '
repo_depends:
- python-lxml
- python-jinja
- python
- gcc
update_on:
- alias: python
- alpm: gcovr
  source: archpkg
