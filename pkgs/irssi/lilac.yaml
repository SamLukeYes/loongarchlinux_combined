destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''irssi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- glib2
- openssl
- libotr
- perl
- ncurses
repo_makedepends:
- meson
- ninja
update_on:
- alias: perl
- alpm: ncurses
  provides: libncursesw.so
  source: alpm
- alpm: irssi
  source: archpkg
