destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openldap'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsasl
- libsodium
- perl
- unixodbc
- libtool
- systemd: systemd-libs
update_on:
- alias: perl
- alpm: openldap
  source: archpkg
