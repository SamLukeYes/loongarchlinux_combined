destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hcxtools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssl
- curl
- zlib
update_on:
- alpm: curl
  provides: libcurl.so
  source: alpm
- alpm: hcxtools
  source: archpkg
