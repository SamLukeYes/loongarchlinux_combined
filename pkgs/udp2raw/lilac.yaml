destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''udp2raw'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- iptables
repo_makedepends:
- git
update_on:
- alpm: iptables
  source: alpm
  strip_release: true
- alpm: udp2raw
  source: archpkg
