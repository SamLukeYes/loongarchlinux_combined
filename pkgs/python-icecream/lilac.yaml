destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-icecream'')

  '
repo_checkdepends: []
repo_depends:
- python-colorama
- python-pygments
- python-executing
- python-asttokens
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-icecream
  source: archpkg
