destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mailgraph'')

  '
repo_depends:
- perl-file-tail
- rrdtool
update_on:
- alpm: mailgraph
  source: archpkg
