destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kblackbox'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libkdegames
- hicolor-icon-theme
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: kblackbox
  source: archpkg
