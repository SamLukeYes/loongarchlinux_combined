destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''percona-server'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zstd
- libfido2
- jemalloc
- numactl
- libevent
- curl
- lz4
- zlib
- libtirpc
- icu
- libaio
- readline
- openssl
- protobuf
- pam
update_on:
- alias: readline
- alias: protobuf
- alias: icu
- alpm: percona-server
  source: archpkg
