destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xreader'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- xapp
- poppler: poppler-glib
- webkit2gtk-4.1
update_on:
- alpm: xreader
  source: archpkg
