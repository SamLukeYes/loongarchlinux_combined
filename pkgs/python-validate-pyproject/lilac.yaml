destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-validate-pyproject'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-fastjsonschema
- python-packaging
- python-trove-classifiers
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools-scm
update_on:
- alpm: python-validate-pyproject
  source: archpkg
