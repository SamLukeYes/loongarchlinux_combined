destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-discid'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- libdiscid
- python
repo_makedepends: []
update_on:
- alias: python
- alpm: python-discid
  source: archpkg
