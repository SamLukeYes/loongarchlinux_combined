destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yq'')

  '
repo_checkdepends: []
repo_depends:
- jq
- python-argcomplete
- python-tomlkit
- python-xmltodict
- python-yaml
repo_makedepends:
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: yq
  source: archpkg
