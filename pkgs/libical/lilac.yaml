destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libical'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxml2
- glibc
- glib2
- icu
update_on:
- alias: icu
- alpm: libical
  source: archpkg
