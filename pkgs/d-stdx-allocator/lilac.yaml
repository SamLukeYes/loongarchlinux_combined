destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''d-stdx-allocator'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ldc: liblphobos
- d-mir-core
repo_makedepends:
- meson
- ldc
update_on:
- alpm: d-stdx-allocator
  source: archpkg
