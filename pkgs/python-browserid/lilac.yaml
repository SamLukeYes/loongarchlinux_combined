destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-browserid'')

  '
repo_checkdepends: []
repo_depends:
- python-requests
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-browserid
  source: archpkg
