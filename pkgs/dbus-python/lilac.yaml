destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dbus-python'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- dbus
- glib2
- python
update_on:
- alias: python
- alpm: dbus-python
  source: archpkg
