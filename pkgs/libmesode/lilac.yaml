destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmesode'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- check
repo_depends:
- glibc
- openssl
- expat
- glibc
- openssl
- expat
repo_makedepends:
- doxygen
update_on:
- alpm: expat
  provides: libexpat.so
  source: alpm
- alpm: libmesode
  source: archpkg
