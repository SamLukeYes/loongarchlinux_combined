destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''soapyplutosdr'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- soapysdr
- libad9361
update_on:
- alpm: libiio
  provides: libiio.so
  source: alpm
- alpm: soapyplutosdr
  source: archpkg
