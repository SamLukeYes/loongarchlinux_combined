destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opentimelineio'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python-aaf2
repo_makedepends:
- cmake
- python-setuptools
- pybind11
- git
- imath
update_on:
- alpm: opentimelineio
  source: archpkg
