destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''scenarist'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- hunspell
- qt5-multimedia
- qt5-svg
- qt5-webengine
repo_makedepends:
- git
update_on:
- alpm: scenarist
  source: archpkg
