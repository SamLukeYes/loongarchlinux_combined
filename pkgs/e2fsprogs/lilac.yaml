destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''e2fsprogs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- util-linux: util-linux-libs
repo_makedepends:
- systemd
- util-linux
update_on:
- alpm: e2fsprogs
  source: archpkg
