destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gsocket'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- binutils
- openssh
- grep
- rsync
- bash
- sshfs
- openssl
- glibc
- coreutils
update_on:
- alpm: gsocket
  source: archpkg
