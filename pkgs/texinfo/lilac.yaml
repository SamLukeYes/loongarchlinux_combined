destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''texinfo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl
- gzip
- ncurses
update_on:
- alias: perl
- alpm: texinfo
  source: archpkg
