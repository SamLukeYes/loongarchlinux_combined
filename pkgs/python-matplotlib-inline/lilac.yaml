destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-matplotlib-inline'')

  '
repo_checkdepends:
- ipython
- python-matplotlib
repo_depends:
- python-traitlets
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-matplotlib-inline
  source: archpkg
