destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''trompeloeil'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- catch2
repo_depends: []
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: trompeloeil
  source: archpkg
