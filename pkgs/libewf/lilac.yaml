destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libewf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bzip2
- util-linux
- zlib
update_on:
- alpm: libewf
  source: archpkg
