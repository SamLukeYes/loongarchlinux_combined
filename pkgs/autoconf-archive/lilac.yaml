destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''autoconf-archive'')

  '
repo_depends:
- autoconf
update_on:
- alpm: autoconf-archive
  source: archpkg
