destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-xml-parser'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- expat
- perl-libwww
update_on:
- alpm: perl-xml-parser
  source: archpkg
