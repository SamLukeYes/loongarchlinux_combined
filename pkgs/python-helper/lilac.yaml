destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-helper'')

  '
repo_checkdepends:
- python-nose
- python-coverage
repo_depends:
- python-yaml
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-helper
  source: archpkg
