destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wingpanel-indicator-a11y'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- granite
update_on:
- alpm: granite
  provides: libgranite.so
  source: alpm
- alpm: wingpanel-indicator-a11y
  source: archpkg
