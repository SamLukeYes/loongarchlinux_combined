destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''snapper'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- acl
- json-c
- btrfs-progs
- dbus
- libxml2
- boost: boost-libs
update_on:
- alpm: snapper
  source: archpkg
