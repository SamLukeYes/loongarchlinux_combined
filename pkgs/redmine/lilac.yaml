destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''redmine'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ruby2.7
- imagemagick
- libxml2
- libxslt
repo_makedepends:
- mariadb: mariadb-libs
- postgresql: postgresql-libs
- sqlite
- systemd
update_on:
- alpm: redmine
  source: archpkg
