destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sphinx-pytest'')

  '
repo_checkdepends: []
repo_depends:
- python-sphinx
- python-pytest
repo_makedepends:
- python-build
- python-installer
- python-flit-core
- python-wheel
update_on:
- alpm: python-sphinx-pytest
  source: archpkg
