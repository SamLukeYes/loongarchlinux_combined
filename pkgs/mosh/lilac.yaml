destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mosh'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl-io-tty
- openssh
- openssl
- protobuf
- libutempter
update_on:
- alias: protobuf
- alpm: mosh
  source: archpkg
