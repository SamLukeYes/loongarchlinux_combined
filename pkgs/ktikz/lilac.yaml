destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ktikz'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- poppler: poppler-qt5
- ktexteditor
- kparts
update_on:
- alpm: ktikz
  source: archpkg
