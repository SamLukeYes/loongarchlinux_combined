destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''msmtp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libsecret
- gsasl
repo_makedepends:
- libsecret
- gsasl
update_on:
- alpm: msmtp
  source: archpkg
