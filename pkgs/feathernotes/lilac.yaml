destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''feathernotes'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- hicolor-icon-theme
- hunspell
- qt6-svg
repo_makedepends:
- cmake
- ninja
- qt6-tools
update_on:
- alpm: feathernotes
  source: archpkg
