destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-io-digest'')

  '
repo_depends:
- perlio-via-dynamic
- perl
update_on:
- alias: perl
- alpm: perl-io-digest
  source: archpkg
