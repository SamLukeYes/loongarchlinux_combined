destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyclibrary'')

  '
repo_depends:
- python-future
- python-pyparsing
- python-setuptools
update_on:
- alpm: python-pyclibrary
  source: archpkg
