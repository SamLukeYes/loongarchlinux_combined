destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-rope'')

  '
repo_depends:
- python-pytoolconfig
update_on:
- alpm: python-rope
  source: archpkg
