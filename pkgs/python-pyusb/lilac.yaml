destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyusb'')

  '
repo_checkdepends: []
repo_depends:
- python
- libusb
repo_makedepends:
- git
- python-setuptools-scm
update_on:
- alias: python
- alpm: python-pyusb
  source: archpkg
