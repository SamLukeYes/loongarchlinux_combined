destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libxfce4ui'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- startup-notification
- libepoxy
- libsm
- libgtop
- libxfce4util
- gtk3
- xfconf
- hicolor-icon-theme
update_on:
- alpm: libxfce4ui
  source: archpkg
