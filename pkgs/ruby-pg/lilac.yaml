destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-pg'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby
- glibc
- postgresql: postgresql-libs
- ruby-rspec-core
update_on:
- alias: ruby
- alpm: ruby-pg
  source: archpkg
