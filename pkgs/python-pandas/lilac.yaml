destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pandas'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python-dateutil
- python-numpy
- python-pytz
- python-setuptools
update_on:
- alpm: python-pandas
  source: archpkg
