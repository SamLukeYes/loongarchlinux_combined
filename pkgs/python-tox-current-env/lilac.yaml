destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-tox-current-env'')

  '
repo_depends:
- python-tox
- python
update_on:
- alias: python
- alpm: python-tox-current-env
  source: archpkg
