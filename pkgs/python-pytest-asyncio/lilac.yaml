destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-asyncio'')

  '
repo_checkdepends:
- python-flaky
- python-hypothesis
- python-pytest-trio
repo_depends:
- python-pytest
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-pytest-asyncio
  source: archpkg
