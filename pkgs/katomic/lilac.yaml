destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''katomic'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libkdegames
- perl
- hicolor-icon-theme
update_on:
- alias: perl
- alpm: katomic
  source: archpkg
