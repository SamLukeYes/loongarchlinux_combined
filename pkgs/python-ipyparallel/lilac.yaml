destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ipyparallel'')

  '
repo_checkdepends:
- python-pytest-tornado
- python-pytest-asyncio
- python-tqdm
repo_depends:
- jupyterlab
- python-psutil
repo_makedepends:
- python-build
- python-installer
- python-hatchling
update_on:
- alpm: python-ipyparallel
  source: archpkg
