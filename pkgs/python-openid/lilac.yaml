destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-openid'')

  '
repo_checkdepends:
- python-coverage
- django: python-django
- python-httplib2
- python-mysqlclient
- python-psycopg2
- python-pycurl
- python-pytest
repo_depends:
- python-defusedxml
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-openid
  source: archpkg
