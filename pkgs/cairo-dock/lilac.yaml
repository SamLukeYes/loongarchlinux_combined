destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cairo-dock'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glu
- gtk3
- dbus-glib
- curl
update_on:
- alpm: cairo-dock
  source: archpkg
