destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''blobby2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- sdl2
- physfs
repo_makedepends:
- cmake
- boost
- mesa
- zip
update_on:
- alpm: blobby2
  source: archpkg
