destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''podofo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fontconfig
- libjpeg-turbo
- libtiff
- libidn
- openssl
- lua
update_on:
- alias: lua
- alpm: podofo
  source: archpkg
