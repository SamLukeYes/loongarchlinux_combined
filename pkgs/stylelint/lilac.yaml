destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''stylelint'')

  '
repo_checkdepends: []
repo_depends:
- nodejs
repo_makedepends:
- git
- npm
- rsync
update_on:
- alpm: stylelint
  source: archpkg
