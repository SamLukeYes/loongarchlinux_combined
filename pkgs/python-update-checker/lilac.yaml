destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-update-checker'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-requests
repo_makedepends:
- python-setuptools
- python-requests
update_on:
- alpm: python-update-checker
  source: archpkg
