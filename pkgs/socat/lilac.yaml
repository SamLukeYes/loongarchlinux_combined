destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''socat'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- bash
- net-tools
- procps-ng
repo_depends:
- glibc
- readline
- openssl
repo_makedepends: []
update_on:
- alias: readline
- alpm: readline
  provides: libreadline.so
  source: alpm
- alpm: socat
  source: archpkg
