destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libinstpatch'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib2
- libsndfile
update_on:
- alpm: glib2
  provides: libgobject-2.0.so
  source: alpm
- alpm: libsndfile
  provides: libsndfile.so
  source: alpm
- alpm: libinstpatch
  source: archpkg
