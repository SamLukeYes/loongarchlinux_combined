destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tldr'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-shtab
- python-colorama
- python-termcolor
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-sphinx-argparse
- python-wheel
update_on:
- alpm: tldr
  source: archpkg
