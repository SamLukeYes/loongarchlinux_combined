destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pythondialog'')

  '
repo_depends:
- dialog
- python
update_on:
- alias: python
- alpm: python-pythondialog
  source: archpkg
