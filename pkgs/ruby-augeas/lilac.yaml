destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-augeas'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ruby
- augeas
repo_makedepends:
- rubygems
- ruby-rdoc
update_on:
- alias: ruby
- alpm: ruby-augeas
  source: archpkg
