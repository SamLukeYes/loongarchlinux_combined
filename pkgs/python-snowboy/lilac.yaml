destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-snowboy'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- lapack: cblas
- python-pyaudio
update_on:
- alpm: python-snowboy
  source: archpkg
