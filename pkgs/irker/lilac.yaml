destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''irker'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-pysocks
repo_makedepends:
- docbook-xml
- docbook-xsl
- xmlto
update_on:
- alias: python
- alpm: irker
  source: archpkg
