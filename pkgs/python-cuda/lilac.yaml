destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-cuda'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
- python-pytest-benchmark
- python-numpy
- python-scipy
repo_depends:
- cuda
- cython
- python-pyclibrary
repo_makedepends:
- git
- python-wheel
- python-build
- python-installer
- python-setuptools-scm
update_on:
- alpm: python-cuda
  source: archpkg
