destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''btrfs-progs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zlib
- libgcrypt
- util-linux: util-linux-libs
- zstd
- glibc
- systemd: systemd-libs
- lzo
update_on:
- alpm: btrfs-progs
  source: archpkg
