destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-primecountpy'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cysignals: python-cysignals
- primecount
repo_makedepends:
- cython
- python-setuptools
update_on:
- alpm: python-primecountpy
  source: archpkg
