destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-h5py'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
- python-pytest-mpi
- python-pytables
repo_depends:
- hdf5
- liblzf
- python-numpy
repo_makedepends:
- cython
- python-pkgconfig
- python-setuptools
update_on:
- alpm: python-h5py
  source: archpkg
