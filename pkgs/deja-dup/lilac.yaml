destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''deja-dup'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- duplicity
- gvfs
- libsecret
- pygobject: python-gobject
- python-pydrive2
- libadwaita
repo_makedepends:
- appstream-glib
- itstool
- meson
- vala
update_on:
- alpm: deja-dup
  source: archpkg
