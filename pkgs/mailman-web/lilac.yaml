destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mailman-web'')

  '
repo_checkdepends: []
repo_depends:
- hyperkitty
- postorius
- django: python-django
- python-whoosh
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: mailman-web
  source: archpkg
