destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''erlang-cl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- erlang
repo_makedepends:
- git
- opencl-headers
- rebar
update_on:
- alpm: erlang-cl
  source: archpkg
