destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''unicode-emoji'')

  '
repo_checkdepends: []
repo_depends:
- unicode-character-database
repo_makedepends: []
update_on:
- alpm: unicode-emoji
  source: archpkg
