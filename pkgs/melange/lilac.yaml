destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''melange'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- apk-tools
- alpine-keyring
- bubblewrap
repo_makedepends:
- go
update_on:
- alpm: melange
  source: archpkg
