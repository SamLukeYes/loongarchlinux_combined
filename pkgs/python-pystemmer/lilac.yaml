destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pystemmer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- snowball: libstemmer
repo_makedepends:
- cython
- python-setuptools
update_on:
- alias: python
- alpm: python-pystemmer
  source: archpkg
