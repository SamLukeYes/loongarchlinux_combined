destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- curl
- expat
- perl
- perl-error
- perl-mailtools
- openssl
- pcre2
- grep
- shadow
- zlib
repo_makedepends:
- python
- xmlto
- asciidoc
update_on:
- alias: perl
- alpm: git
  source: archpkg
