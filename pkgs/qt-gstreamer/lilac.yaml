destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qt-gstreamer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- qt5-declarative
- gstreamer: gst-plugins-good
repo_makedepends:
- cmake
- boost
- doxygen
- python
update_on:
- alpm: qt5-declarative
  source: alpm
  strip_release: true
- alpm: qt-gstreamer
  source: archpkg
