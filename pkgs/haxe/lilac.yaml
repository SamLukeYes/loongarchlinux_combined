destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''haxe'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- neko
- mbedtls2
update_on:
- alpm: haxe
  source: archpkg
