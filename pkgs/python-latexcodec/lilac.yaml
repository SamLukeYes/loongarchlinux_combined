destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-latexcodec'')

  '
repo_checkdepends:
- python-nose
- python-pytest
repo_depends:
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-latexcodec
  source: archpkg
