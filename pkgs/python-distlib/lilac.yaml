destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-distlib'')

  '
repo_checkdepends: []
repo_depends:
- python
repo_makedepends:
- python-setuptools
- python-build
- python-installer
- python-wheel
update_on:
- alias: python
- alpm: python-distlib
  source: archpkg
