destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''source-highlight'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bash
- boost: boost-libs
repo_makedepends:
- ctags
- boost
update_on:
- alpm: source-highlight
  source: archpkg
