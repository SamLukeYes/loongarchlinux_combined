destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rsnapshot'')

  '
repo_depends:
- perl-lchown
- perl
- openssh
- rsync
update_on:
- alias: perl
- alpm: rsnapshot
  source: archpkg
