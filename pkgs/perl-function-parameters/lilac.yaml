destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-function-parameters'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- perl-test-fatal
repo_depends: []
repo_makedepends: []
update_on:
- alpm: perl-function-parameters
  source: archpkg
