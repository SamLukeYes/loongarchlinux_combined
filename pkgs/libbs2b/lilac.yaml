destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libbs2b'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsndfile
update_on:
- manual: 1
  source: manual
- alpm: libsndfile
  provides: libsndfile.so
  source: alpm
- alpm: libbs2b
  source: archpkg
