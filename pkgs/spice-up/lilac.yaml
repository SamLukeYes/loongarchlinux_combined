destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''spice-up'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- xorg-server: xorg-server-xvfb
repo_depends:
- granite
- libevdev
- libgudev
- libsoup
repo_makedepends:
- meson
- vala
update_on:
- alpm: spice-up
  source: archpkg
