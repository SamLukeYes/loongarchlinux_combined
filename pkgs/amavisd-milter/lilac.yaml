destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''amavisd-milter'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- amavisd-new
repo_makedepends:
- libmilter
update_on:
- alpm: amavisd-milter
  source: archpkg
