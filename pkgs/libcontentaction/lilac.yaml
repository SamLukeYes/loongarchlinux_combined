destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libcontentaction'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- mlite
- qt5-systems
update_on:
- alpm: libcontentaction
  source: archpkg
