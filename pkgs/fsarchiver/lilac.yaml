destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fsarchiver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bzip2
- xz
- lz4
- e2fsprogs
- zlib
- libgcrypt
- zstd
- lzo
update_on:
- alpm: fsarchiver
  source: archpkg
