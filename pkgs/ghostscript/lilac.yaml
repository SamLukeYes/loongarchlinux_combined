destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ghostscript'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- poppler-data
- fontconfig
- cups: libcups
- libpaper
- libtiff
- zlib
- ijs
- libidn
- dbus
- lcms2
- libxt
- jbig2dec
- openjpeg2
- libpng
update_on:
- alpm: ghostscript
  source: archpkg
