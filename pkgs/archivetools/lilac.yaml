destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''archivetools'')

  '
repo_depends:
- xz
- util-linux
- rsync
- bash
- systemd
update_on:
- alpm: archivetools
  source: archpkg
