destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''toxcore'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsodium
- opus
- libconfig
- libvpx
- systemd
update_on:
- alpm: toxcore
  source: archpkg
