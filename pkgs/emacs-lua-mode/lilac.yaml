destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''emacs-lua-mode'')

  '
update_on:
- alpm: emacs-lua-mode
  source: archpkg
