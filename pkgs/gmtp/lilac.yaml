destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gmtp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- flac
- gtk3
- libmtp
- libid3tag
- libvorbis
repo_makedepends: []
update_on:
- alpm: gmtp
  source: archpkg
