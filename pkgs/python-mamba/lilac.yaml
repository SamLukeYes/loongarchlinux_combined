destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mamba'')

  '
repo_checkdepends:
- python-doublex-expects
repo_depends:
- python-coverage
- python-clint
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-mamba
  source: archpkg
