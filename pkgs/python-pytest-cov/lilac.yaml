destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-cov'')

  '
repo_checkdepends:
- python-virtualenv
- python-process-tests
- python-pytest-xdist
- python-fields
repo_depends:
- python-coverage
- python-pytest
- python-tomli
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-cov
  source: archpkg
