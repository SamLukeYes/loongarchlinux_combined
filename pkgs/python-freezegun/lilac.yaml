destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-freezegun'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-dateutil
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-freezegun
  source: archpkg
