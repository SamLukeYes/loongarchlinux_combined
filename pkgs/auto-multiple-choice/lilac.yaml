destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''auto-multiple-choice'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl
- opencv
update_on:
- alias: perl
- alpm: opencv
  source: alpm
  strip_release: true
- alpm: auto-multiple-choice
  source: archpkg
