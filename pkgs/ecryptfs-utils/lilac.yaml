destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ecryptfs-utils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pam
- nss
update_on:
- alpm: ecryptfs-utils
  source: archpkg
