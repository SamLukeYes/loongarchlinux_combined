destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-dbi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- perl-test-pod
- perl-test-pod-coverage
repo_depends:
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-dbi
  source: archpkg
