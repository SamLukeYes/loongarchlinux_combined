destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-prctl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- iputils
- procps-ng
repo_depends:
- python
- libcap
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-prctl
  source: archpkg
