destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ceres-solver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- google-glog
- eigen
- suitesparse
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: ceres-solver
  source: archpkg
