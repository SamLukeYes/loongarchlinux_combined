destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aws-c-io'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- aws-c-common
- s2n-tls
- aws-c-cal
update_on:
- alpm: aws-c-io
  source: archpkg
