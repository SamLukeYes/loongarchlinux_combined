destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''guile'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ncurses
- libunistring
- gc
- libxcrypt
- libffi
- gmp
update_on:
- alpm: guile
  source: archpkg
