destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''borg'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- acl
- zstd
- xz
- lz4
- python-packaging
- python-msgpack
- openssl
- python
update_on:
- alias: python
- alpm: borg
  source: archpkg
