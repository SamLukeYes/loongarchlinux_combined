destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-async-pool'')

  '
update_on:
- alpm: ruby-async-pool
  source: archpkg
