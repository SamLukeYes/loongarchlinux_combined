destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hwdetect'')

  '
repo_depends:
- gzip
- sed
- grep
- e2fsprogs
- coreutils
update_on:
- alpm: hwdetect
  source: archpkg
