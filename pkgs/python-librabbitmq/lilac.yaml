destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-librabbitmq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- pifpaf
- rabbitmq
repo_depends:
- python-amqp
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-librabbitmq
  source: archpkg
