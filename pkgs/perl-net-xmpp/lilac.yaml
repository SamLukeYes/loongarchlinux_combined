destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-net-xmpp'')

  '
repo_depends:
- perl-xml-stream
- perl-digest-sha1
- perl-digest-hmac
update_on:
- alpm: perl-net-xmpp
  source: archpkg
