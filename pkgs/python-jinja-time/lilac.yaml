destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jinja-time'')

  '
repo_checkdepends:
- python-freezegun
- python-pytest
repo_depends:
- python-arrow
- python-jinja
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-jinja-time
  source: archpkg
