destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libdnf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- rpm-tools
- json-c
- libsolv
- librepo
- gpgme
- zlib
- sqlite
- openssl
- glib2
- libmodulemd
update_on:
- alpm: libdnf
  source: archpkg
