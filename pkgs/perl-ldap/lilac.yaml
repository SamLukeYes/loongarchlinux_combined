destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-ldap'')

  '
repo_depends:
- perl-xml-sax-writer
- perl-convert-asn1
- perl-text-soundex
update_on:
- alpm: perl-ldap
  source: archpkg
