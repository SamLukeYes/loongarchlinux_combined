destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zsh'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- pcre
- libcap
- gdbm
repo_makedepends:
- pcre
- libcap
- gdbm
- yodl
update_on:
- alpm: zsh
  source: archpkg
