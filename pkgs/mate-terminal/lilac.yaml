destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mate-terminal'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libsm
- gettext
- mate-desktop
- vte3
- perl
repo_makedepends:
- itstool
- python
update_on:
- alias: perl
- alpm: mate-terminal
  source: archpkg
