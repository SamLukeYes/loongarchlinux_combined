destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mitmproxy-wireguard'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
repo_makedepends:
- maturin
- python-installer
update_on:
- alias: python
- alpm: python-mitmproxy-wireguard
  source: archpkg
