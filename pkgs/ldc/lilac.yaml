destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ldc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ldc: liblphobos
- llvm: llvm-libs
- gcc
- compiler-rt
- curl
repo_makedepends:
- git
- cmake
- llvm
- ldc
- ninja
update_on:
- alpm: ldc
  source: archpkg
