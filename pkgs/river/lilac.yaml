destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''river'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libevdev
- libxkbcommon
- mesa
- pixman
- wayland
- wlroots
- xorg-xwayland
repo_makedepends:
- scdoc
- wayland-protocols
- zig
update_on:
- alpm: river
  source: archpkg
