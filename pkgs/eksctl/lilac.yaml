destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''eksctl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kubernetes: kubectl
- glibc
repo_makedepends:
- go
update_on:
- alpm: eksctl
  source: archpkg
