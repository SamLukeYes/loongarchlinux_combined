destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libfm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libexif
- gtk2
- gtk3
- glib2
- menu-cache
update_on:
- alpm: libfm
  source: archpkg
