destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-epub-thumbnailer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gdk-pixbuf2
- librsvg
- libarchive
- libxml2
repo_makedepends:
- git
- meson
update_on:
- alpm: gnome-epub-thumbnailer
  source: archpkg
