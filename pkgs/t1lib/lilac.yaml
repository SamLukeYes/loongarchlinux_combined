destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''t1lib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxaw
update_on:
- alpm: t1lib
  source: archpkg
