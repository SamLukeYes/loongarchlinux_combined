destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''slurp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cairo
- wayland
- libxkbcommon
repo_makedepends:
- meson
- scdoc
- wayland-protocols
update_on:
- alpm: slurp
  source: archpkg
