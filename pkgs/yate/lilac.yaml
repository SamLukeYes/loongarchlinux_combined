destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yate'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- alsa-lib
- speex
- postgresql: postgresql-libs
- sqlite
- openssl
- mariadb: mariadb-libs
- gsm
update_on:
- alpm: yate
  source: archpkg
