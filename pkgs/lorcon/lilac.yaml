destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lorcon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- libpcap
- glibc
- python
- lorcon
repo_makedepends:
- glibc
- python
- libpcap
update_on:
- alias: python
- alpm: lorcon
  source: archpkg
