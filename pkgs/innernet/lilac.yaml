destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''innernet'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- sqlite
repo_makedepends:
- rust
- git
- clang
- llvm
update_on:
- alpm: innernet
  source: archpkg
