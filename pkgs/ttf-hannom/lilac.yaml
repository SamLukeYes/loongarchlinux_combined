destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ttf-hannom'')

  '
repo_depends: []
update_on:
- alpm: ttf-hannom
  source: archpkg
