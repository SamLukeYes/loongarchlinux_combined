destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gupnp-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gtksourceview4
- gupnp
- gupnp-av
update_on:
- alpm: gupnp-tools
  source: archpkg
