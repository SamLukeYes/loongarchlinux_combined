destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''deepin-picker'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- deepin-qt5integration
- libxtst
- qt5-svg
repo_makedepends:
- qt5-tools
- deepin-gettext-tools
update_on:
- alpm: deepin-picker
  source: archpkg
