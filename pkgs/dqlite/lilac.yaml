destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dqlite'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libuv
- raft
- sqlite
repo_makedepends:
- tcl
- readline
- zlib
update_on:
- alpm: dqlite
  source: archpkg
