destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libjcat'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- json-glib
- gnutls
- gpgme
repo_makedepends:
- meson
- gobject-introspection
- gtk-doc
- vala
- help2man
update_on:
- alpm: libjcat
  source: archpkg
