destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-colorclass'')

  '
repo_checkdepends:
- python-pytest
- python-docopt
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-poetry-core
update_on:
- alias: python
- alpm: python-colorclass
  source: archpkg
