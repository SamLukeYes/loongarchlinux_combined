destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glava'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- pulseaudio
- libxext
- libxcomposite
- libxrender
repo_makedepends:
- git
- python
update_on:
- alpm: glava
  source: archpkg
