destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''intel-opencl-clang'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- clang
update_on:
- alias: clang
- alpm: intel-opencl-clang
  source: archpkg
