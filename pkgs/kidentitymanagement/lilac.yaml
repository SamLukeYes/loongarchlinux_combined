destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kidentitymanagement'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kpimtextedit
repo_makedepends:
- extra-cmake-modules
- doxygen
- qt5-tools
update_on:
- alpm: kidentitymanagement
  source: archpkg
