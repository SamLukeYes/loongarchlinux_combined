destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zcash'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libevent
- zeromq
- boost: boost-libs
- libsodium
update_on:
- alpm: zcash
  source: archpkg
