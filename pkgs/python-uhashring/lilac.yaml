destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-uhashring'')

  '
repo_checkdepends:
- python-pytest
- python-memcached
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-hatchling
update_on:
- alias: python
- alpm: python-uhashring
  source: archpkg
