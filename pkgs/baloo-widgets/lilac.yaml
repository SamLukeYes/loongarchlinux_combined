destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''baloo-widgets'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- baloo
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: baloo-widgets
  source: archpkg
