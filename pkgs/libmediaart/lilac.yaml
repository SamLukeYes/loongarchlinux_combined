destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmediaart'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- glib2
- gdk-pixbuf2
update_on:
- alpm: libmediaart
  source: archpkg
