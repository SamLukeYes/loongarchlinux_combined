destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fontmath'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-fonttools
- python-setuptools-scm
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alpm: python-fontmath
  source: archpkg
