destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ephemeral-port-reserve'')

  '
repo_depends:
- python
update_on:
- alias: python
- alpm: python-ephemeral-port-reserve
  source: archpkg
