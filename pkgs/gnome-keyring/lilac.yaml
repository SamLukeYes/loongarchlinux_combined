destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-keyring'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssh
- gcr-4
- libcap-ng
- gcr
- pam
update_on:
- alpm: gnome-keyring
  source: archpkg
