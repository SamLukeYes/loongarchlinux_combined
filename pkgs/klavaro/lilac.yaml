destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''klavaro'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- curl
- glib2
- gtk3
- gtkdatabox
- pango
repo_makedepends:
- cairo
- intltool
update_on:
- alpm: klavaro
  source: archpkg
