destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libfprint'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- nss
- pixman
- libgusb
- systemd
- libgudev
update_on:
- alpm: libfprint
  source: archpkg
