destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ccache'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- gcc: gcc-libs
- hiredis
- zstd
repo_makedepends:
- cmake
- asciidoctor
- perl
update_on:
- alpm: zstd
  provides: libzstd.so
  source: alpm
- alpm: ccache
  source: archpkg
