destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-matrix-common'')

  '
repo_checkdepends:
- python-twisted
- python-service-identity
- python-aiounittest
- python-pip
repo_depends:
- python-setuptools
- python-pydantic
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alpm: python-matrix-common
  source: archpkg
