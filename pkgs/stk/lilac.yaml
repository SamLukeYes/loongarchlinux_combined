destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''stk'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- alsa-lib
- gcc: gcc-libs
- glibc
update_on:
- alpm: alsa-lib
  provides: libasound.so
  source: alpm
- alpm: jack2
  provides: libjack.so
  source: alpm
- alpm: stk
  source: archpkg
