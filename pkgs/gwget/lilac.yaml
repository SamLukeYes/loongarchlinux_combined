destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gwget'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
- libnotify
- wget
repo_makedepends:
- intltool
- imagemagick
update_on:
- alpm: gwget
  source: archpkg
