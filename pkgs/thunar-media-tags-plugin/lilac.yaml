destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''thunar-media-tags-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- taglib
- thunar
repo_makedepends:
- intltool
update_on:
- alpm: thunar-media-tags-plugin
  source: archpkg
