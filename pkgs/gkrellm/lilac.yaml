destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gkrellm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk2
- libsm
- lm_sensors
repo_makedepends:
- gettext
update_on:
- alpm: gkrellm
  source: archpkg
