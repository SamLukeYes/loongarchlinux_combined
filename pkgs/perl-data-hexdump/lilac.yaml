destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-data-hexdump'')

  '
repo_depends:
- perl
update_on:
- manual: 1
  source: manual
- alias: perl
- alpm: perl-data-hexdump
  source: archpkg
