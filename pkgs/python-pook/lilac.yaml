destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pook'')

  '
repo_checkdepends:
- python-nose
- python-pytest
- python-aiohttp
- python-requests
repo_depends:
- python-furl
- python-jsonschema
- python-xmltodict
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-pook
  source: archpkg
