destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rocblas'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openmp
update_on:
- alpm: rocblas
  source: archpkg
