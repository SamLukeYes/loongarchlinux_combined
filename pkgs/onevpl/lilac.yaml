destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''onevpl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libdrm
- libva
- wayland
repo_makedepends:
- cmake
- libx11
- wayland-protocols
update_on:
- alpm: onevpl
  source: archpkg
