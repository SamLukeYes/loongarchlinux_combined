destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dolphin-plugins'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- dolphin
repo_makedepends:
- extra-cmake-modules
- ktexteditor
update_on:
- alpm: dolphin-plugins
  source: archpkg
