destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pageedit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt6-webengine
- qt6-5compat
- qt6-svg
update_on:
- alpm: pageedit
  source: archpkg
