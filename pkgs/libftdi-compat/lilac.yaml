destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libftdi-compat'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb-compat
- gcc: gcc-libs
update_on:
- alpm: libftdi-compat
  source: archpkg
