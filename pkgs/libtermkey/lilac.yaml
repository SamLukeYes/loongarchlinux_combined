destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libtermkey'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- unibilium
update_on:
- alpm: unibilium
  provides: libunibilium.so
  source: alpm
- alpm: libtermkey
  source: archpkg
