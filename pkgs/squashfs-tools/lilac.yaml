destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''squashfs-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- xz
- lz4
- zlib
- zstd
- glibc
- lzo
update_on:
- alpm: squashfs-tools
  source: archpkg
