destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ansible'')

  '
repo_depends:
- ansible-core
- python
update_on:
- alias: python
- alpm: ansible
  source: archpkg
