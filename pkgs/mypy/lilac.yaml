destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mypy'')

  '
repo_depends:
- python-psutil
- python-tomli
- python-mypy_extensions
- python-typing_extensions
update_on:
- alpm: mypy
  source: archpkg
