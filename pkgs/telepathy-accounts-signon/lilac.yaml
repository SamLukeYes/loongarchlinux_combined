destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''telepathy-accounts-signon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- telepathy-mission-control
- libaccounts-glib
- libsignon-glib
update_on:
- alpm: telepathy-accounts-signon
  source: archpkg
