destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-extutils-config'')

  '
update_on:
- alpm: perl-extutils-config
  source: archpkg
