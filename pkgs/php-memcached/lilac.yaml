destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''php-memcached'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- php
- php-legacy
repo_makedepends:
- php
- php-legacy
- git
update_on:
- alpm: php-memcached
  source: archpkg
