destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fuse3'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fuse3: fuse-common
- glibc
repo_makedepends:
- meson
update_on:
- alpm: fuse3
  source: archpkg
