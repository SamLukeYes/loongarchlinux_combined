destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''deepin-reader'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- poppler: poppler-qt5
- libspectre
- djvulibre
- karchive
- libchardet
- deepin-qt5integration
- qt5-webengine
update_on:
- alpm: deepin-reader
  source: archpkg
