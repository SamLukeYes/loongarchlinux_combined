destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zmap'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gmp
- hiredis
- json-c
- libpcap
repo_makedepends:
- byacc
- cmake
- gengetopt
update_on:
- alpm: zmap
  source: archpkg
