destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-hcloud'')

  '
repo_depends:
- python-future
- python-requests
- python-dateutil
update_on:
- alpm: python-hcloud
  source: archpkg
