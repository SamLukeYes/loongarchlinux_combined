destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mako'')

  '
repo_checkdepends:
- python-pytest
- python-babel
- python-beaker
- python-dogpile.cache
- python-pygments
repo_depends:
- python-markupsafe
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-mako
  source: archpkg
