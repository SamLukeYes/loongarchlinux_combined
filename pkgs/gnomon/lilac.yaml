destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnomon'')

  '
repo_checkdepends: []
repo_depends:
- nodejs
- semver
repo_makedepends:
- npm
update_on:
- alpm: gnomon
  source: archpkg
