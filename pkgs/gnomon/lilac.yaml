destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnomon'')

  '
repo_depends:
- semver
- nodejs
update_on:
- alpm: gnomon
  source: archpkg
