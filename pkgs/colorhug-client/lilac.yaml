destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''colorhug-client'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- colord-gtk
- libcanberra
update_on:
- alpm: colorhug-client
  source: archpkg
