destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-cvxopt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- suitesparse
repo_makedepends:
- gsl
- fftw
- glpk
- dsdp
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alias: python
- alpm: python-cvxopt
  source: archpkg
