destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libgtop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
- glibc
- libxau
repo_makedepends:
- gobject-introspection
update_on:
- alpm: libgtop
  source: archpkg
