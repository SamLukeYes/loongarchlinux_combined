destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-xxhash'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- xxhash
repo_makedepends:
- git
- python-setuptools-scm
- python-build
- python-installer
- python-wheel
update_on:
- alias: python
- alpm: python-xxhash
  source: archpkg
