destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''facile'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- ocaml
- ocaml: ocaml-compiler-libs
update_on:
- alpm: facile
  source: archpkg
