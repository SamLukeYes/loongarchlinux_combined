destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zstd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- gcc: gcc-libs
- zlib
- xz
- lz4
repo_makedepends:
- cmake
- gtest
- ninja
update_on:
- alpm: zstd
  source: archpkg
