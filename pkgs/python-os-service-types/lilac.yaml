destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-os-service-types'')

  '
repo_checkdepends:
- python-oslotest
- python-requests-mock
repo_depends:
- python-pbr
repo_makedepends:
- git
update_on:
- alpm: python-os-service-types
  source: archpkg
