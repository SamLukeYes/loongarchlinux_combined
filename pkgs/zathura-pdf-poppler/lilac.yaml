destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zathura-pdf-poppler'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- desktop-file-utils
- poppler: poppler-glib
- zathura
update_on:
- alpm: zathura-pdf-poppler
  source: archpkg
