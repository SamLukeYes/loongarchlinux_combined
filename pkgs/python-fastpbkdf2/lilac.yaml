destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fastpbkdf2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest-runner
repo_depends:
- python-cffi
- python-six
repo_makedepends:
- python-setuptools
- git
update_on:
- alpm: python-fastpbkdf2
  source: archpkg
