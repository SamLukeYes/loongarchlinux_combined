destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fio'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gperftools
- numactl
- glusterfs
- libaio
- bash
- python
update_on:
- alias: python
- alpm: fio
  source: archpkg
