destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bluez'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libical
- alsa-lib
- glibc
# - cups
- json-c
- dbus
- glib2
- systemd
update_on:
- alpm: bluez
  source: archpkg
