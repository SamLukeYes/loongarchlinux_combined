destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''squid'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- systemd: systemd-libs
- tdb
- perl
- libcap
- libxml2
- gnutls
- openssl
- libnsl
- nettle
- pam
update_on:
- alias: perl
- alpm: squid
  source: archpkg
