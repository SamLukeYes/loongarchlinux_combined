destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yaz'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gnutls
- libxslt
- icu
repo_makedepends:
- zsh
update_on:
- alias: icu
- alpm: yaz
  source: archpkg
