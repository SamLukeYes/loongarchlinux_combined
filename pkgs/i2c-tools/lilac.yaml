destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''i2c-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl
update_on:
- alias: perl
- alpm: i2c-tools
  source: archpkg
