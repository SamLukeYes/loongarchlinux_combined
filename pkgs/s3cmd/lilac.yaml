destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''s3cmd'')

  '
repo_depends:
- python-magic
- python-dateutil
update_on:
- alpm: s3cmd
  source: archpkg
