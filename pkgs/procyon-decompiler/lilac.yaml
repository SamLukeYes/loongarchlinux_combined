destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''procyon-decompiler'')

  '
update_on:
- alpm: procyon-decompiler
  source: archpkg
