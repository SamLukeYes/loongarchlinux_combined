destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ipdb'')

  '
repo_checkdepends: []
repo_depends:
- ipython
repo_makedepends:
- python-setuptools
- python-tomli
update_on:
- alpm: python-ipdb
  source: archpkg
