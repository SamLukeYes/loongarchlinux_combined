destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-env-shellwords'')

  '
repo_checkdepends: []
repo_depends:
- perl
repo_makedepends:
- perl-test2-suite
update_on:
- alias: perl
- alpm: perl-env-shellwords
  source: archpkg
