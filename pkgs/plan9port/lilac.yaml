destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plan9port'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fontconfig
- xorg-server
- fuse2
- libxt
update_on:
- alpm: plan9port
  source: archpkg
