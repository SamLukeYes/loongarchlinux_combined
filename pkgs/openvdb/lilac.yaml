destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openvdb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- jemalloc
- zlib
- log4cplus
- blosc
- boost: boost-libs
update_on:
- alpm: openvdb
  source: archpkg
