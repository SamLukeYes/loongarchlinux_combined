destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tesseract'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- curl
- leptonica
- libarchive
#- tesseract-data: tesseract-data-osd
update_on:
- alpm: tesseract
  source: archpkg
