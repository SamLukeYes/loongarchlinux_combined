destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lockfile-progs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- liblockfile
repo_makedepends:
- liblockfile
update_on:
- alpm: lockfile-progs
  source: archpkg
