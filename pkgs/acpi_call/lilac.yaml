destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''acpi_call'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- linux
repo_makedepends:
- linux: linux-headers
- acpi_call-dkms
update_on:
- alpm: acpi_call
  source: archpkg
