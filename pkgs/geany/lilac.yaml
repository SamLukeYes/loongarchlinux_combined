destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''geany'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
repo_makedepends:
- doxygen
- intltool
- python-lxml
update_on:
- alpm: geany
  source: archpkg
