destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kcm-wacomtablet'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- xf86-input-wacom
- plasma-workspace
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: kcm-wacomtablet
  source: archpkg
