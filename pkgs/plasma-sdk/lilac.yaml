destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plasma-sdk'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- plasma-framework
- perl
repo_makedepends:
- extra-cmake-modules
- kdoctools
- ktexteditor
update_on:
- alias: perl
- alpm: plasma-sdk
  source: archpkg
