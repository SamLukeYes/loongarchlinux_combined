destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-regressions'')

  '
repo_checkdepends: []
repo_depends:
- python-pytest
- python-pytest-datadir
- python-yaml
repo_makedepends:
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-pytest-regressions
  source: archpkg
