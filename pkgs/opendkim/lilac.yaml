destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opendkim'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- db
- openssl
- libbsd
- opendbx
- openldap: libldap
repo_makedepends:
- libmilter
- git
update_on:
- alpm: opendkim
  source: archpkg
