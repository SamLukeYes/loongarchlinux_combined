destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libcork'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python
repo_depends:
- glibc
repo_makedepends:
- cmake
- check
- git
update_on:
- alpm: libcork
  source: archpkg
