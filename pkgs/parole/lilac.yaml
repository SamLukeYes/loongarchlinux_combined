destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''parole'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- dbus-glib
- libnotify
- gstreamer: gst-plugins-good
- libxfce4ui
- gstreamer: gst-plugins-base
update_on:
- alpm: parole
  source: archpkg
