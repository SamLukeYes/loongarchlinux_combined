destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''agda-stdlib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- agda
repo_makedepends:
- ghc
- haskell-filemanip
update_on:
- alpm: agda-stdlib
  source: archpkg
