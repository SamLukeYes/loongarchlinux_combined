destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kbackup'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libarchive
- hicolor-icon-theme
- kio
update_on:
- alpm: kbackup
  source: archpkg
