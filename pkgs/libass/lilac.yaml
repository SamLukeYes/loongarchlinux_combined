destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libass'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fontconfig
- glibc
- harfbuzz
- fribidi
- glib2
update_on:
- alpm: freetype2
  provides: libfreetype.so
  source: alpm
- alpm: libass
  source: archpkg
