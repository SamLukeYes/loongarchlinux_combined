destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-zope-copy'')

  '
repo_depends:
- python-zope-interface
- python-setuptools
update_on:
- alpm: python-zope-copy
  source: archpkg
