destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''autoconf'')

  '
repo_depends:
- perl
update_on:
- alias: perl
- alpm: autoconf
  source: archpkg
