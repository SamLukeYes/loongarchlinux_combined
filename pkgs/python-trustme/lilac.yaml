destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-trustme'')

  '
repo_checkdepends:
- python-pytest
- python-service-identity
repo_depends:
- python
- python-cryptography
- python-idna
- python-pyopenssl
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-trustme
  source: archpkg
