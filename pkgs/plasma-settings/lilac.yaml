destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plasma-settings'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kirigami-addons
- modemmanager-qt
- networkmanager-qt
- plasma-framework
update_on:
- alpm: plasma-settings
  source: archpkg
