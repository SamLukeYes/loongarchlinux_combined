destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-django'')

  '
repo_checkdepends:
- django: python-django
- python-pytest-xdist
repo_depends:
- python-pytest
repo_makedepends:
- git
- python-setuptools-scm
update_on:
- alpm: python-pytest-django
  source: archpkg
