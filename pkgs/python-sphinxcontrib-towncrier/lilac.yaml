destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sphinxcontrib-towncrier'')

  '
repo_checkdepends:
- python-pytest-xdist
repo_depends:
- python-sphinx
- towncrier
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-sphinxcontrib-towncrier
  source: archpkg
