destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''audex'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libkcddb
- hicolor-icon-theme
- kcmutils
- qt5-script
repo_makedepends:
- extra-cmake-modules
- git
update_on:
- alpm: audex
  source: archpkg
