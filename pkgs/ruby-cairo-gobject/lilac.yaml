destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-cairo-gobject'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby-cairo
- ruby
- ruby-glib2
update_on:
- alias: ruby
- alpm: ruby-cairo-gobject
  source: archpkg
