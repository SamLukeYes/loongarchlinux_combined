destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''scapy'')

  '
repo_depends:
- python
- tcpdump
update_on:
- alias: python
- alias: python
- alpm: scapy
  source: archpkg
