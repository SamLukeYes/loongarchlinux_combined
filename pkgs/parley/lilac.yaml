destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''parley'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- knewstuff
- libkeduvocdocument
- qt5-webengine
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: parley
  source: archpkg
