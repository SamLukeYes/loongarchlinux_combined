destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-ntlm'')

  '
repo_checkdepends: []
repo_depends:
- perl
repo_makedepends:
- perl-digest-hmac
update_on:
- alias: perl
- alpm: perl-ntlm
  source: archpkg
