destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rime-cantonese'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- rime-stroke
- rime-emoji
- rime-luna-pinyin
- rime-cangjie
- rime-loengfan
update_on:
- alpm: rime-cantonese
  source: archpkg
