destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jsonmerge'')

  '
repo_checkdepends:
- python-pytest-runner
repo_depends:
- python-jsonschema
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-jsonmerge
  source: archpkg
