destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-dbi-shell'')

  '
repo_depends:
- perl-text-reform
- perl-io-tee
update_on:
- alpm: perl-dbi-shell
  source: archpkg
