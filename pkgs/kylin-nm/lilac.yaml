destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kylin-nm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gsettings-qt
- qt5-x11extras
- qt5-svg
- kwindowsystem
update_on:
- alpm: kylin-nm
  source: archpkg
