destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ponyc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- pcre2
repo_depends:
- llvm: llvm-libs
repo_makedepends:
- benchmark
- cmake
- gtest
- llvm
update_on:
- alpm: ponyc
  source: archpkg
