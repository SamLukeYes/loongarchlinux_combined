destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''android-udev'')

  '
repo_checkdepends: []
repo_depends:
- libmtp
repo_makedepends: []
update_on:
- alpm: android-udev
  source: archpkg
