destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kfind'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kio
- kfilemetadata
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: kfind
  source: archpkg
