destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyerfa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- erfa
- glibc
- python
- python-numpy
repo_makedepends:
- python-build
- python-installer
- python-jinja
- python-packaging
- python-setuptools-scm
- python-wheel
update_on:
- alias: python
- alpm: python-pyerfa
  source: archpkg
