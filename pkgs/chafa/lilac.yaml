destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''chafa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- imagemagick
- libwebp
- libxslt
- librsvg
update_on:
- alpm: chafa
  source: archpkg
