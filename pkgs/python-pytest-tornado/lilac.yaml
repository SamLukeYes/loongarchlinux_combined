destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-tornado'')

  '
repo_depends:
- python-setuptools
- python-tornado
- python-pytest
update_on:
- alpm: python-pytest-tornado
  source: archpkg
