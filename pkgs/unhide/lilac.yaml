destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''unhide'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- iproute2
- lsof
- psmisc
- net-tools
- procps-ng
repo_makedepends: []
update_on:
- alpm: unhide
  source: archpkg
