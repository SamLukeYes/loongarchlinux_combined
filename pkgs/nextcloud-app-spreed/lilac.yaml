destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nextcloud-app-spreed'')

  '
repo_checkdepends:
- yq
repo_depends: []
repo_makedepends:
- nextcloud
update_on:
- alpm: nextcloud-app-spreed
  source: archpkg
