destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libvirt-glib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libvirt
- libcap-ng
repo_makedepends:
- meson
- intltool
- gobject-introspection
- vala
- python
update_on:
- alpm: libvirt-glib
  source: archpkg
