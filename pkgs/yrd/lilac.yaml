destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yrd'')

  '
repo_checkdepends: []
repo_depends:
- python-argh
- python-requests
- cjdns
repo_makedepends:
- python-setuptools
update_on:
- alpm: yrd
  source: archpkg
