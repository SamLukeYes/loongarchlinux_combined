destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xfce4-mpc-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- xfce4-panel
- libmpd
repo_makedepends:
- intltool
update_on:
- alpm: xfce4-mpc-plugin
  source: archpkg
