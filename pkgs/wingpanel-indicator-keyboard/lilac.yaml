destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wingpanel-indicator-keyboard'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
- gtk3
- ibus
- libxml2
- wingpanel
repo_makedepends:
- git
- meson
- vala
update_on:
- alpm: wingpanel-indicator-keyboard
  source: archpkg
