destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''purpose'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kaccounts-integration
- kirigami2
- accounts-qml-module
repo_makedepends:
- extra-cmake-modules
- intltool
update_on:
- alpm: purpose
  source: archpkg
