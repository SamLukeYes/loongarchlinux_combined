destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''obsidian'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- electron
- hicolor-icon-theme
- zlib
update_on:
- alpm: obsidian
  source: archpkg
