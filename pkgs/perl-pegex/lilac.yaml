destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-pegex'')

  '
repo_depends:
- perl-yaml-pp
- perl-file-sharedir-install
- perl-yaml-libyaml
update_on:
- alpm: perl-pegex
  source: archpkg
