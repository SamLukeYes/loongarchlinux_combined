destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-devel-symdump'')

  '
repo_depends: []
update_on:
- alpm: perl-devel-symdump
  source: archpkg
