destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libcec'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- p8-platform
- libxrandr
repo_makedepends:
- cmake
- swig
- python
update_on:
- alpm: libcec
  source: archpkg
