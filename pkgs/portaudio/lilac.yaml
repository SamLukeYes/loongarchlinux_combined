destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''portaudio'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
- gcc: gcc-libs
- glibc
- alsa-lib
repo_makedepends:
- alsa-lib
- cmake
update_on:
- alpm: alsa-lib
  provides: libasound.so
  source: alpm
- alpm: jack2
  provides: libjack.so
  source: alpm
- alpm: portaudio
  source: archpkg
