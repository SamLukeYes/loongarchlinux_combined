destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''suscan'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- sigutils
- libsndfile
- fftw
- soapysdr
- libxml2
- libvolk
repo_makedepends:
- cmake
update_on:
- alpm: suscan
  source: archpkg
