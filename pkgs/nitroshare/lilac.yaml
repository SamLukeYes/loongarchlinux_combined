destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nitroshare'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python
- qt5-svg
- hicolor-icon-theme
- qmdnsengine
- qhttpengine
update_on:
- alias: python
- alpm: nitroshare
  source: archpkg
