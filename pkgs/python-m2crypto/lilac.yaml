destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-m2crypto'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- openssl
repo_makedepends:
- python-setuptools
- swig
update_on:
- alias: python
- alpm: python-m2crypto
  source: archpkg
