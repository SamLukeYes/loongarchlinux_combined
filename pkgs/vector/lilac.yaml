destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vector'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- cargo-nextest
repo_depends:
- gcc: gcc-libs
- zlib
repo_makedepends:
- rustup
- protobuf
- python
- perl
- cmake
- git
update_on:
- alpm: vector
  source: archpkg
