destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-em-proxy'')

  '
repo_depends:
- ruby
- ruby-eventmachine
update_on:
- alias: ruby
- alpm: ruby-em-proxy
  source: archpkg
