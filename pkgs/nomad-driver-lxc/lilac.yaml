destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nomad-driver-lxc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nomad
- lxc
repo_makedepends:
- git
- go
update_on:
- alpm: nomad-driver-lxc
  source: archpkg
