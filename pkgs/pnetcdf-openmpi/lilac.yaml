destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pnetcdf-openmpi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- inetutils
repo_depends:
- openmpi
- netcdf-openmpi
repo_makedepends:
- gcc: gcc-fortran
update_on:
- alpm: pnetcdf-openmpi
  source: archpkg
