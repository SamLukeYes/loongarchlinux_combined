destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmysofa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- nodejs
repo_depends:
- zlib
repo_makedepends:
- cmake
- cunit
- ninja
update_on:
- alpm: libmysofa
  source: archpkg
