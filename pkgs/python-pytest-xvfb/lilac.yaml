destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-xvfb'')

  '
repo_checkdepends: []
repo_depends:
- python-pytest
- python-pyvirtualdisplay
- xorg-server: xorg-server-xvfb
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-xvfb
  source: archpkg
