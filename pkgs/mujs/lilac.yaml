destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mujs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- readline
update_on:
- manual: 1
  source: manual
- alias: readline
- alpm: mujs
  source: archpkg
