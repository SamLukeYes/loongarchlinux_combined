destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mruby'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- readline
repo_makedepends:
- bison
- ruby
- ruby-rake
update_on:
- alias: readline
- alpm: mruby
  source: archpkg
