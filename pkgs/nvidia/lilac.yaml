destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nvidia'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- linux
- nvidia-utils
- libglvnd
repo_makedepends:
- linux: linux-headers
- nvidia-utils: nvidia-dkms
update_on:
- alpm: nvidia
  source: archpkg
