destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libappimage'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libarchive
- boost: boost-libs
- squashfuse
- librsvg
update_on:
- alpm: libappimage
  source: archpkg
