destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-html5-parser'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-beautifulsoup4
repo_depends:
- python-chardet
- python-lxml
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-html5-parser
  source: archpkg
