destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''telepathy-morse'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- telepathy-qt
- telegram-qt
repo_makedepends:
- cmake
update_on:
- alpm: telepathy-morse
  source: archpkg
