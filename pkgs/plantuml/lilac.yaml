destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plantuml'')

  '
repo_checkdepends: []
repo_depends:
- bash
- bash
repo_makedepends: []
update_on:
- alpm: plantuml
  source: archpkg
