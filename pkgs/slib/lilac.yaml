destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''slib'')

  '
repo_depends:
- guile
update_on:
- alpm: guile
  source: alpm
  strip_release: true
- alpm: slib
  source: archpkg
