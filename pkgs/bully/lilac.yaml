destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bully'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libpcap
- glibc
- aircrack-ng
update_on:
- alpm: bully
  source: archpkg
