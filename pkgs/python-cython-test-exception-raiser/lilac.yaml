destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-cython-test-exception-raiser'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
repo_makedepends:
- python-setuptools
- cython
update_on:
- alias: python
- alpm: python-cython-test-exception-raiser
  source: archpkg
