destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-black'')

  '
repo_checkdepends: []
repo_depends:
- python-black
- python-pytest
- python-toml
repo_makedepends:
- git
- python-setuptools-scm
update_on:
- alpm: python-pytest-black
  source: archpkg
