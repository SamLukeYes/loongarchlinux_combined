destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-carp-always'')

  '
repo_checkdepends:
- perl-test-base
repo_depends:
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-carp-always
  source: archpkg
