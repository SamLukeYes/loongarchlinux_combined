destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-mpl'')

  '
repo_depends:
- python-jinja
- python-matplotlib
- python-packaging
- python-pytest
update_on:
- alpm: python-pytest-mpl
  source: archpkg
