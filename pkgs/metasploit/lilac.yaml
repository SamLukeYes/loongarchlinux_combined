destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''metasploit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ruby
- ruby-bundler
- libpcap
- postgresql: postgresql-libs
- sqlite
- libxslt
- libxml2
- inetutils
- git
repo_makedepends: []
update_on:
- alias: ruby
- alpm: metasploit
  source: archpkg
