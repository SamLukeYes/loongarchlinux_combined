destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gphoto2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libgphoto2
- popt
update_on:
- alpm: gphoto2
  source: archpkg
