destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xtrabackup'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl-dbd-mysql
- numactl
- curl
- libaio
- libgcrypt
- libev
- procps-ng
- protobuf
update_on:
- alias: protobuf
- alpm: xtrabackup
  source: archpkg
