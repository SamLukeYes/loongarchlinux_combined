destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''birdfont'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libgee
- libxmlbird
- webkit2gtk
repo_makedepends:
- python
- vala
update_on:
- alpm: birdfont
  source: archpkg
