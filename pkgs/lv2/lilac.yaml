destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lv2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsndfile
- libsamplerate
update_on:
- alpm: lv2
  source: archpkg
