destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-nptyping'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python-numpy
- python-typing_extensions
update_on:
- alpm: python-nptyping
  source: archpkg
