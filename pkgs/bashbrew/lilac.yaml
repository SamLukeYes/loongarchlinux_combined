destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bashbrew'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- docker
- git
repo_makedepends:
- go
- git
update_on:
- alpm: bashbrew
  source: archpkg
