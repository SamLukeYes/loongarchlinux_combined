destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-branchless'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- git
repo_depends:
- git
- libgit2
repo_makedepends: []
update_on:
- alpm: git-branchless
  source: archpkg
