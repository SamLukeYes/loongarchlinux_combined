destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pkcs11-helper'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gnutls
- nss
- openssl
repo_makedepends: []
update_on:
- alpm: pkcs11-helper
  source: archpkg
