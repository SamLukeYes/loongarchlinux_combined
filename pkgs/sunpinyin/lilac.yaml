destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sunpinyin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sqlite
- make
- gcc: gcc-libs
update_on:
- alpm: sunpinyin
  source: archpkg
