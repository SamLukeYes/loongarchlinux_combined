destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-trio'')

  '
repo_depends:
- python-async_generator
- python-hypothesis
- python-trio
- python-pytest
- python
update_on:
- alias: python
- alpm: python-pytest-trio
  source: archpkg
