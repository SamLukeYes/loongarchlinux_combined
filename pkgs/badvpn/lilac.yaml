destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''badvpn'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nss
- openssl
- iproute2
repo_makedepends:
- cmake
update_on:
- alpm: badvpn
  source: archpkg
