destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''avrdude'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- elfutils
- hidapi
- libftdi
- libusb-compat
- readline
repo_makedepends:
- cmake
- ninja
update_on:
- alias: readline
- alpm: avrdude
  source: archpkg
