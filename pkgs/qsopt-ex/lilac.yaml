destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qsopt-ex'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bzip2
- gmp
- zlib
update_on:
- alpm: qsopt-ex
  source: archpkg
