destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lifeograph'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- enchant
- gtkmm3
- libchamplain
update_on:
- alpm: lifeograph
  source: archpkg
