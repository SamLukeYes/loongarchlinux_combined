destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-cppy'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-setuptools
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-cppy
  source: archpkg
