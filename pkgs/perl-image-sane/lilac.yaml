destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-image-sane'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- perl
- sane
- perl-extutils-depends
- perl-extutils-pkgconfig
- perl-readonly
- perl-test-requires
- perl-exception-class
- perl-try-tiny
repo_makedepends:
- imagemagick
update_on:
- alias: perl
- alpm: perl-image-sane
  source: archpkg
