destsubdir: extra
repo_depends:
- acl
- alsa-lib
- dbus
- fontconfig
- freetype2
- gtk3
- gdk-pixbuf2
- giflib
- glib2
- gpm
- harfbuzz
- darktable
- ncurses
- pango
- librsvg
- systemd: systemd-libs
- libxml2
update_on:
- manual: 1
  source: manual
update_on_soname:
- libs:
  - libacl.so
  pkgname: acl
- libs:
  - libasound.so
  pkgname: alsa-lib
- libs:
  - libdbus-1.so
  pkgname: dbus
- libs:
  - libfontconfig.so
  pkgname: fontconfig
- libs:
  - libfreetype.so
  pkgname: freetype2
- libs:
  - libgdk-3.so
  - libgtk-3.so
  pkgname: gtk3
- libs:
  - libgdk_pixbuf-2.0.so
  pkgname: gdk-pixbuf2
- libs:
  - libgif.so
  pkgname: giflib
- libs:
  - libgio-2.0.so
  - libglib-2.0.so
  - libgobject-2.0.so
  pkgname: glib2
- libs:
  - libgpm.so
  pkgname: gpm
- libs:
  - libharfbuzz.so
  pkgname: harfbuzz
- libs:
  - libjpeg.so
  - libtiff.so
  pkgname: darktable
- libs:
  - libncursesw.so
  pkgname: ncurses
- libs:
  - libpango-1.0.so
  pkgname: pango
- libs:
  - librsvg-2.so
  pkgname: librsvg
- libs:
  - libsystemd.so
  pkgbase: systemd
  pkgname: systemd-libs
- libs:
  - libxml2.so
  pkgname: libxml2
