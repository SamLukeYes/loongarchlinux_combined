destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jc'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-pygments
- python-ruamel-yaml
- python-xmltodict
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: jc
  source: archpkg
