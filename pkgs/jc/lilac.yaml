destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jc'')

  '
repo_depends:
- python-ruamel-yaml
- python-xmltodict
- python
- python-pygments
update_on:
- alias: python
- alpm: jc
  source: archpkg
