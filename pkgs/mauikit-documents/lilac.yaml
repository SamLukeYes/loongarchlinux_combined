destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mauikit-documents'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- mauikit
- kio
- kfilemetadata
repo_makedepends:
- extra-cmake-modules
- poppler: poppler-qt5
update_on:
- alpm: mauikit-documents
  source: archpkg
