destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libforensic1394'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- python
update_on:
- alias: python
- alpm: libforensic1394
  source: archpkg
