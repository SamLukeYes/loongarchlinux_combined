destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''power-profiles-daemon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-dbusmock
- python-isort
- python-mccabe
- umockdev
repo_depends:
- upower
- polkit
repo_makedepends:
- meson
update_on:
- alpm: power-profiles-daemon
  source: archpkg
