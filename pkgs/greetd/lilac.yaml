destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''greetd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
- systemd
- gcc: gcc-libs
- glibc
- systemd
- pam
- gcc: gcc-libs
repo_makedepends:
- scdoc
update_on:
- alpm: greetd
  source: archpkg
