destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- oniguruma
repo_makedepends:
- autoconf
- automake
- bison
- flex
- python
update_on:
- alpm: jq
  source: archpkg
