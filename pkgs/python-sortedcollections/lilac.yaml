destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sortedcollections'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-cov
repo_depends:
- python
- python-sortedcontainers
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-sortedcollections
  source: archpkg
