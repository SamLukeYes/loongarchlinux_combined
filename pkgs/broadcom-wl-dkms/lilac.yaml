destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''broadcom-wl-dkms'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- dkms
repo_makedepends: []
update_on:
- alpm: broadcom-wl-dkms
  source: archpkg
