destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''duplicity'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- librsync
- python-fasteners
- python-paramiko
- python-future
- gnupg
update_on:
- alpm: duplicity
  source: archpkg
