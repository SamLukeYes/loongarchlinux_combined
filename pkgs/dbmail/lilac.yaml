destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dbmail'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libevent
- mhash
- gmime3
- libzdb
update_on:
- alpm: dbmail
  source: archpkg
