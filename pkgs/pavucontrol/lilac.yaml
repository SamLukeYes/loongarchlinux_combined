destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pavucontrol'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- json-glib
- gtkmm3
- libsigc++
- libcanberra
update_on:
- alpm: pavucontrol
  source: archpkg
