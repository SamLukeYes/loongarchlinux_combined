destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sagetex'')

  '
repo_checkdepends: []
repo_depends:
- sagemath
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alpm: sagetex
  source: archpkg
