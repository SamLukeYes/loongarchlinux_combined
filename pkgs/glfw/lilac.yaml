destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glfw'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxkbcommon
- libxrandr
- libxi
- wayland
- libxinerama
- libxcursor
update_on:
- alpm: glfw
  source: archpkg
