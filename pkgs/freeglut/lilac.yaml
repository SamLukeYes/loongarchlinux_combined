destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''freeglut'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxi
- libxrandr
repo_makedepends:
- mesa
- glu
- libxxf86vm
- cmake
- ninja
- git
update_on:
- alpm: freeglut
  source: archpkg
