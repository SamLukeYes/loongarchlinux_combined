destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''java-commons-daemon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libcap
repo_makedepends:
- docbook2x
- docbook-utils
- docbook-xsl
- git
- maven
update_on:
- alpm: java-commons-daemon
  source: archpkg
