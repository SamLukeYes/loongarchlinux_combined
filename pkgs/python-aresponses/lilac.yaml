destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-aresponses'')

  '
repo_checkdepends:
- python-pytest-cov
repo_depends:
- python-aiohttp
- python-pytest-asyncio
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-aresponses
  source: archpkg
