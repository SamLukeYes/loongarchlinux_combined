destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-parallel_tests'')

  '
repo_depends:
- ruby
update_on:
- alias: ruby
- alpm: ruby-parallel_tests
  source: archpkg
