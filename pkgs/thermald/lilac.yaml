destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''thermald'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- dbus-glib
- libxml2
- libevdev
- upower
repo_makedepends:
- autoconf-archive
- python
- gtk-doc
update_on:
- alpm: thermald
  source: archpkg
