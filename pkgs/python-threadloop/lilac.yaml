destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-threadloop'')

  '
repo_depends:
- python-tornado
update_on:
- alpm: python-threadloop
  source: archpkg
