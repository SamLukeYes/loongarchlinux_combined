destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-notify2'')

  '
update_on:
- alpm: python-notify2
  source: archpkg
