destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fs'')

  '
repo_depends:
- python-appdirs
- python-six
- python-pytz
- python-setuptools
update_on:
- alpm: python-fs
  source: archpkg
