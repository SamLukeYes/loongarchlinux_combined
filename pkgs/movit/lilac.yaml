destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''movit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fftw
- libepoxy
repo_makedepends:
- eigen
- gtest
- sdl2
update_on:
- alpm: movit
  source: archpkg
