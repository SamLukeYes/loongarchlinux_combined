destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''krita-plugin-gmic'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- krita
- gmic
repo_makedepends:
- extra-cmake-modules
- qt5-tools
update_on:
- alpm: krita-plugin-gmic
  source: archpkg
