destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ufoprocessor'')

  '
repo_checkdepends: []
repo_depends:
- python-mutatormath
- python-fontparts
repo_makedepends:
- python-build
- python-installer
- python-wheel
- python-setuptools-scm
update_on:
- alpm: python-ufoprocessor
  source: archpkg
