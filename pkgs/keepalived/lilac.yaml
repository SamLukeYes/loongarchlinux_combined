destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''keepalived'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- file
- libnl
- iptables
- openssl
- glibc
- systemd: systemd-libs
update_on:
- alpm: iptables
  source: alpm
  strip_release: true
- alpm: keepalived
  source: archpkg
