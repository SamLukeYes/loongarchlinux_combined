destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyvcd'')

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- python-setuptools-scm
update_on:
- alpm: python-pyvcd
  source: archpkg
