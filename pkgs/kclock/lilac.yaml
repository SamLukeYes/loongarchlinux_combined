destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kclock'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kirigami-addons
- qt5-svg
- plasma-framework
- qt5-multimedia
update_on:
- alpm: kclock
  source: archpkg
