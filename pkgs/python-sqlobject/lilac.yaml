destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sqlobject'')

  '
repo_depends:
- python-pydispatcher
- python-formencode
update_on:
- alpm: python-sqlobject
  source: archpkg
