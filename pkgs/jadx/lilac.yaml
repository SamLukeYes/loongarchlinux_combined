destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jadx'')

  '
repo_depends:
- bash
update_on:
- alpm: jadx
  source: archpkg
