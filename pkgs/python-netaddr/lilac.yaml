destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-netaddr'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-cov
repo_depends:
- python
repo_makedepends:
- python-setuptools
- python-sphinx
update_on:
- alias: python
- alpm: python-netaddr
  source: archpkg
