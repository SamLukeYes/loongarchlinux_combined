destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kirigami-addons'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends: []
update_on:
- alpm: kirigami-addons
  source: archpkg
