destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rime-pinyin-simp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- rime-stroke
repo_makedepends:
- librime
- rime-prelude
- rime-essay
update_on:
- alpm: rime-pinyin-simp
  source: archpkg
