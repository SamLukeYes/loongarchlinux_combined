destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''passenger'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby
- nginx
update_on:
- manual: 1
  source: manual
- alias: ruby
- alpm: nginx
  source: alpm
  strip_release: true
- alpm: passenger
  source: archpkg
