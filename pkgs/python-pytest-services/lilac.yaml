destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-services'')

  '
repo_depends:
- python-psutil
- python-zc.lockfile
- python-requests
- python-pytest
update_on:
- alpm: python-pytest-services
  source: archpkg
