destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pynamodb'')

  '
repo_checkdepends:
- python-blinker
- python-pytest-env
- python-pytest-mock
repo_depends:
- python
- python-botocore
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-pynamodb
  source: archpkg
