destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnucobol'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- db
- gmp
repo_makedepends:
- gcc
update_on:
- alpm: gnucobol
  source: archpkg
