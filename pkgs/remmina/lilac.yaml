destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''remmina'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsoup3
- avahi
- libsodium
- vte3
- libayatana-appindicator
- libgcrypt
- libssh
update_on:
- alpm: remmina
  source: archpkg
