destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''borgmatic'')

  '
repo_depends:
- python-requests
- python-setuptools
- borg
- python-colorama
- python-ruamel-yaml
- python-jsonschema
update_on:
- alpm: borgmatic
  source: archpkg
