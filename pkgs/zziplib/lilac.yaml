destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zziplib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- unzip
repo_depends:
- zlib
repo_makedepends:
- python
- xmlto
- zip
- cmake
- ninja
update_on:
- alpm: zziplib
  source: archpkg
