destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rocm-opencl-runtime'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- hsakmt-roct
- hsa-rocr
- comgr
- mesa
repo_makedepends:
- rocm-cmake
update_on:
- alpm: rocm-opencl-runtime
  source: archpkg
