destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-hatch-vcs'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-hatchling
- python-setuptools-scm
repo_makedepends:
- git
- python-build
- python-installer
update_on:
- alpm: python-hatch-vcs
  source: archpkg
