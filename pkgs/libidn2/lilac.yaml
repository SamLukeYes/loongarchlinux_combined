destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libidn2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libunistring
- libunistring
repo_makedepends:
- ruby-ronn-ng
update_on:
- alpm: libunistring
  provides: libunistring.so
  source: alpm
- alpm: libidn2
  source: archpkg
