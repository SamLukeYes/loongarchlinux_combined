destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''most'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- slang
update_on:
- alpm: most
  source: archpkg
