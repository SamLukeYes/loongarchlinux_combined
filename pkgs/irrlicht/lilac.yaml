destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''irrlicht'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bzip2
- libpng
repo_makedepends:
- mesa
- unzip
- libxcursor
update_on:
- alpm: irrlicht
  source: archpkg
