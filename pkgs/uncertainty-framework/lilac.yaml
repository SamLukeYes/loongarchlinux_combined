destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''uncertainty-framework'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ceres-solver
update_on:
- alpm: uncertainty-framework
  source: archpkg
