destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-tornasync'')

  '
repo_checkdepends: []
repo_depends:
- python-tornado
- python-pytest
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-tornasync
  source: archpkg
