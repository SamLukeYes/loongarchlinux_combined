destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ming'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- freetype2
- libpng
- flex
- giflib
repo_makedepends:
- bison
- pkgconf
- swig
- python
- git
update_on:
- alpm: ming
  source: archpkg
