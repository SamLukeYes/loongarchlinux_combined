destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''i3status'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- alsa-lib
- confuse
- libnl
- pulseaudio: libpulse
- yajl
repo_makedepends:
- asciidoc
- xmlto
- meson
update_on:
- alpm: i3status
  source: archpkg
