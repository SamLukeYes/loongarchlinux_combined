destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''grilo-plugins'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- lua
- grilo
update_on:
- alias: lua
- alpm: grilo
  provides: libgrlpls-0.3.so
  source: alpm
- alpm: grilo-plugins
  source: archpkg
