destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''avahi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- dbus
update_on:
- manual: 1
  source: manual
- alpm: dbus
  provides: libdbus-1.so
  source: alpm
- alpm: avahi
  source: archpkg
