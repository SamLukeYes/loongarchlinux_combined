destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-smash'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- gcc: gcc-libs
- git
repo_makedepends:
- scdoc
update_on:
- alpm: git-smash
  source: archpkg
