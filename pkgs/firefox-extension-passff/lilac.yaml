destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''firefox-extension-passff'')

  '
repo_depends:
- passff-host
- firefox
update_on:
- alpm: firefox-extension-passff
  source: archpkg
