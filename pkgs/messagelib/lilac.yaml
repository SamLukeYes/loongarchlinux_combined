destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''messagelib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libkleo
- kidentitymanagement
- libgravatar
- kmailtransport
- qca: qca-qt5
- kmbox
update_on:
- alpm: messagelib
  source: archpkg
