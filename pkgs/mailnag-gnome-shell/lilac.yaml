destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mailnag-gnome-shell'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- folks
- gnome-shell
- mailnag
repo_makedepends:
- vala
update_on:
- alpm: mailnag-gnome-shell
  source: archpkg
