destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gpscorrelate'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- exiv2
- gtk3
repo_makedepends:
- libxslt
update_on:
- alpm: gpscorrelate
  source: archpkg
