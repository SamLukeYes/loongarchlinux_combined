destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fig2dev'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxpm
- ghostscript
- netpbm
- bc
- libpng
update_on:
- alpm: fig2dev
  source: archpkg
