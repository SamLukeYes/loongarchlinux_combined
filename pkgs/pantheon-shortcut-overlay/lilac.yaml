destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pantheon-shortcut-overlay'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
- libgee
repo_makedepends:
- git
- meson
- vala
update_on:
- alpm: granite7
  provides: libgranite-7.so
  source: alpm
- alpm: pantheon-shortcut-overlay
  source: archpkg
