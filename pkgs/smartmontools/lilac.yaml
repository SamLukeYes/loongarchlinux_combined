destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''smartmontools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libcap-ng
- gcc: gcc-libs
- systemd: systemd-libs
- bash
update_on:
- alpm: smartmontools
  source: archpkg
