destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wikicurses'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-urwid
- python-beautifulsoup4
- python-lxml
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: wikicurses
  source: archpkg
