destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nvidia-prime'')

  '
repo_depends:
- nvidia-utils
- bash
update_on:
- alpm: nvidia-prime
  source: archpkg
