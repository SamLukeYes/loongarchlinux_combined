destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-power_assert'')

  '
repo_depends:
- ruby
update_on:
- alias: ruby
- alpm: ruby-power_assert
  source: archpkg
