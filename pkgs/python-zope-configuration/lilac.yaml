destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-zope-configuration'')

  '
repo_depends:
- python-zope-i18nmessageid
- python-zope-schema
- python-zope-interface
- python-setuptools
update_on:
- alpm: python-zope-configuration
  source: archpkg
