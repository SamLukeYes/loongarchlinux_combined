destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libzip'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bzip2
- openssl
- zlib
- gnutls
- zstd
update_on:
- alpm: libzip
  source: archpkg
