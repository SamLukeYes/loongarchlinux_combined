destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libquvi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libquvi-scripts
- curl
- libgcrypt
- libproxy
- glib2
- lua52
update_on:
- alpm: libquvi
  source: archpkg
