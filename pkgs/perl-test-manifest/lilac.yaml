destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-test-manifest'')

  '
repo_depends:
- perl
update_on:
- alias: perl
- alpm: perl-test-manifest
  source: archpkg
