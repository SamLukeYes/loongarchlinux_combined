destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''networkmanager-strongswan'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsecret
- networkmanager: libnm
update_on:
- alpm: networkmanager-strongswan
  source: archpkg
