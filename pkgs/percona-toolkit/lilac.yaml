destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''percona-toolkit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl-term-readkey
update_on:
- alpm: percona-toolkit
  source: archpkg
