destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kubectl-cert-manager'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kubernetes: kubectl
repo_makedepends:
- git
- go
update_on:
- alpm: kubectl-cert-manager
  source: archpkg
