destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''minidlna'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libexif
- libid3tag
- flac
- libvorbis
- ffmpeg
- sqlite
repo_makedepends:
- git
update_on:
- alpm: minidlna
  source: archpkg
