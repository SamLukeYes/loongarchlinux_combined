destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''distcc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- avahi
- popt
- python
- gcc
repo_makedepends:
- git
- gtk3
update_on:
- alias: python
- alpm: distcc
  source: archpkg
