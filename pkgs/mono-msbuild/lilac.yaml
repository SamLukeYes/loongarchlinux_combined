destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mono-msbuild'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- mono
- mono-msbuild
- dotnet-core: dotnet-host
repo_makedepends:
- bash
- git
- dotnet-core: dotnet-sdk
- unzip
update_on:
- alpm: mono-msbuild
  source: archpkg
