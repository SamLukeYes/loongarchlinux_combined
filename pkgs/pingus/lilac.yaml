destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pingus'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- sdl_image
- sdl_mixer
repo_makedepends:
- scons
- mesa
- glu
update_on:
- alpm: pingus
  source: archpkg
