destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dbeaver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gtk4: gtk-update-icon-cache
- gtk3
- libsecret
update_on:
- alpm: dbeaver
  source: archpkg
