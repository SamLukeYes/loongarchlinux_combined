destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''check-sieve'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python
repo_depends:
- gcc: gcc-libs
repo_makedepends:
- git
update_on:
- alpm: check-sieve
  source: archpkg
