destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mpv'')

  '
repo_depends:
- mpv
- python
update_on:
- alias: python
- alpm: python-mpv
  source: archpkg
