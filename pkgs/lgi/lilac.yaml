destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lgi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib2
- gobject-introspection: gobject-introspection-runtime
- libffi
- lua53
- glibc
- lua
update_on:
- alias: lua
- alpm: lgi
  source: archpkg
