destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cairo-perl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cairo
- perl
repo_makedepends:
- perl-extutils-depends
- perl-extutils-pkgconfig
update_on:
- alias: perl
- alpm: cairo-perl
  source: archpkg
