destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kvirc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- enchant
- qt5-x11extras
- qt5-multimedia
- qt5-svg
- hicolor-icon-theme
update_on:
- alpm: kvirc
  source: archpkg
