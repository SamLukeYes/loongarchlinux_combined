destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''srs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssl
- gcc: gcc-libs
- srs-state-threads
update_on:
- alpm: srs
  source: archpkg
