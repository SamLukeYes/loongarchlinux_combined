destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyct'')

  '
repo_checkdepends:
- python-pytest
- python-requests
- python-yaml
repo_depends:
- python-param
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pyct
  source: archpkg
