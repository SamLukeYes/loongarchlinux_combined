destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''riscv64-linux-gnu-gcc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- riscv64-linux-gnu-binutils
- riscv64-linux-gnu-glibc
- libmpc
- libisl
- zstd
repo_makedepends: []
update_on:
- alpm: riscv64-linux-gnu-gcc
  source: archpkg
