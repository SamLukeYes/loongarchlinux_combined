destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bftpd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxcrypt
update_on:
- alpm: libxcrypt
  provides: libcrypt.so
  source: alpm
- alpm: bftpd
  source: archpkg
