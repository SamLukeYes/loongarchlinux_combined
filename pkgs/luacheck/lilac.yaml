destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''luacheck'')

  '
repo_checkdepends: []
repo_depends:
- lua
- lua-filesystem
- lua-argparse
repo_makedepends:
- luarocks
update_on:
- alias: lua
- alpm: luacheck
  source: archpkg
