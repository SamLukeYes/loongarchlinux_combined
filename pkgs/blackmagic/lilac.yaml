destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''blackmagic'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libusb
- libftdi
repo_makedepends:
- git
- hidapi
- python
update_on:
- alpm: hidapi
  provides: libhidapi-libusb.so
  source: alpm
- alpm: blackmagic
  source: archpkg
