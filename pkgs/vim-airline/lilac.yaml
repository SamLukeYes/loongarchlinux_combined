destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vim-airline'')

  '
repo_checkdepends: []
repo_depends:
- vim
- vim-airline-themes
repo_makedepends:
- git
update_on:
- alpm: vim-airline
  source: archpkg
