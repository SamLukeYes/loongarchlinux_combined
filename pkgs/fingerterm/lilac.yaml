destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fingerterm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- qt5-feedback
repo_makedepends:
- git
- nemo-qml-plugin-notifications
- qt5-tools
update_on:
- alpm: fingerterm
  source: archpkg
