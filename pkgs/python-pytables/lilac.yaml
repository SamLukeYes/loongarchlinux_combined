destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytables'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- blosc
- lzo
- hdf5
- python-numexpr
repo_makedepends:
- python-setuptools
- cython
update_on:
- alpm: python-pytables
  source: archpkg
