destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''geoip'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- zlib
- geoip-database
repo_makedepends:
- autoconf
- libtool
update_on:
- alpm: geoip
  source: archpkg
