destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''supermin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- e2fsprogs
- pacman
- pacman-contrib
- cpio
repo_makedepends:
- ocaml
- ocaml-findlib
update_on:
- alpm: supermin
  source: archpkg
