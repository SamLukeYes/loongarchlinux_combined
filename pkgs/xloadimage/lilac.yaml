destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xloadimage'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libtiff
- libpng
- libx11
- libxext
repo_makedepends:
- patch
- imake
update_on:
- alpm: xloadimage
  source: archpkg
