destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''virtualbox-host-modules-arch'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- linux
repo_makedepends:
- linux: linux-headers
- virtualbox: virtualbox-host-dkms
update_on:
- alpm: virtualbox-host-modules-arch
  source: archpkg
