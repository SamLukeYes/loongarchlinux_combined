destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''partclone'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- nilfs-utils
- ntfs-3g
update_on:
- alpm: partclone
  source: archpkg
