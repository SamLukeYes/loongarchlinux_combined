destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cloud-utils'')

  '
repo_depends:
- ca-certificates
- wget
- qemu: qemu-img
- cdrtools
- python
update_on:
- alias: python
- alias: python
- alpm: cloud-utils
  source: archpkg
