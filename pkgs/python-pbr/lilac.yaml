destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pbr'')

  '
repo_depends:
- python-importlib-metadata
- python-setuptools
update_on:
- alpm: python-pbr
  source: archpkg
