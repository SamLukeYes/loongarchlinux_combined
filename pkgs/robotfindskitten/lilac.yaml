destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''robotfindskitten'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ncurses
repo_makedepends:
- setconf
update_on:
- alpm: robotfindskitten
  source: archpkg
