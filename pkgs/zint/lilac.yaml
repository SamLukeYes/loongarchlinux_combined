destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zint'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt6-tools
- hicolor-icon-theme
- libpng
update_on:
- alpm: zint
  source: archpkg
