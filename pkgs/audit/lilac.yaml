destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''audit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libcap-ng
- krb5
- glibc
- python
update_on:
- alpm: krb5
  provides: libkrb5.so
  source: alpm
- alpm: krb5
  provides: libgssapi_krb5.so
  source: alpm
- alpm: libcap-ng
  provides: libcap-ng.so
  source: alpm
- alpm: audit
  provides: libaudit.so
  source: alpm
- alpm: audit
  provides: libauparse.so
  source: alpm
- alias: python
- alpm: audit
  source: archpkg
