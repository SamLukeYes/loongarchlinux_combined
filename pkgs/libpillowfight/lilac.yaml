destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libpillowfight'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- python-pillow
- glibc
- python-pillow
repo_makedepends:
- cmake
- git
- ninja
- python-nose
- python-setuptools
update_on:
- alpm: libpillowfight
  source: archpkg
