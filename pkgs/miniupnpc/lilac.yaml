destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''miniupnpc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- python
- glibc
repo_makedepends:
- glibc
- lsb-release
- python
- python-setuptools
update_on:
- alias: python
- alpm: miniupnpc
  source: archpkg
