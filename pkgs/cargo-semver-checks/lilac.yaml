destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cargo-semver-checks'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- zlib
- libgit2
- openssl
- glibc
update_on:
- alpm: cargo-semver-checks
  source: archpkg
