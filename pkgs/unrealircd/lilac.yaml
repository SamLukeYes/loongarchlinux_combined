destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''unrealircd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openssl
- curl
- c-ares
- tre
- pcre2
- libnsl
- argon2
repo_makedepends: []
update_on:
- alpm: unrealircd
  source: archpkg
