destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kmon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kmod
- libxcb
repo_makedepends:
- rust
- python
- git
update_on:
- alpm: kmon
  source: archpkg
