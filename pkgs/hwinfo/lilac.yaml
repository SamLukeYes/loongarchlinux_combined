destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hwinfo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl-xml-writer
- libx86emu
- perl-xml-parser
update_on:
- alpm: hwinfo
  source: archpkg
