destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-scramp'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-mock
- python-passlib
repo_depends:
- python
- python-asn1crypto
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-versioningit
- python-wheel
update_on:
- alias: python
- alpm: python-scramp
  source: archpkg
