destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-ref-util'')

  '
repo_checkdepends:
- perl-ref-util-xs
- perl-readonly
repo_depends:
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-ref-util
  source: archpkg
