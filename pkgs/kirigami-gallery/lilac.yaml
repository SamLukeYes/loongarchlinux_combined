destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kirigami-gallery'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kirigami2
- kitemmodels
- qt5-svg
repo_makedepends:
- extra-cmake-modules
- qt5-tools
update_on:
- alpm: kirigami-gallery
  source: archpkg
