destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dbus-glib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- dbus
- glib2
update_on:
- alpm: dbus-glib
  source: archpkg
