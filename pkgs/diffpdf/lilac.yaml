destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''diffpdf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- poppler: poppler-qt5
- hicolor-icon-theme
repo_makedepends:
- cmake
- extra-cmake-modules
- qt5-tools
update_on:
- alpm: diffpdf
  source: archpkg
