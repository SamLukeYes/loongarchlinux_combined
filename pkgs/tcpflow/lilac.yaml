destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tcpflow'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libpcap
- cairo
- openssl
- sqlite
repo_makedepends:
- git
- boost
update_on:
- alpm: tcpflow
  source: archpkg
