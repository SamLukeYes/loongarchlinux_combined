destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mailutils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openldap: libldap
- pam
- gnutls
- gsasl
repo_makedepends:
- emacs
update_on:
- alpm: mailutils
  source: archpkg
