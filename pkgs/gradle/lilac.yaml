destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gradle'')

  '
repo_depends:
- bash
- sed
- which
- findutils
- coreutils
update_on:
- alpm: gradle
  source: archpkg
