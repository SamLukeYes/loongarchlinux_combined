destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''isomd5sum'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- popt
- python
repo_makedepends:
- popt
- python
update_on:
- alias: python
- alpm: isomd5sum
  source: archpkg
