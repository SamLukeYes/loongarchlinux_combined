destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''phonon-vlc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- vlc
- phonon: phonon-qt5
repo_makedepends:
- extra-cmake-modules
- qt5-tools
update_on:
- alpm: phonon-vlc
  source: archpkg
