destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-crypt-openssl-rsa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl-crypt-openssl-random
update_on:
- alpm: perl-crypt-openssl-rsa
  source: archpkg
