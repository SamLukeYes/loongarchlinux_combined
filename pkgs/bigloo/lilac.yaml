destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bigloo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libunistring
- gc
- openssl
- libuv
- libnsl
- gmp
update_on:
- alpm: bigloo
  source: archpkg
