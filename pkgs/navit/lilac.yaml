destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''navit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gpsd
update_on:
- alpm: gpsd
  source: alpm
  strip_release: true
- alpm: navit
  source: archpkg
