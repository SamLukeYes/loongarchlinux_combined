destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''poke'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- dejagnu
repo_depends:
- gc
- readline
- gettext
- json-c
repo_makedepends:
- help2man
update_on:
- alias: readline
- alpm: poke
  source: archpkg
