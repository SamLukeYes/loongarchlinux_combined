destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-delta'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- git
repo_depends:
- glibc
- gcc: gcc-libs
- zlib
repo_makedepends:
- mdbook
update_on:
- alpm: julia
  provides: libgit2.so
  source: alpm
- alpm: git-delta
  source: archpkg
