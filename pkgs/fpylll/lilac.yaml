destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fpylll'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fplll
- python-numpy
- cysignals: python-cysignals
repo_makedepends:
- cython
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: fpylll
  source: archpkg
