destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-tiny-proxy'')

  '
repo_depends:
- python-anyio
update_on:
- alpm: python-tiny-proxy
  source: archpkg
