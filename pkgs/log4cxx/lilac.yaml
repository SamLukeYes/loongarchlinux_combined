destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''log4cxx'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- apr-util
- libxml2
repo_makedepends:
- cmake
- patch
- zip
- gzip
- sed
update_on:
- alpm: log4cxx
  source: archpkg
