destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''systemd-ui'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gtk3
- libgee
- libnotify
update_on:
- alpm: systemd-ui
  source: archpkg
