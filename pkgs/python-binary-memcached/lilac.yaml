destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-binary-memcached'')

  '
repo_depends:
- python-uhashring
- python-six
update_on:
- alpm: python-binary-memcached
  source: archpkg
