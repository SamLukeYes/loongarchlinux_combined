destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cl-flexi-streams'')

  '
update_on:
- alpm: cl-flexi-streams
  source: archpkg
