destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''seqdiag'')

  '
repo_checkdepends:
- python-docutils
- python-nose
- python-pycodestyle
- python-reportlab
repo_depends:
- blockdiag
repo_makedepends: []
update_on:
- alpm: seqdiag
  source: archpkg
