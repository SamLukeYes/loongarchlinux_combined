destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''docbook-dsssl'')

  '
repo_checkdepends: []
repo_depends:
- sgml-common
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: docbook-dsssl
  source: archpkg
