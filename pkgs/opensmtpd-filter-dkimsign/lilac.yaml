destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opensmtpd-filter-dkimsign'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libopensmtpd
- openssl
repo_makedepends:
- gzip
- mandoc
update_on:
- alpm: opensmtpd-filter-dkimsign
  source: archpkg
