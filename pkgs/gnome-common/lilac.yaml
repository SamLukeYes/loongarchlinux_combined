destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-common'')

  '
repo_depends:
- intltool
- yelp-tools
- gtk-doc
- autoconf-archive
update_on:
- alpm: gnome-common
  source: archpkg
