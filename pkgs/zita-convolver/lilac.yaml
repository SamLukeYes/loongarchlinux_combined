destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zita-convolver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- gcc: gcc-libs
- fftw
update_on:
- alpm: zita-convolver
  source: archpkg
