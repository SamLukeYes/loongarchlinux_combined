destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pdal'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gdal
- libgeotiff
- jsoncpp
- libunwind
repo_makedepends:
- cmake
- python
- python-numpy
- ninja
- postgresql: postgresql-libs
update_on:
- alias: jsoncpp
- alpm: pdal
  source: archpkg
