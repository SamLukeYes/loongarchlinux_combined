destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-testrepository'')

  '
repo_checkdepends:
- python-testresources
- python-pytz
- python-testscenarios
repo_depends:
- python-testtools
- python-fixtures
- python-subunit
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-testrepository
  source: archpkg
