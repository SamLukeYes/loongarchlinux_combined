destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kontact'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kontactinterface
- kmail-account-wizard
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: kontact
  source: archpkg
