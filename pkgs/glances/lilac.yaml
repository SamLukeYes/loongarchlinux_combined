destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glances'')

  '
repo_depends:
- python
- python-psutil
- python-ujson
- python-future
- python-defusedxml
update_on:
- alias: python
- alpm: glances
  source: archpkg
