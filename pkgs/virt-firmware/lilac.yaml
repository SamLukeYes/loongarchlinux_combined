destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''virt-firmware'')

  '
repo_depends:
- python-setuptools
- python-crc32c
- python-cryptography
- python-pefile
- python
update_on:
- alias: python
- alpm: virt-firmware
  source: archpkg
