destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libnice'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
- gnutls
- gupnp-igd
repo_makedepends:
- git
- gobject-introspection
- graphviz
- gstreamer
- gtk-doc
- meson
update_on:
- alpm: libnice
  source: archpkg
