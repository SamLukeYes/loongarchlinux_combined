destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-redcarpet'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- tidy
repo_depends:
- ruby
repo_makedepends:
- git
- ruby-rake
- ruby-rake-compiler
- ruby-test-unit
update_on:
- alias: ruby
- alpm: ruby-redcarpet
  source: archpkg
