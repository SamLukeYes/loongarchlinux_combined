destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libdwarf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python
repo_depends:
- elfutils
repo_makedepends: []
update_on:
- alpm: libdwarf
  source: archpkg
