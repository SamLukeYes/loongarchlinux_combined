destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-spec'')

  '
repo_checkdepends: []
repo_depends:
- python-nose
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-spec
  source: archpkg
