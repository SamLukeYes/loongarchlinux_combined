destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''i2pd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zlib
- openssl
- boost: boost-libs
update_on:
- alpm: miniupnpc
  provides: libminiupnpc.so
  source: alpm
- alpm: i2pd
  source: archpkg
