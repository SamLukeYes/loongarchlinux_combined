destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''alice-vision'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- opencv
update_on:
- alpm: opencv
  source: alpm
  strip_release: true
- alpm: alice-vision
  source: archpkg
