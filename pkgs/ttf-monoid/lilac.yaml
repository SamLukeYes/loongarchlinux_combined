destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ttf-monoid'')

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- fontforge
- python
update_on:
- alpm: ttf-monoid
  source: archpkg
