destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zenity'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gtk3
- libnotify
update_on:
- alpm: zenity
  source: archpkg
