destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-psycopg'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-asyncio
repo_depends:
- python
- postgresql: postgresql-libs
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-psycopg
  source: archpkg
