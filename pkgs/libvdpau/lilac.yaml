destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libvdpau'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxext
update_on:
- alpm: libvdpau
  source: archpkg
