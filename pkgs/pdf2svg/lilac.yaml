destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pdf2svg'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- poppler: poppler-glib
- glib2
- cairo
update_on:
- alpm: pdf2svg
  source: archpkg
