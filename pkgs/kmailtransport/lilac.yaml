destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kmailtransport'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- akonadi-mime
- qtkeychain: qtkeychain-qt5
- ksmtp
- libkgapi
update_on:
- alpm: kmailtransport
  source: archpkg
