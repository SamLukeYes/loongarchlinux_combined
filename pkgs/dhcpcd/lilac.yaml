destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dhcpcd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- systemd: systemd-libs
update_on:
- alpm: dhcpcd
  source: archpkg
