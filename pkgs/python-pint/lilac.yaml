destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pint'')

  '
repo_checkdepends:
- python-pytest-subtests
- python-numpy
- python-uncertainties
repo_depends:
- python
repo_makedepends:
- python-setuptools-scm
update_on:
- alias: python
- alpm: python-pint
  source: archpkg
