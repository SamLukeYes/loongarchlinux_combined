destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ktorrent'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- phonon: phonon-qt5
- libktorrent
- knotifyconfig
- kparts
- kcmutils
- qt5-webengine
update_on:
- alpm: ktorrent
  source: archpkg
