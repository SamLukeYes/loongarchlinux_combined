destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openttd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fluidsynth
- fontconfig
- hicolor-icon-theme
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: openttd
  source: archpkg
