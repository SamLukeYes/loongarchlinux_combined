destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-googleapis-common-protos'')

  '
repo_checkdepends: []
repo_depends:
- protobuf: python-protobuf
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-googleapis-common-protos
  source: archpkg
