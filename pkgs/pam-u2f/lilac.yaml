destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pam-u2f'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssl
- libfido2
- pam
update_on:
- alpm: pam-u2f
  source: archpkg
