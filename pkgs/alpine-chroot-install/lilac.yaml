destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''alpine-chroot-install'')

  '
repo_checkdepends: []
repo_depends:
- coreutils
repo_makedepends: []
update_on:
- alpm: alpine-chroot-install
  source: archpkg
