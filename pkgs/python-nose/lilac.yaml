destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-nose'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-setuptools
repo_makedepends:
- dos2unix
- python-build
- python-coverage
- python-installer
- python-wheel
update_on:
- alias: python
- alpm: python-nose
  source: archpkg
