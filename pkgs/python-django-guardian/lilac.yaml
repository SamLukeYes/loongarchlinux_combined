destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-django-guardian'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-django
repo_depends:
- django: python-django
- python-django-environ
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-django-guardian
  source: archpkg
