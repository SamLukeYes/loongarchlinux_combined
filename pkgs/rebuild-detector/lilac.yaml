destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rebuild-detector'')

  '
repo_depends:
- parallel
- pacutils
- pacman-contrib
- binutils
update_on:
- alpm: rebuild-detector
  source: archpkg
