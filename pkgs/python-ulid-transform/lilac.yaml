destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ulid-transform'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
- python
repo_makedepends:
- cython
- git
- python-build
- python-installer
- python-poetry-core
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-ulid-transform
  source: archpkg
