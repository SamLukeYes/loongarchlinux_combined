destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-localserver'')

  '
repo_checkdepends:
- python-requests
repo_depends:
- python-pytest
- python-werkzeug
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-localserver
  source: archpkg
