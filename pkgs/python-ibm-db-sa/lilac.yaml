destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ibm-db-sa'')

  '
repo_checkdepends:
- python-nose
- python-coverage
repo_depends:
- python-sqlalchemy
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-ibm-db-sa
  source: archpkg
