destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libbpf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- elfutils: libelf
- linux-api-headers
- glibc
update_on:
- alpm: libbpf
  source: archpkg
