destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kleopatra'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libkleo
- kidentitymanagement
- kmailtransport
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: kleopatra
  source: archpkg
