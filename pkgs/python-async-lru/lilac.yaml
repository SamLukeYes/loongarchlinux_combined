destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-async-lru'')

  '
repo_checkdepends:
- python-pytest-cov
- python-pytest-asyncio
- python-pytest-timeout
repo_depends:
- python-typing_extensions
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-async-lru
  source: archpkg
