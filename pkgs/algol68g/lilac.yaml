destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''algol68g'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gsl
- mpfr
- plotutils
repo_makedepends: []
update_on:
- alpm: gsl
  source: alpm
  strip_release: true
- alpm: algol68g
  source: archpkg
