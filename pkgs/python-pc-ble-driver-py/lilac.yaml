destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pc-ble-driver-py'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
- pc-ble-driver
- python-cryptography
- python-wrapt
repo_makedepends:
- cmake
- ninja
- python-build
- python-installer
- python-scikit-build
- python-setuptools
- python-wheel
- swig
update_on:
- alpm: python-pc-ble-driver-py
  source: archpkg
