destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''slop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxext
- libx11
- mesa
- glew
- libxrender
repo_makedepends:
- cmake
- glm
update_on:
- alpm: slop
  source: archpkg
