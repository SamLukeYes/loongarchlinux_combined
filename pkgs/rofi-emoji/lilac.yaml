destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rofi-emoji'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- check
repo_depends:
- rofi
repo_makedepends: []
update_on:
- alpm: rofi-emoji
  source: archpkg
