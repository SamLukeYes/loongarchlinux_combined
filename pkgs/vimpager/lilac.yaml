destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vimpager'')

  '
repo_checkdepends: []
repo_depends:
- vim
repo_makedepends:
- sharutils
update_on:
- alpm: vimpager
  source: archpkg
