destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-keras-preprocessing'')

  '
repo_depends:
- python-numpy
- python
update_on:
- alias: python
- alpm: python-keras-preprocessing
  source: archpkg
