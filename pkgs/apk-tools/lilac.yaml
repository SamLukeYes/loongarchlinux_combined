destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''apk-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- zlib
- openssl
repo_makedepends:
- scdoc
- lua53
- lua-zlib: lua53-zlib
update_on:
- alpm: apk-tools
  source: archpkg
