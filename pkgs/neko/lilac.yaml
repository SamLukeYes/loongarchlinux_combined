destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''neko'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- mbedtls2
- gc
- sqlite
- gtk3
- mariadb: mariadb-libs
update_on:
- alpm: neko
  source: archpkg
