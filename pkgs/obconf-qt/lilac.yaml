destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''obconf-qt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openbox
- qt5-x11extras
- hicolor-icon-theme
update_on:
- alpm: obconf-qt
  source: archpkg
