destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ugrep'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bzip2
- pcre2
- xz
- lz4
- zlib
- zstd
update_on:
- alpm: ugrep
  source: archpkg
