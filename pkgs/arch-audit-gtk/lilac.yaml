destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''arch-audit-gtk'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- arch-audit
- libappindicator: libappindicator-gtk3
repo_makedepends:
- clang
- llvm
update_on:
- alpm: arch-audit-gtk
  source: archpkg
