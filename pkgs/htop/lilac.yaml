destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''htop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libcap
- ncurses
update_on:
- alpm: libcap
  provides: libcap.so
  source: alpm
- alpm: ncurses
  provides: libncursesw.so
  source: alpm
- alpm: htop
  source: archpkg
