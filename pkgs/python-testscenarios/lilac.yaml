destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-testscenarios'')

  '
repo_checkdepends: []
repo_depends:
- python-testtools
repo_makedepends:
- python-setuptools
- python-pbr
update_on:
- alpm: python-testscenarios
  source: archpkg
