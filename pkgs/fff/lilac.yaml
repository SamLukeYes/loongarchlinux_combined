destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fff'')

  '
repo_checkdepends: []
repo_depends:
- xdg-utils
repo_makedepends:
- git
update_on:
- alpm: fff
  source: archpkg
