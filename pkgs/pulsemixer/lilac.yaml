destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pulsemixer'')

  '
repo_depends:
- pulseaudio
- python
update_on:
- alias: python
- alpm: pulsemixer
  source: archpkg
