destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sniffglue'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libpcap
- libseccomp
repo_makedepends:
- scdoc
update_on:
- alpm: sniffglue
  source: archpkg
