destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pygame-sdl2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sdl2_mixer
- sdl2_ttf
- sdl2_image
- python
update_on:
- alias: python
- alpm: python-pygame-sdl2
  source: archpkg
