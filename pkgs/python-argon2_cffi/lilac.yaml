destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-argon2_cffi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python-argon2-cffi-bindings
repo_makedepends:
- git
- python-flit-core
- python-build
- python-installer
update_on:
- alpm: python-argon2_cffi
  source: archpkg
