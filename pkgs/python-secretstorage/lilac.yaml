destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-secretstorage'')

  '
repo_checkdepends:
- xorg-server: xorg-server-xvfb
repo_depends:
- python-cryptography
- python-jeepney
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-secretstorage
  source: archpkg
