destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-apptools'')

  '
repo_checkdepends: []
repo_depends:
- python-traits
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-apptools
  source: archpkg
