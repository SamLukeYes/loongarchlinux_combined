destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ejabberd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- erlang: erlang-unixodbc
- gd
- zlib
- erlang-nox
- iproute2
- expat
- libyaml
- sqlite
- openssl
- pam
update_on:
- alpm: ejabberd
  source: archpkg
