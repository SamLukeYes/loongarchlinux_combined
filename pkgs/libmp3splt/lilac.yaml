destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmp3splt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libid3tag
- flac
- pcre
- libvorbis
- libmad
update_on:
- alpm: libmp3splt
  source: archpkg
