destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-websocket-driver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby-websocket-extensions
- ruby
update_on:
- alias: ruby
- alpm: ruby-websocket-driver
  source: archpkg
