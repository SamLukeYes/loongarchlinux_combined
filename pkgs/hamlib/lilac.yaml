destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hamlib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libuhd
- libtool
- libusb-compat
- perl
- libindi
- libxml2
- tcl
- python
- lua
update_on:
- alias: perl
- alias: python
- alias: lua
- alpm: libindi
  source: alpm
  strip_release: true
- alpm: hamlib
  source: archpkg
