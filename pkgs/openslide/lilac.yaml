destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openslide'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sqlite
- cairo
- gdk-pixbuf2
- openjpeg2
update_on:
- alpm: openslide
  source: archpkg
