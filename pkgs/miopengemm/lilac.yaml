destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''miopengemm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ocl-icd
repo_makedepends:
- opencl-headers
- rocm-cmake
update_on:
- alpm: miopengemm
  source: archpkg
