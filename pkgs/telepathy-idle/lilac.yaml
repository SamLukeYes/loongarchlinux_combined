destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''telepathy-idle'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- telepathy-glib
repo_makedepends:
- libxslt
- python
update_on:
- alpm: telepathy-idle
  source: archpkg
