destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-io-compress-brotli'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- perl
- perl-file-slurper
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-io-compress-brotli
  source: archpkg
