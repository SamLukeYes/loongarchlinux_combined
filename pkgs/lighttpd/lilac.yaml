destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lighttpd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- pcre2
- systemd
- brotli
repo_makedepends:
- mariadb: mariadb-libs
- lua
- libxml2
- e2fsprogs
- sqlite
update_on:
- alpm: lighttpd
  source: archpkg
