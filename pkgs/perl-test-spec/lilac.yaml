destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-test-spec'')

  '
repo_depends:
- perl-tie-ixhash
- perl-test-trap
update_on:
- alpm: perl-test-spec
  source: archpkg
