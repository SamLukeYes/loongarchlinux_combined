destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cifs-utils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libcap-ng
- keyutils
- krb5
- talloc
- pam
- samba: libwbclient
repo_makedepends:
- python-docutils
update_on:
- alpm: cifs-utils
  source: archpkg
