destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libcoverart'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- neon
- jansson
update_on:
- alpm: libcoverart
  source: archpkg
