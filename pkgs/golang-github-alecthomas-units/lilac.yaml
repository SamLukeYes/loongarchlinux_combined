destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''golang-github-alecthomas-units'')

  '
repo_depends:
- go
update_on:
- alpm: golang-github-alecthomas-units
  source: archpkg
