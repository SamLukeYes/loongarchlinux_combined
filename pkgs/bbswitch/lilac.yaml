destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bbswitch'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- linux
- dkms
repo_makedepends:
- linux: linux-headers
update_on:
- alpm: bbswitch
  source: archpkg
