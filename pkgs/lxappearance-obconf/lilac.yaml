destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lxappearance-obconf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openbox
update_on:
- alpm: openbox
  provides: libobrender.so
  source: alpm
- alpm: lxappearance-obconf
  source: archpkg
