destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vokoscreen'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-x11extras
- gstreamer: gst-plugins-good
- qt5-multimedia
update_on:
- alpm: vokoscreen
  source: archpkg
