destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pciutils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kmod
- glibc
- hwdata
update_on:
- alpm: pciutils
  source: archpkg
