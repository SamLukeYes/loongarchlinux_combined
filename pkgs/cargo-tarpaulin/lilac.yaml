destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cargo-tarpaulin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- zlib
- libssh2
- libgit2
- openssl
- glibc
update_on:
- alpm: curl
  provides: libcurl.so
  source: alpm
- alpm: cargo-tarpaulin
  source: archpkg
