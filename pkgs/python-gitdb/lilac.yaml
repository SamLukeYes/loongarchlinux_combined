destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-gitdb'')

  '
repo_depends:
- python-smmap
- git
update_on:
- alpm: python-gitdb
  source: archpkg
