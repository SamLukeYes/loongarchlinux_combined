destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-rawkit'')

  '
repo_checkdepends:
- python-mock
- python-pytest
repo_depends:
- libraw
- python
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-rawkit
  source: archpkg
