destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''powertop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- libnl
- ncurses
- pciutils
repo_makedepends:
- autoconf-archive
- patch
update_on:
- alpm: powertop
  source: archpkg
