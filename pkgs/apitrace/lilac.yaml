destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''apitrace'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- procps-ng
- python
update_on:
- alias: python
- alpm: apitrace
  source: archpkg
