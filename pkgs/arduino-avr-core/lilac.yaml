destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''arduino-avr-core'')

  '
repo_depends: []
update_on:
- alpm: arduino-avr-core
  source: archpkg
