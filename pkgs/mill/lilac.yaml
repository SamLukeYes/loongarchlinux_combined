destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mill'')

  '
repo_checkdepends: []
repo_depends:
- bash
repo_makedepends:
- mill
update_on:
- alpm: mill
  source: archpkg
