destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glib-networking'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- ca-certificates
repo_depends:
- glib2
- gnutls
- gsettings-desktop-schemas
- libproxy
repo_makedepends:
- git
- meson
update_on:
- alpm: glib-networking
  source: archpkg
