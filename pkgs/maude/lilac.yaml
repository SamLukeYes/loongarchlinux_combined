destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''maude'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- buddy
- cvc4
- libtecla
- gmp
- libsigsegv
- yices
repo_makedepends:
- flex
- bison
update_on:
- alpm: maude
  source: archpkg
