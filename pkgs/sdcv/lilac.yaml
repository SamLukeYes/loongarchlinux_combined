destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sdcv'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
- readline
- zlib
repo_makedepends:
- cmake
update_on:
- alias: readline
- alpm: sdcv
  source: archpkg
