destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-signedjson'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-canonicaljson
- python-unpaddedbase64
- python-pynacl
repo_makedepends:
- python-setuptools
- python-setuptools-scm
- python-wheel
update_on:
- alias: python
- alpm: python-signedjson
  source: archpkg
