destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''etherape'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib2
- goocanvas
- popt
- libpcap
- gtk3
- glibc
update_on:
- alpm: libpcap
  provides: libpcap.so
  source: alpm
- alpm: etherape
  source: archpkg
