destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jupyter-packaging'')

  '
repo_depends:
- python-tomlkit
- python-setuptools
- python-packaging
- python-wheel
- python-deprecation
update_on:
- alpm: python-jupyter-packaging
  source: archpkg
