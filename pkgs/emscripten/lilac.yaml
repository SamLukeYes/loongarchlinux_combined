destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''emscripten'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nodejs
- python
- which
- acorn
repo_makedepends:
- cmake
- libxml2
- git
- ninja
- npm
update_on:
- alias: python
- alpm: emscripten
  source: archpkg
