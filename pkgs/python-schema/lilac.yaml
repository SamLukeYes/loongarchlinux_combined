destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-schema'')

  '
repo_depends:
- python-contextlib2
- python
update_on:
- alias: python
- alpm: python-schema
  source: archpkg
