destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''woff2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- brotli
update_on:
- alpm: woff2
  source: archpkg
