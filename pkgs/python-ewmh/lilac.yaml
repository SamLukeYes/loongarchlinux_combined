destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ewmh'')

  '
repo_checkdepends: []
repo_depends:
- python-xlib
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-ewmh
  source: archpkg
