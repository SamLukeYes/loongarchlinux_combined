destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-psycopg2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- postgresql: postgresql-libs
repo_makedepends:
- python-setuptools
- postgresql: postgresql-libs
update_on:
- alias: python
- alpm: python-psycopg2
  source: archpkg
