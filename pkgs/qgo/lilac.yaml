destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qgo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libsm
- qt5-multimedia
repo_makedepends:
- git
- qt5-tools
update_on:
- alpm: qgo
  source: archpkg
