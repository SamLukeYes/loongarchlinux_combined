destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-credential-gopass'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends: []
update_on:
- alpm: git-credential-gopass
  source: archpkg
