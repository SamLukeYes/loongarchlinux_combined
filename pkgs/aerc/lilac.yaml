destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aerc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- notmuch: notmuch-runtime
- gawk
repo_makedepends:
- go
- scdoc
update_on:
- alpm: aerc
  source: archpkg
