destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-paste'')

  '
repo_checkdepends:
- python-pytest
- python-flup
repo_depends:
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-paste
  source: archpkg
