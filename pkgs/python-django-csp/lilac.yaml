destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-django-csp'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-django
- python-six
repo_depends:
- django: python-django
- python-jinja
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-django-csp
  source: archpkg
