destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gauche'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libatomic_ops
- libxcrypt
- slib
repo_makedepends:
- autoconf
- git
update_on:
- alpm: gauche
  source: archpkg
