destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-click-plugins'')

  '
repo_checkdepends:
- python-pytest
- python-click
repo_depends:
- python-click
- python-click
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-click-plugins
  source: archpkg
