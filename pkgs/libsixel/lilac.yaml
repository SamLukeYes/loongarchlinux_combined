destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libsixel'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libjpeg-turbo
- libpng
- python
- curl
- gdk-pixbuf2
repo_makedepends:
- meson
update_on:
- alias: python
- alpm: libsixel
  source: archpkg
