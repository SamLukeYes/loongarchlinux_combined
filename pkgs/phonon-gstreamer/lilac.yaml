destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''phonon-gstreamer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-x11extras
- gstreamer: gst-plugins-base
- phonon: phonon-qt5
update_on:
- alpm: phonon-gstreamer
  source: archpkg
