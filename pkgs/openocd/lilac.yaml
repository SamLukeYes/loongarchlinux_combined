destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openocd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb-compat
- libftdi-compat
- hidapi
update_on:
- alpm: systemd-libs
  provides: libudev.so
  source: alpm
- alpm: openocd
  source: archpkg
