destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ktexteditor'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kparts
- syntax-highlighting
- editorconfig-core-c
repo_makedepends:
- extra-cmake-modules
- doxygen
- qt5-tools
- qt5-doc
update_on:
- alpm: ktexteditor
  source: archpkg
