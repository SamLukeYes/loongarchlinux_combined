destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-vcr'')

  '
repo_checkdepends:
- python-coverage
- python-pytest
repo_depends:
- python-pytest
- python-vcrpy
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-vcr
  source: archpkg
