destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libssh2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssl
- zlib
update_on:
- alpm: libssh2
  source: archpkg
