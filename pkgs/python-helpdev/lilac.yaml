destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-helpdev'')

  '
repo_checkdepends:
- python-pytest
- python-pip
repo_depends:
- python-importlib-metadata
- python-psutil
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-helpdev
  source: archpkg
