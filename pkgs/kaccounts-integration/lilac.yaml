destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kaccounts-integration'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- signon-ui
- signon-kwallet-extension
- signon-plugin-oauth2
- kcmutils
update_on:
- alpm: kaccounts-integration
  source: archpkg
