destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''caja'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- exempi
- gvfs
- libexif
- libsm
- mate-desktop
- libnotify
repo_makedepends:
- gobject-introspection
- intltool
update_on:
- alpm: caja
  source: archpkg
