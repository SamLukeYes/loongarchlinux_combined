destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''danmaq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- qt5-x11extras
repo_makedepends:
- cmake
- qt5-tools
update_on:
- alpm: danmaq
  source: archpkg
