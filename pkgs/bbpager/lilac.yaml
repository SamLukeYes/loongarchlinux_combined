destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bbpager'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- libxft
- libxext
- blackbox
repo_makedepends:
- patch
update_on:
- alpm: bbpager
  source: archpkg
