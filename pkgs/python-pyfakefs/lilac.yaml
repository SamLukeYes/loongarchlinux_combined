destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyfakefs'')

  '
repo_checkdepends:
- python-tox
repo_depends:
- python-pytest
repo_makedepends:
- git
- python-setuptools
update_on:
- alpm: python-pyfakefs
  source: archpkg
