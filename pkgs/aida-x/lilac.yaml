destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aida-x'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- alsa-lib
- gcc: gcc-libs
- dbus
- libx11
- libglvnd
- libxext
update_on:
- alpm: aida-x
  source: archpkg
