destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''grml-zsh-config'')

  '
repo_depends:
- sed
- grep
- zsh
- coreutils
update_on:
- alpm: grml-zsh-config
  source: archpkg
