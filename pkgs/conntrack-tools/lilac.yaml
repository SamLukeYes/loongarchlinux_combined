destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''conntrack-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- libnetfilter_conntrack
- libnetfilter_cttimeout
- libnetfilter_cthelper
- libnetfilter_queue
- libmnl
- systemd: systemd-libs
- libnfnetlink
repo_makedepends:
- systemd
- libtirpc
update_on:
- alpm: conntrack-tools
  source: archpkg
