destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pygls'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-asyncio
repo_depends:
- python-lsprotocol
- python-typeguard
repo_makedepends:
- python-build
- python-installer
- python-setuptools-scm
- python-toml
- python-wheel
update_on:
- alpm: python-pygls
  source: archpkg
