destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yara'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- file
- openssl
update_on:
- alpm: file
  provides: libmagic.so
  source: alpm
- alpm: yara
  source: archpkg
