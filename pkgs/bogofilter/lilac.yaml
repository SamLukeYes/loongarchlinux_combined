destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bogofilter'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- db5.3
- perl
- gsl
repo_makedepends:
- valgrind
- xmlto
update_on:
- alias: perl
- alpm: gsl
  source: alpm
  strip_release: true
- alpm: bogofilter
  source: archpkg
