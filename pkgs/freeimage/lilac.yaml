destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''freeimage'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libjpeg-turbo
- openexr
- openjpeg2
- libwebp
- libraw
- libtiff
- jxrlib
repo_makedepends:
- mesa
- glu
update_on:
- alpm: freeimage
  source: archpkg
