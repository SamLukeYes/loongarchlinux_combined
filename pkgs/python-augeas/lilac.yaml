destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-augeas'')

  '
repo_checkdepends: []
repo_depends:
- augeas
- python-cffi
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-augeas
  source: archpkg
