destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-qdldl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-scipy
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
- pybind11
- cmake
update_on:
- alpm: python-qdldl
  source: archpkg
