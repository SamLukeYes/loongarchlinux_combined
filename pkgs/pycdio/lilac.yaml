destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pycdio'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- libcdio
repo_makedepends:
- python-setuptools
- swig
update_on:
- alias: python
- alpm: pycdio
  source: archpkg
