destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-subprocess-tee'')

  '
repo_checkdepends:
- ansible-core
- molecule
- python-enrich
- python-pytest
- python-pytest-xdist
repo_depends:
- python
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools
- python-setuptools-scm
- python-wheel
update_on:
- alias: python
- alpm: python-subprocess-tee
  source: archpkg
