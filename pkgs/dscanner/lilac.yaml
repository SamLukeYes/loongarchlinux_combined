destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dscanner'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ldc: liblphobos
repo_makedepends:
- ldc
- git
update_on:
- alpm: dscanner
  source: archpkg
