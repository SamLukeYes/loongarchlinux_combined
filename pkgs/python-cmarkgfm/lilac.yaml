destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-cmarkgfm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- cmark-gfm
- python-cffi
repo_makedepends:
- cmark-gfm
- cython
- python-build
- python-installer
- python-setuptools
- python-cffi
- python-wheel
update_on:
- alpm: python-cmarkgfm
  source: archpkg
