destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''terragrunt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- terraform
- glibc
update_on:
- alpm: terragrunt
  source: archpkg
