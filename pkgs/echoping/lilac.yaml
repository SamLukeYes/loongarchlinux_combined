destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''echoping'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- popt
- libidn
- openldap: libldap
update_on:
- alpm: echoping
  source: archpkg
