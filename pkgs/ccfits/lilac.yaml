destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ccfits'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cfitsio
repo_makedepends: []
update_on:
- alpm: cfitsio
  source: alpm
  strip_release: true
- alpm: ccfits
  source: archpkg
