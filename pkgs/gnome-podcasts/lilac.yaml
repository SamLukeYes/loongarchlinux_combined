destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-podcasts'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gstreamer: gst-plugins-bad
- gstreamer: gst-plugins-base
- gstreamer: gst-plugins-good
- libhandy
repo_makedepends:
- meson
- rust
update_on:
- alpm: gnome-podcasts
  source: archpkg
