destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cups'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gnutls
# - avahi
- acl
- pam
# - cups-filters
- dbus
# - systemd
- libpaper
- hicolor-icon-theme
repo_makedepends:
- acl
- pam
- gnutls
# - cups-filters
# - colord
- libusb
# - avahi
# - systemd
- libpaper
update_on:
- alpm: cups
  source: archpkg
