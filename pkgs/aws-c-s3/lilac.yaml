destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aws-c-s3'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- aws-c-common
- aws-c-auth
- aws-checksums
repo_makedepends:
- cmake
update_on:
- alpm: aws-c-s3
  source: archpkg
