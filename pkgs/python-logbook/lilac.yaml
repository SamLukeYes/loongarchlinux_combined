destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-logbook'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- redis
- python-pytest
- python-sqlalchemy
- python-redis
- python-pyzmq
- python-execnet
- python-jinja
- brotli: python-brotli
- pifpaf
repo_depends:
- python-six
repo_makedepends:
- python-setuptools
- cython
- python-six
update_on:
- alpm: python-logbook
  source: archpkg
