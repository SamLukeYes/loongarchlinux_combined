destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''schemacrawler'')

  '
repo_depends:
- graphviz
update_on:
- alpm: schemacrawler
  source: archpkg
