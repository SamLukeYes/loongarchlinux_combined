destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''iempluginsuite'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- alsa-lib
- gcc: gcc-libs
- libjpeg-turbo
- zlib
- freetype2
- fftw
- libglvnd
- glibc
- libpng
update_on:
- alpm: iempluginsuite
  source: archpkg
