destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-krb5'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
- python-k5test
repo_depends:
- python
- krb5
repo_makedepends:
- cython
- python-setuptools
update_on:
- alias: python
- alpm: python-krb5
  source: archpkg
