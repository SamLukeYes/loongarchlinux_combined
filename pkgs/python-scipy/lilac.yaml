destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-scipy'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python-pooch
- python-numpy
update_on:
- alpm: python-scipy
  source: archpkg
