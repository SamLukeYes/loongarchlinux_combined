destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''freeradius'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- krb5
- net-snmp
- postgresql: postgresql-libs
- mariadb: mariadb-libs
- talloc
- libpcap
- libxcrypt
- samba: smbclient
repo_makedepends:
- curl
- freetds
- hiredis
- json-c
- perl
- python
- samba
- sqlite
- unixodbc
- yubico-c-client
update_on:
- alpm: libxcrypt
  provides: libcrypt.so
  source: alpm
- alpm: freeradius
  source: archpkg
