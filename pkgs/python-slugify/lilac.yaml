destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-slugify'')

  '
repo_checkdepends: []
repo_depends:
- python-text-unidecode
repo_makedepends:
- git
- python-setuptools
update_on:
- alpm: python-slugify
  source: archpkg
