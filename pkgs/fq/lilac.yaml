destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- expect
repo_depends:
- glibc
repo_makedepends:
- go
- git
update_on:
- alpm: fq
  source: archpkg
