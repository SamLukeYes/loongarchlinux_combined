destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kscreen'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-sensors
- layer-shell-qt
- libkscreen
- plasma-framework
update_on:
- alpm: kscreen
  source: archpkg
