destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''flake8'')

  '
repo_depends:
- python-mccabe
- python-pycodestyle
- python-entrypoints
- python-pyflakes
update_on:
- alpm: flake8
  source: archpkg
