destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''flake8'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-pyflakes
- python-mccabe
- python-pycodestyle
- python-entrypoints
repo_makedepends:
- python-setuptools
update_on:
- alpm: flake8
  source: archpkg
