destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tracer'')

  '
repo_depends:
- pyalpm
- python-six
- python-psutil
- dbus-python
- python-distro
- python
update_on:
- alias: python
- alpm: tracer
  source: archpkg
