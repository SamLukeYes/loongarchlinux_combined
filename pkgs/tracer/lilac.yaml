destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tracer'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-distro
- python-psutil
- python-six
- dbus-python
- pyalpm
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: tracer
  source: archpkg
