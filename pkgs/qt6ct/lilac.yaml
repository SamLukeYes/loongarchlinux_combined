destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qt6ct'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- qt6-svg
repo_makedepends:
- cmake
- qt6-tools
update_on:
- alpm: qt6ct
  source: archpkg
