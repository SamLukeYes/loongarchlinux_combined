destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-xdist'')

  '
repo_checkdepends:
- python-filelock
- python-psutil
repo_depends:
- python-execnet
- python-pytest
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-pytest-xdist
  source: archpkg
