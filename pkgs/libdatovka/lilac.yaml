destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libdatovka'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxml2
- openssl
- curl
repo_makedepends:
- docbook-xsl
update_on:
- alpm: libdatovka
  source: archpkg
