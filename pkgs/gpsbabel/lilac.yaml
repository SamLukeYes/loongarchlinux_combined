destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gpsbabel'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb
- qt6-serialport
- shapelib
- qt6-5compat
- hicolor-icon-theme
update_on:
- alpm: gpsbabel
  source: archpkg
