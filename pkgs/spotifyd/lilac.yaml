destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''spotifyd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libogg
- dbus
- alsa-lib
- pulseaudio: libpulse
update_on:
- alpm: spotifyd
  source: archpkg
