destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gource'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ftgl
- sdl2
- sdl2_image
- pcre2
- glew
- boost: boost-libs
repo_makedepends:
- boost
- glm
- mesa
update_on:
- alpm: gource
  source: archpkg
