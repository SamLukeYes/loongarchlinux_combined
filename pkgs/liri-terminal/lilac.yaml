destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''liri-terminal'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fluid
- qt5-gsettings
repo_makedepends:
- cmake
- liri-cmake-shared
- ninja
- qt5-tools
update_on:
- alpm: liri-terminal
  source: archpkg
