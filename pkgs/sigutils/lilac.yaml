destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sigutils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libsndfile
- fftw
- libvolk
repo_makedepends:
- cmake
update_on:
- alpm: sigutils
  source: archpkg
