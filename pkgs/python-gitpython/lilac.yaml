destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-gitpython'')

  '
repo_checkdepends:
- python-ddt
- python-nose
- python-pytest
- python-virtualenv
repo_depends:
- git
- python-gitdb
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-gitpython
  source: archpkg
