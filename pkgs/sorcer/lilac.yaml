destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sorcer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- ntk
- cairo
- glibc
update_on:
- alpm: ntk
  provides: libntk.so
  source: alpm
- alpm: sorcer
  source: archpkg
