destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bolt'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- umockdev
repo_depends:
- polkit
- systemd
repo_makedepends:
- asciidoc
- meson
- python-setuptools
- patch
update_on:
- alpm: bolt
  source: archpkg
