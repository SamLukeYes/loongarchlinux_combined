destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''iptables'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libnftnl
- libpcap
- libnfnetlink
- libnetfilter_conntrack
- bash
- libnftnl
- libpcap
- libnfnetlink
- libnetfilter_conntrack
- bash
- nftables
repo_makedepends:
- linux-api-headers
update_on:
- alpm: iptables
  source: archpkg
