destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mysqlclient'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- mariadb: mariadb-libs
repo_makedepends:
- mariadb
- python-setuptools
update_on:
- alias: python
- alpm: python-mysqlclient
  source: archpkg
