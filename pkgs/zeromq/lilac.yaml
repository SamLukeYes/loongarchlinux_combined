destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zeromq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- libpgm
- libsodium
- util-linux
- gnutls
- glibc
update_on:
- alpm: zeromq
  source: archpkg
