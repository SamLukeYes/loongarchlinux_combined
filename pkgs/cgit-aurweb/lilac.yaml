destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cgit-aurweb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openssl
- luajit
repo_makedepends:
- git
- zlib
- curl
- asciidoc
update_on:
- alpm: cgit-aurweb
  source: archpkg
