destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''asciinema'')

  '
repo_checkdepends: []
repo_depends:
- python
repo_makedepends:
- git
- python-setuptools
- python-wheel
- python-build
- python-installer
update_on:
- alias: python
- alpm: asciinema
  source: archpkg
