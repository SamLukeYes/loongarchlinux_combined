destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmatemixer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pulseaudio: libpulse
- alsa-lib
- glib2
- gettext
update_on:
- alpm: libmatemixer
  source: archpkg
