destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-packagekit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- desktop-file-utils
- gtk3
- libnotify
- packagekit
update_on:
- alpm: gnome-packagekit
  source: archpkg
