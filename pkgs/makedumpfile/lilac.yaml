destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''makedumpfile'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- perl
- elfutils: libelf
- zstd
repo_makedepends:
- git
update_on:
- alias: perl
- alpm: makedumpfile
  source: archpkg
