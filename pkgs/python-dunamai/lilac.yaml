destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-dunamai'')

  '
repo_checkdepends:
- python-pytest
- python-setuptools
repo_depends:
- python
- python-packaging
repo_makedepends:
- git
- python-build
- python-installer
- python-poetry-core
update_on:
- alias: python
- alpm: python-dunamai
  source: archpkg
