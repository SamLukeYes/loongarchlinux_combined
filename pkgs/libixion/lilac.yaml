destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libixion'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- boost: boost-libs
- python
repo_makedepends:
- boost
- mdds
- spdlog
update_on:
- alias: python
- alpm: libixion
  source: archpkg
