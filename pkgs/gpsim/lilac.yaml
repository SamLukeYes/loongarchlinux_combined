destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gpsim'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk2
- gtk2+extra
- popt
repo_makedepends:
- gtk2+extra
- gputils
update_on:
- alpm: gpsim
  source: archpkg
