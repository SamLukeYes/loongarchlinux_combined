destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gpsmanshp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- shapelib
- glibc
- tcl
update_on:
- alpm: gpsmanshp
  source: archpkg
