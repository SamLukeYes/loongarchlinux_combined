destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ufw'')

  '
repo_depends:
- iptables
- python
update_on:
- alpm: iptables
  source: alpm
  strip_release: true
- alias: python
- alpm: ufw
  source: archpkg
