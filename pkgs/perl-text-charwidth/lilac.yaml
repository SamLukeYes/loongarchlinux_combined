destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-text-charwidth'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends: []
update_on:
- alpm: perl-text-charwidth
  source: archpkg
