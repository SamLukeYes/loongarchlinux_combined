destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-patchwork'')

  '
repo_checkdepends: []
repo_depends:
- fabric
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-patchwork
  source: archpkg
