destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-nenv'')

  '
repo_checkdepends:
- ruby-rspec
- ruby-rake
repo_depends:
- ruby
repo_makedepends:
- git
update_on:
- alias: ruby
- alpm: ruby-nenv
  source: archpkg
