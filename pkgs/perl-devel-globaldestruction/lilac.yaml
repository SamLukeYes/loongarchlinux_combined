destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-devel-globaldestruction'')

  '
repo_depends:
- perl-sub-exporter-progressive
- perl
- perl-sub-exporter
update_on:
- alias: perl
- alpm: perl-devel-globaldestruction
  source: archpkg
