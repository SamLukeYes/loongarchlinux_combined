destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qtspell'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- enchant
- qt6-base
- iso-codes
update_on:
- alpm: qt6-base
  source: alpm
  strip_release: true
- alpm: qtspell
  source: archpkg
