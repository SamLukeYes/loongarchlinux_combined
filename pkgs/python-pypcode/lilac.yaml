destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pypcode'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-cffi
repo_makedepends:
- cmake
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-pypcode
  source: archpkg
