destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hipsparse'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- rocsparse
repo_makedepends:
- rocm-cmake
- gcc: gcc-fortran
update_on:
- alpm: hipsparse
  source: archpkg
