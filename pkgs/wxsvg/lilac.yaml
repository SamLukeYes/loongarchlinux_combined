destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wxsvg'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ffmpeg
- libexif
- wxwidgets: wxwidgets-gtk3
repo_makedepends:
- webkit2gtk
update_on:
- alpm: wxwidgets-gtk3
  source: alpm
  strip_release: true
- alpm: wxsvg
  source: archpkg
