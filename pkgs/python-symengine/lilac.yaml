destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-symengine'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- symengine
repo_makedepends:
- cmake
- cython
- cereal
- llvm
update_on:
- alias: python
- alpm: python-symengine
  source: archpkg
