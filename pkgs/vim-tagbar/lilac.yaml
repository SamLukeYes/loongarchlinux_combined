destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vim-tagbar'')

  '
repo_depends:
- ctags
update_on:
- alpm: vim-tagbar
  source: archpkg
