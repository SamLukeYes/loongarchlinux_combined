destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''filemanager-actions'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
- libgtop
repo_makedepends:
- intltool
- caja
- nautilus: libnautilus-extension
- nemo
- yelp-tools
- gtk-doc
update_on:
- alpm: filemanager-actions
  source: archpkg
