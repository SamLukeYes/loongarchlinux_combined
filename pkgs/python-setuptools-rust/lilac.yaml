destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-setuptools-rust'')

  '
repo_depends:
- rust
- python-semantic-version
- python-setuptools
update_on:
- alpm: python-setuptools-rust
  source: archpkg
