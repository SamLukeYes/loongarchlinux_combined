destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''iec16022'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zlib
- popt
update_on:
- alpm: iec16022
  source: archpkg
