destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qmdnsengine'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- qt5-base
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: qt5-base
  source: alpm
  strip_release: true
- alpm: qmdnsengine
  source: archpkg
