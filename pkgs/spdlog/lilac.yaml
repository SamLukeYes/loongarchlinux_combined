destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''spdlog'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fmt
update_on:
- manual: 1
  source: manual
- alpm: fmt
  provides: libfmt.so
  source: alpm
- alpm: spdlog
  source: archpkg
