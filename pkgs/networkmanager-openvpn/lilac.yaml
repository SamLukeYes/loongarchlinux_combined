destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''networkmanager-openvpn'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- networkmanager: libnm
- libsecret
- openvpn
repo_makedepends:
- libnma
- libnma: libnma-gtk4
- python
- git
update_on:
- alpm: networkmanager-openvpn
  source: archpkg
