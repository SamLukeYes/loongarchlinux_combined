destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libzim'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- xapian-core
- zlib
- xz
- icu
- zstd
repo_makedepends:
- meson
- gtest
- git
update_on:
- alias: icu
- alpm: libzim
  source: archpkg
