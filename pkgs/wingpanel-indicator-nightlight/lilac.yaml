destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wingpanel-indicator-nightlight'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- wingpanel
- gtk3
- glib2
update_on:
- alpm: wingpanel-indicator-nightlight
  source: archpkg
