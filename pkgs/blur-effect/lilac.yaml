destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''blur-effect'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gdk-pixbuf2
- mesa
update_on:
- alpm: blur-effect
  source: archpkg
