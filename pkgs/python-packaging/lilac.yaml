destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-packaging'')

  '
repo_checkdepends:
- python-pytest
- python-pretend
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-flit-core
update_on:
- alias: python
- alpm: python-packaging
  source: archpkg
