destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glabels'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- evolution-data-server
- qrencode
- iec16022
update_on:
- alpm: glabels
  source: archpkg
