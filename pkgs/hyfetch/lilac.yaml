destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hyfetch'')

  '
repo_depends:
- python-importlib-metadata
- python-typing_extensions
- python-setuptools
update_on:
- alpm: hyfetch
  source: archpkg
