destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mauikit-texteditor'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- mauikit
- syntax-highlighting
repo_makedepends:
- extra-cmake-modules
- kio
update_on:
- alpm: mauikit-texteditor
  source: archpkg
