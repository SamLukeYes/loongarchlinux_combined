destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sdcc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bash
- gcc: gcc-libs
- boost: boost-libs
repo_makedepends:
- gputils
- flex
- bison
- patch
- boost
update_on:
- alpm: sdcc
  source: archpkg
