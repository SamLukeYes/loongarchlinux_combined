destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aria2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ca-certificates
- c-ares
- libssh2
- libxml2
- sqlite
- gnutls
update_on:
- alpm: aria2
  source: archpkg
