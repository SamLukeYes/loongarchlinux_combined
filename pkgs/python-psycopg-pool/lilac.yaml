destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-psycopg-pool'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-psycopg
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-psycopg-pool
  source: archpkg
