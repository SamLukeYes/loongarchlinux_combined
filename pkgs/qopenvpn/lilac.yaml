destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qopenvpn'')

  '
repo_checkdepends: []
repo_depends:
- pyqt5: python-pyqt5
- openvpn
- systemd
repo_makedepends:
- python-setuptools
update_on:
- alpm: qopenvpn
  source: archpkg
