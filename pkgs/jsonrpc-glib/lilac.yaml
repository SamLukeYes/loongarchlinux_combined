destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jsonrpc-glib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- json-glib
repo_makedepends:
- gi-docgen
- git
- gobject-introspection
- meson
- vala
update_on:
- alpm: jsonrpc-glib
  source: archpkg
