destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''byzanz'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
- gstreamer: gst-plugins-base
- gstreamer: gst-plugins-good
repo_makedepends:
- gnome-common
- intltool
- git
update_on:
- alpm: byzanz
  source: archpkg
