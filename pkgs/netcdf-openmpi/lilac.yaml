destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''netcdf-openmpi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- inetutils
- unzip
repo_depends:
- hdf5-openmpi
- pnetcdf-openmpi
- curl
- libxml2
- libzip
repo_makedepends:
- cmake
update_on:
- alpm: netcdf-openmpi
  source: archpkg
