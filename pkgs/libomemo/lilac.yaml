destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libomemo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sqlite
- libgcrypt
- mxml
- glib2
update_on:
- alpm: libomemo
  source: archpkg
