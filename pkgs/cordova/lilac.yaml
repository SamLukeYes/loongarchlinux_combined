destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cordova'')

  '
repo_depends:
- node-gyp
- npm
- nodejs-nopt
- semver
update_on:
- alpm: cordova
  source: archpkg
