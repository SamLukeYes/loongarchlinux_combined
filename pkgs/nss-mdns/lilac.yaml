destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nss-mdns'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- avahi
update_on:
- alpm: nss-mdns
  source: archpkg
