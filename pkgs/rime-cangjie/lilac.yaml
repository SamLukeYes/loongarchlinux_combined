destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rime-cangjie'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- rime-luna-pinyin
repo_makedepends:
- librime
- rime-prelude
- rime-essay
update_on:
- alpm: rime-cangjie
  source: archpkg
