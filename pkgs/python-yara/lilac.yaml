destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-yara'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python
- yara
update_on:
- alias: python
- alpm: yara
  provides: libyara.so
  source: alpm
- alpm: python-yara
  source: archpkg
