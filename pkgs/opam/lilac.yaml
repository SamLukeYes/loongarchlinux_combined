destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opam'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bubblewrap
- ocaml: ocaml-compiler-libs
- unzip
repo_makedepends:
- git
update_on:
- alpm: opam
  source: archpkg
