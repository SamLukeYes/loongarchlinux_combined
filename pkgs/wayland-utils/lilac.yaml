destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wayland-utils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- wayland
- libdrm
update_on:
- alpm: wayland-utils
  source: archpkg
