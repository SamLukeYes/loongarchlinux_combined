destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pysrt'')

  '
repo_checkdepends:
- python-nose
repo_depends:
- python-chardet
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pysrt
  source: archpkg
