destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-django-fake-model'')

  '
repo_depends:
- django: python-django
update_on:
- alpm: python-django-fake-model
  source: archpkg
