destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kalendar'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kdepim-runtime
- mailcommon
- eventviews
- qqc2-desktop-style
- kirigami-addons
repo_makedepends:
- extra-cmake-modules
update_on:
- alpm: kalendar
  source: archpkg
