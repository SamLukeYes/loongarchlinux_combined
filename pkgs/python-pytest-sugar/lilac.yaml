destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-sugar'')

  '
repo_checkdepends: []
repo_depends:
- python-pytest
- python-termcolor
- python-packaging
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-pytest-sugar
  source: archpkg
