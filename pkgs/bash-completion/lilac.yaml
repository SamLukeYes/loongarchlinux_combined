destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bash-completion'')

  '
repo_depends:
- bash
update_on:
- alpm: bash-completion
  source: archpkg
