destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libheif'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- dav1d
- libde265
- aom
- x265
update_on:
- alpm: libheif
  source: archpkg
