destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ripgrep-all'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ripgrep
- xz
repo_makedepends: []
update_on:
- alpm: ripgrep-all
  source: archpkg
