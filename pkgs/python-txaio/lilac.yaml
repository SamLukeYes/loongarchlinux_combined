destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-txaio'')

  '
repo_checkdepends:
- python-pytest
- python-twisted
- python: python-tests
repo_depends:
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-txaio
  source: archpkg
