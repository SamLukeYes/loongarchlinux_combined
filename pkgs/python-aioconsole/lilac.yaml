destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-aioconsole'')

  '
repo_checkdepends:
- python-pytest-asyncio
- python-pytest-cov
- python-pytest-repeat
repo_depends:
- python
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-aioconsole
  source: archpkg
