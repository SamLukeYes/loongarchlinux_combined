destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pinta'')

  '
repo_depends:
- webp-pixbuf-loader
update_on:
- alpm: pinta
  source: archpkg
