destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mesa-amber'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxfixes
- wayland
- libxxf86vm
- libdrm
- libglvnd
- zstd
- libxshmfence
update_on:
- alpm: mesa-amber
  source: archpkg
