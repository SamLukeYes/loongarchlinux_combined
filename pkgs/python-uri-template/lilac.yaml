destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-uri-template'')

  '
repo_depends:
- python
update_on:
- alias: python
- alpm: python-uri-template
  source: archpkg
