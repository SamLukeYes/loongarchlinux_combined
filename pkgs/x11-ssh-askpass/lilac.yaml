destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''x11-ssh-askpass'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxt
update_on:
- alpm: x11-ssh-askpass
  source: archpkg
