destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''asp'')

  '
repo_depends:
- git
- libarchive
- bash
- jq
update_on:
- alpm: asp
  source: archpkg
