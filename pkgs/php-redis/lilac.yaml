destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''php-redis'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- php-igbinary
- glibc
- php-igbinary: php-legacy-igbinary
update_on:
- alpm: liblzf
  provides: liblzf.so
  source: alpm
- alpm: liblzf
  provides: liblzf.so
  source: alpm
- alpm: php-redis
  source: archpkg
