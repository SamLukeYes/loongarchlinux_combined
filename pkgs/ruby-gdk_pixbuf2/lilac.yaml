destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-gdk_pixbuf2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ruby
- ruby-gio2
- gdk-pixbuf2
repo_makedepends:
- ruby-native-package-installer
- ruby-rake
update_on:
- alias: ruby
- alpm: ruby-gdk_pixbuf2
  source: archpkg
