destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''valabind'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- glib2
- vala
- swig
repo_makedepends:
- meson
update_on:
- alpm: vala
  provides: libvala-0.56.so
  source: alpm
- alpm: valabind
  source: archpkg
