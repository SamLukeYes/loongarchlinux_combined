destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-pacman'')

  '
repo_depends:
- python-setuptools
- python
- python-pytest
update_on:
- alias: python
- alpm: python-pytest-pacman
  source: archpkg
