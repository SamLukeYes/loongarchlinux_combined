destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''skanpage'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ksanecore
- kirigami2
- purpose
- tesseract
repo_makedepends:
- extra-cmake-modules
update_on:
- alpm: skanpage
  source: archpkg
