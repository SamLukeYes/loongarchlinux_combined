destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kde-dev-scripts'')

  '
repo_checkdepends: []
repo_depends:
- perl
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alias: perl
- alpm: kde-dev-scripts
  source: archpkg
