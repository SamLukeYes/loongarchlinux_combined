destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wasmer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxkbcommon
- gcc: gcc-libs
- ncurses
- zlib
- libffi
update_on:
- alpm: wasmer
  source: archpkg
