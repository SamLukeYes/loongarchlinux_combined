destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-meilisearch'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-ordering
repo_depends:
- python
- python-requests
- python-camel-converter
- python-pydantic
repo_makedepends:
- python-setuptools
- python-build
- python-installer
- python-wheel
update_on:
- alias: python
- alpm: python-meilisearch
  source: archpkg
