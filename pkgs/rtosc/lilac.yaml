destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rtosc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
repo_makedepends:
- asciidoctor
- cmake
- doxygen
update_on:
- alpm: rtosc
  source: archpkg
