destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-mock'')

  '
repo_checkdepends:
- python-pytest-asyncio
repo_depends:
- python-pytest
repo_makedepends:
- git
- python-setuptools-scm
update_on:
- alpm: python-pytest-mock
  source: archpkg
