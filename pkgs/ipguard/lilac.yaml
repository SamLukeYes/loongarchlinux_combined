destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ipguard'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libpcap
- libnet
update_on:
- alpm: ipguard
  source: archpkg
