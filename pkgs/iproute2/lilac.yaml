destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''iproute2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- iptables
- libcap
- elfutils: libelf
- libbpf
repo_makedepends:
- linux-atm
update_on:
- alpm: iptables
  source: alpm
  strip_release: true
- alpm: iproute2
  source: archpkg
