destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rocminfo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- pciutils
- python
- hsa-rocr
repo_makedepends:
- rocm-cmake
update_on:
- alias: python
- alpm: rocminfo
  source: archpkg
