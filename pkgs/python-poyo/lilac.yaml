destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-poyo'')

  '
repo_depends:
- python
update_on:
- alias: python
- alpm: python-poyo
  source: archpkg
