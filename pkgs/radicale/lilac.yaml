destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''radicale'')

  '
repo_depends:
- python-dateutil
- python-pytz
- python-passlib
- python-vobject
- python-bcrypt
- python-defusedxml
update_on:
- alpm: radicale
  source: archpkg
