destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wingpanel-indicator-notifications'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- wingpanel
- dbus-glib
- gtk3
- glib2
- libwnck3
update_on:
- alpm: granite
  provides: libgranite.so
  source: alpm
- alpm: libhandy
  provides: libhandy-1.so
  source: alpm
- alpm: wingpanel-indicator-notifications
  source: archpkg
