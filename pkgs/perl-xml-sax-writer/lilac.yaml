destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-xml-sax-writer'')

  '
repo_checkdepends: []
repo_depends:
- perl
- perl-text-iconv
- perl-xml-filter-buffertext
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-xml-sax-writer
  source: archpkg
