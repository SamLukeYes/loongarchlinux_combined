destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''totem-pl-parser'')

  add_into_array(''arch'', [''loong64''])

  '
update_on:
- alpm: totem-pl-parser
  source: archpkg
