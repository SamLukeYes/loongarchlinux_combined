destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-mozilla-ca'')

  '
repo_depends:
- perl
- ca-certificates
update_on:
- alias: perl
- alpm: perl-mozilla-ca
  source: archpkg
