destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-test-yaml'')

  '
repo_depends:
- perl-test-base
update_on:
- alpm: perl-test-yaml
  source: archpkg
