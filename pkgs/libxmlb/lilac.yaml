destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libxmlb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- shared-mime-info
repo_depends:
- glib2
- snowball: libstemmer
- xz
repo_makedepends:
- meson
- gobject-introspection
- gtk-doc
update_on:
- alpm: libxmlb
  source: archpkg
