destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pysnmp'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-pysmi
- python-pyasn1
repo_makedepends:
- python-setuptools
- python-sphinx
update_on:
- alias: python
- alpm: python-pysnmp
  source: archpkg
