destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''podman-compose'')

  '
repo_depends:
- podman
- python-dotenv
- python-yaml
- podman-dnsname
- python
update_on:
- alias: python
- alpm: podman-compose
  source: archpkg
