destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openmotif'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxp
- libxft
- libxmu
- libpng
repo_makedepends:
- xbitmaps
- xorgproto
update_on:
- alpm: openmotif
  source: archpkg
