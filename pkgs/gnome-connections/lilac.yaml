destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-connections'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- freerdp
- gtk-vnc
- libhandy
- libsecret
update_on:
- alpm: gnome-connections
  source: archpkg
