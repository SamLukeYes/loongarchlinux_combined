destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-xlib'')

  '
repo_checkdepends:
- xorg-xauth
- xorg-server: xorg-server-xvfb
- python-nose
repo_depends:
- python-six
repo_makedepends:
- python-setuptools-scm
update_on:
- alpm: python-xlib
  source: archpkg
