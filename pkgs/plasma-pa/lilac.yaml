destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plasma-pa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- perl
- pulseaudio
- plasma-workspace
update_on:
- alias: perl
- alpm: plasma-pa
  source: archpkg
