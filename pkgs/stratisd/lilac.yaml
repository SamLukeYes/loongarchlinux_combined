destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''stratisd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pyudev
repo_depends:
- dbus
- cryptsetup
- util-linux: util-linux-libs
- clevis
- systemd
repo_makedepends:
- asciidoc
- rust
- clang
- dracut
update_on:
- alpm: stratisd
  source: archpkg
