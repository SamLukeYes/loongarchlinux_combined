destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libepubgen'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- librevenge
repo_makedepends:
- boost
- cppunit
- doxygen
- libxml2
update_on:
- alpm: libepubgen
  source: archpkg
