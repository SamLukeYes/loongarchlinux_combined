destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vagrant'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby
- xz
- curl
- libxslt
- libssh2
- libarchive
- perl
- rsync
- libxml2
update_on:
- alias: ruby
- alias: perl
- alpm: vagrant
  source: archpkg
