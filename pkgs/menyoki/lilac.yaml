destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''menyoki'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsixel
- gcc: gcc-libs
- libxrandr
update_on:
- alpm: menyoki
  source: archpkg
