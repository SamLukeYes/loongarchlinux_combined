destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-apipkg'')

  '
repo_checkdepends:
- python-pytest-runner
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-hatchling
- python-hatch-vcs
update_on:
- alias: python
- alpm: python-apipkg
  source: archpkg
