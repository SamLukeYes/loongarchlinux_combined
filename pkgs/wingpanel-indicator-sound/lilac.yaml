destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wingpanel-indicator-sound'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pulseaudio: libpulse
- wingpanel
- gdk-pixbuf2
- libgee
- libnotify
- libcanberra
- gtk3
- cairo
- glib2
update_on:
- alpm: granite
  provides: libgranite.so
  source: alpm
- alpm: wingpanel-indicator-sound
  source: archpkg
