destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lirc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb-compat
- alsa-lib
- libftdi
- libx11
update_on:
- alpm: lirc
  source: archpkg
