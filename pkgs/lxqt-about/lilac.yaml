destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lxqt-about'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- liblxqt
update_on:
- alpm: libqtxdg
  provides: libQt5Xdg.so
  source: alpm
- alpm: lxqt-about
  source: archpkg
