destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''akregator'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- knotifyconfig
- kuserfeedback
- kontactinterface
- messagelib
update_on:
- alpm: akregator
  source: archpkg
