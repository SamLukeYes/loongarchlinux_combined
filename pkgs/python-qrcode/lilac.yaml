destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-qrcode'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-typing_extensions
- python-pypng
repo_makedepends:
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-qrcode
  source: archpkg
