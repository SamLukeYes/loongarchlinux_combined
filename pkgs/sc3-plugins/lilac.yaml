destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sc3-plugins'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fftw
- stk
update_on:
- alpm: fftw
  provides: libfftw3f.so
  source: alpm
- alpm: stk
  provides: libstk-4.6.2.so
  source: alpm
- alpm: sc3-plugins
  source: archpkg
