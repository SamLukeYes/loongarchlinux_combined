destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-h11'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-h11
  source: archpkg
