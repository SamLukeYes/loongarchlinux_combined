destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-whitenoise'')

  '
repo_checkdepends:
- brotli: python-brotli
- django: python-django
- python-pytest
- python-requests
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-whitenoise
  source: archpkg
