destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wasm-bindgen'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- rust-wasm
- nodejs
update_on:
- alpm: wasm-bindgen
  source: archpkg
