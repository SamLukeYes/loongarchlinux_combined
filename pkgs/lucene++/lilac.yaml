destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lucene++'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- boost: boost-libs
repo_makedepends:
- boost
- cmake
- subversion
update_on:
- alpm: lucene++
  source: archpkg
