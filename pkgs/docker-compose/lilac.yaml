destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''docker-compose'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- docker
repo_depends: []
repo_makedepends:
- go
update_on:
- alpm: docker-compose
  source: archpkg
