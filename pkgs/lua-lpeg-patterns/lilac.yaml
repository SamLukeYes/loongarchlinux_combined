destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lua-lpeg-patterns'')

  '
repo_checkdepends: []
repo_depends:
- lpeg: lua-lpeg
- lpeg: lua51-lpeg
- lpeg: lua52-lpeg
- lpeg: lua53-lpeg
repo_makedepends: []
update_on:
- alpm: lua-lpeg-patterns
  source: archpkg
