destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''copyq'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- knotifications
- qt5-svg
- hicolor-icon-theme
- qt5-wayland
update_on:
- alpm: copyq
  source: archpkg
