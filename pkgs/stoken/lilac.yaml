destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''stoken'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nettle
- libxml2
repo_makedepends:
- gtk3
update_on:
- alpm: stoken
  source: archpkg
