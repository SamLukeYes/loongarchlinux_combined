destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cairo-dock-plug-ins'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- cairo-dock
update_on:
- alpm: cairo-dock-plug-ins
  source: archpkg
