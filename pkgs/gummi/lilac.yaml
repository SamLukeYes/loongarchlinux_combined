destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gummi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gtkspell3
- gtksourceview3
- poppler: poppler-glib
update_on:
- alpm: gummi
  source: archpkg
