destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libredefender'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- clamav
update_on:
- alpm: clamav
  provides: libclamav.so
  source: alpm
- alpm: libredefender
  source: archpkg
