destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''magnum'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openal
- sdl2
- vulkan-icd-loader
repo_makedepends:
- cmake
- git
update_on:
- alpm: magnum
  source: archpkg
