destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''leatherman'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- curl
- icu
update_on:
- manual: 1
  source: manual
- alpm: curl
  provides: libcurl.so
  source: alpm
- alias: icu
- alpm: leatherman
  source: archpkg
