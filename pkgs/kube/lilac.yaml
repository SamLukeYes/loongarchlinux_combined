destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kube'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gpgme
- sonnet
- qt5-quickcontrols2
- qt5-webengine
- qt5-quickcontrols
repo_makedepends:
- extra-cmake-modules
update_on:
- alpm: kube
  source: archpkg
