destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''tnftp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ncurses
update_on:
- alpm: ncurses
  provides: libncursesw.so
  source: alpm
- alpm: tnftp
  source: archpkg
