destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''apko'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- apk-tools
- alpine-keyring
repo_makedepends:
- go
update_on:
- alpm: apko
  source: archpkg
