destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-screenshot'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
- libhandy
repo_makedepends:
- git
- meson
- appstream-glib
update_on:
- alpm: gnome-screenshot
  source: archpkg
