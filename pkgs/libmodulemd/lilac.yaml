destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmodulemd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- file
- rpm-tools
- glib2
- libyaml
update_on:
- alpm: libmodulemd
  source: archpkg
