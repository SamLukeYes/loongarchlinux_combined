destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyogrio'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- gdal
- python-certifi
- python-numpy
repo_makedepends:
- cython
- python-setuptools
- python-versioneer
update_on:
- alpm: python-pyogrio
  source: archpkg
