destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ccid'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb
- flex
- pcsclite
update_on:
- alpm: ccid
  source: archpkg
