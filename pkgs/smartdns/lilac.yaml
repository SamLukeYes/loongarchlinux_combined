destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''smartdns'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- gtest
- bind
repo_depends:
- gcc: gcc-libs
- openssl
repo_makedepends:
- systemd
update_on:
- alpm: smartdns
  source: archpkg
