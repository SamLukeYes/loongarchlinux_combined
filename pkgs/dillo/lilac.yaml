destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dillo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zlib
- fltk
- perl
- openssl
- libpng
update_on:
- alias: perl
- alpm: dillo
  source: archpkg
