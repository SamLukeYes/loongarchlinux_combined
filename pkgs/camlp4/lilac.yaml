destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''camlp4'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ocaml
repo_makedepends:
- git
- ocamlbuild
- ocaml: ocaml-compiler-libs
- ocaml-findlib
- camlp-streams
update_on:
- alpm: ocaml
  source: alpm
  strip_release: true
- alpm: camlp4
  source: archpkg
