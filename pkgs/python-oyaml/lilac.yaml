destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-oyaml'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-yaml
repo_makedepends:
- git
- python-setuptools
update_on:
- alias: python
- alpm: python-oyaml
  source: archpkg
