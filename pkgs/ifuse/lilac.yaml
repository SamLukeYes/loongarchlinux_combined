destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ifuse'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fuse2
- libimobiledevice
- libplist
- usbmuxd
repo_makedepends:
- git
update_on:
- alpm: ifuse
  source: archpkg
