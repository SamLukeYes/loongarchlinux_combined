destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''irssi-xmpp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- loudmouth
- glib2
- irssi
update_on:
- alpm: irssi-xmpp
  source: archpkg
