destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-grab'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- git
- gcc: gcc-libs
repo_makedepends:
- rust
update_on:
- alpm: git-grab
  source: archpkg
