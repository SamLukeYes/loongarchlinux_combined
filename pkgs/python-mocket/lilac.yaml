destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mocket'')

  '
repo_depends:
- python-magic
- python-httptools
- python-urllib3
- python-decorator
update_on:
- alpm: python-mocket
  source: archpkg
