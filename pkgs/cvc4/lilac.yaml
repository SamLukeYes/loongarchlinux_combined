destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cvc4'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- boost: boost-libs
- libantlr3c
- cln
repo_makedepends:
- boost
- cmake
- python-toml
update_on:
- alpm: cvc4
  source: archpkg
