destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xfce4-taskmanager'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxfce4ui
- xfconf
- libxmu
- libwnck3
repo_makedepends:
- intltool
update_on:
- alpm: xfce4-taskmanager
  source: archpkg
