destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-janus'')

  '
repo_checkdepends:
- python-pytest-asyncio
repo_depends:
- python-typing_extensions
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-janus
  source: archpkg
