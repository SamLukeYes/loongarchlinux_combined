destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pahole'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- elfutils: libelf
- python-matplotlib
- zlib
- bash
- python
update_on:
- alias: python
- alpm: pahole
  source: archpkg
