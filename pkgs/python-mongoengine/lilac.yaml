destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mongoengine'')

  '
repo_depends:
- python-pymongo
update_on:
- alpm: python-mongoengine
  source: archpkg
