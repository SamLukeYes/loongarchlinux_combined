destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lightdm-webkit2-greeter'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- accountsservice
- webkit2gtk
- lightdm
- gnome-backgrounds
update_on:
- alpm: lightdm-webkit2-greeter
  source: archpkg
