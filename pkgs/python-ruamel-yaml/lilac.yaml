destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-ruamel-yaml'')

  '
repo_depends:
- python-ruamel.yaml.clib
update_on:
- alpm: python-ruamel-yaml
  source: archpkg
