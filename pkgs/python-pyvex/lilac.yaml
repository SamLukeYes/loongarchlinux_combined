destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyvex'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python-cffi
- python-archinfo
- python-bitstring
- python-setuptools
- python
update_on:
- alias: python
- alpm: python-pyvex
  source: archpkg
