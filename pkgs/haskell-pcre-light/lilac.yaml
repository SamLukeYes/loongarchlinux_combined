destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''haskell-pcre-light'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- haskell-hunit
repo_depends:
- ghc: ghc-libs
- pcre
repo_makedepends:
- ghc
update_on:
- alpm: haskell-pcre-light
  source: archpkg
