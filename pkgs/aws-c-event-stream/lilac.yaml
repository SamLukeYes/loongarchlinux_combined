destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aws-c-event-stream'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- aws-checksums
- aws-c-common
- aws-c-io
update_on:
- alpm: aws-c-event-stream
  source: archpkg
