destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''thc-ipv6'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libpcap
- perl
update_on:
- alpm: libpcap
  provides: libpcap.so
  source: alpm
- alias: perl
- alpm: thc-ipv6
  source: archpkg
