destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''geotag'')

  '
repo_depends:
- perl-image-exiftool
- hicolor-icon-theme
update_on:
- alpm: geotag
  source: archpkg
