destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''shim'')

  '
repo_checkdepends:
- efivar
repo_depends: []
repo_makedepends:
- git
update_on:
- alpm: shim
  source: archpkg
