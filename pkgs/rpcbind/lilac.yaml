destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rpcbind'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- bash
- glibc
- libtirpc
- systemd: systemd-libs
repo_makedepends:
- systemd
update_on:
- alpm: rpcbind
  source: archpkg
