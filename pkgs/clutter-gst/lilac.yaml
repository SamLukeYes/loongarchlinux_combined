destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''clutter-gst'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gdk-pixbuf2
- libxdamage
- gstreamer: gst-plugins-base-libs
- clutter
- libgudev
update_on:
- alpm: clutter-gst
  source: archpkg
