destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''leveldb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- snappy
repo_makedepends:
- cmake
- benchmark
update_on:
- alpm: leveldb
  source: archpkg
