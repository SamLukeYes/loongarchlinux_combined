destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sensors-applet'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- lm_sensors
- gnome-panel
- libatasmart
- nvidia-settings: libxnvctrl
update_on:
- alpm: sensors-applet
  source: archpkg
