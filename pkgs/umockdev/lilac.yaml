destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''umockdev'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- systemd
- libgudev
repo_makedepends:
- meson
- gobject-introspection
- gtk-doc
- vala
update_on:
- alpm: umockdev
  source: archpkg
