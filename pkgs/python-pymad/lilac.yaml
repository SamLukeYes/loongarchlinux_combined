destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pymad'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python
- libmad
update_on:
- alias: python
- alpm: python-pymad
  source: archpkg
