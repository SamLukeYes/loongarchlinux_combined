destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bitwarden-cli'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nodejs-lts-gallium
repo_makedepends:
- git
- npm
- modclean
update_on:
- alpm: bitwarden-cli
  source: archpkg
