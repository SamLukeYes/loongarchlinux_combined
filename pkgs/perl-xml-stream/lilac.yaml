destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-xml-stream'')

  '
repo_depends:
- perl-authen-sasl
update_on:
- alpm: perl-xml-stream
  source: archpkg
