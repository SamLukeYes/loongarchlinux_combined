destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-invoke'')

  '
repo_depends:
- python-lexicon
- python-yaml
- python-fluidity
update_on:
- alpm: python-invoke
  source: archpkg
