destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sentry_sdk'')

  '
repo_depends:
- python-urllib3
- python-certifi
update_on:
- alpm: python-sentry_sdk
  source: archpkg
