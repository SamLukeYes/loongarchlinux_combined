destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zita-at1'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- clxclient
- clthreads
- zita-resampler
- fftw
- cairo
update_on:
- alpm: zita-at1
  source: archpkg
