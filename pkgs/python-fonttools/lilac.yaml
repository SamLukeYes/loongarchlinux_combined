destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fonttools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- brotli: python-brotli
- python-fs
- python-pytest
- python-ufolib2
repo_depends:
- python
repo_makedepends:
- cython
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-fonttools
  source: archpkg
