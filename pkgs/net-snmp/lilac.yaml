destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''net-snmp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libnsl
- libpcap
- lm_sensors
- pciutils
- pcre
repo_makedepends:
- python-setuptools
update_on:
- alpm: net-snmp
  source: archpkg
