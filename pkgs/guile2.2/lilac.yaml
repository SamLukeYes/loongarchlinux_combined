destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''guile2.2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends: []
update_on:
- alpm: guile2.2
  source: archpkg
