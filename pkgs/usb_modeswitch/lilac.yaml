destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''usb_modeswitch'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libusb
- tcl
repo_makedepends:
- gcc
- make
- systemd
update_on:
- alpm: usb_modeswitch
  source: archpkg
