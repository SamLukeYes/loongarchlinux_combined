destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''liri-calculator'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fluid
repo_makedepends:
- liri-cmake-shared
- qt5-tools
update_on:
- alpm: liri-calculator
  source: archpkg
