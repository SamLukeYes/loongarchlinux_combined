destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''keycloak'')

  '
repo_checkdepends: []
repo_depends:
- grep
- bash
- coreutils
- util-linux
repo_makedepends:
- maven
update_on:
- alpm: keycloak
  source: archpkg
