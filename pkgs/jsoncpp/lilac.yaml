destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''jsoncpp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
repo_makedepends:
- gcc: gcc-libs
- doxygen
- graphviz
- python
- meson
update_on:
- alpm: jsoncpp
  source: archpkg
