destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hblock'')

  '
repo_checkdepends: []
repo_depends:
- curl
- systemd
repo_makedepends:
- git
update_on:
- alpm: hblock
  source: archpkg
