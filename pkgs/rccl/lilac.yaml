destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rccl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- rocm-smi-lib
repo_makedepends:
- rocm-cmake
- python
update_on:
- alpm: rccl
  source: archpkg
