destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''deepin-printer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cups
- deepin-qt5integration
- samba: smbclient
repo_makedepends:
- gtest
- qt5-tools
update_on:
- alpm: deepin-printer
  source: archpkg
