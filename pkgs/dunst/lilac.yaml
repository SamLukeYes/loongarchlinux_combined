destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dunst'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxinerama
- dbus
- systemd
- wayland
- libxss
- pango
- gdk-pixbuf2
- libxrandr
- glib2
repo_makedepends:
- libnotify
update_on:
- alpm: dunst
  source: archpkg
