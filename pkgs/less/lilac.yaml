destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''less'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pcre2
- glibc
- ncurses
update_on:
- alpm: less
  source: archpkg
