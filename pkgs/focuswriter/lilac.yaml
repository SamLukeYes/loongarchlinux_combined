destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''focuswriter'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt6-multimedia
- qt6-5compat
- hunspell
- hicolor-icon-theme
update_on:
- alpm: focuswriter
  source: archpkg
