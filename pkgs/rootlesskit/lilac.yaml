destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rootlesskit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- openbsd-netcat
repo_depends:
- glibc
- shadow
repo_makedepends:
- git
- go
update_on:
- alpm: rootlesskit
  source: archpkg
