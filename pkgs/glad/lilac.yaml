destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glad'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-jinja
- python-lxml
- python-setuptools
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alias: python
- alpm: glad
  source: archpkg
