destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glad'')

  '
repo_depends:
- python-lxml
- python-jinja
- python
- python-setuptools
update_on:
- alias: python
- alpm: glad
  source: archpkg
