destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''docx2txt'')

  '
repo_depends:
- unzip
- perl
update_on:
- alias: perl
- alpm: docx2txt
  source: archpkg
