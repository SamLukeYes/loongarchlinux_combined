destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''template-glib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib2
- gobject-introspection: gobject-introspection-runtime
update_on:
- alpm: template-glib
  source: archpkg
