destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libftdi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libusb
- confuse
repo_makedepends:
- boost
- cmake
- python
- swig
update_on:
- alpm: libftdi
  source: archpkg
