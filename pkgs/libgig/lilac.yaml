destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libgig'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
- util-linux: util-linux-libs
- libsndfile
repo_makedepends:
- util-linux
update_on:
- alpm: libsndfile
  provides: libsndfile.so
  source: alpm
- alpm: libgig
  source: archpkg
