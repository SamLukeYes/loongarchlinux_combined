destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lire'')

  '
repo_depends:
- perl-dbd-sqlite
- perl-libintl-perl
- perl-curses
- perl-curses-ui
- perl-xml-parser
update_on:
- alpm: lire
  source: archpkg
