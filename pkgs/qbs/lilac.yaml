destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qbs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt6-5compat
update_on:
- alpm: qbs
  source: archpkg
