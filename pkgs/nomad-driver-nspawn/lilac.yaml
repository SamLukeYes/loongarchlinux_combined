destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nomad-driver-nspawn'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- nomad
- systemd
repo_makedepends:
- git
- go
update_on:
- alpm: nomad-driver-nspawn
  source: archpkg
