destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pynacl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libsodium
- python
- python-six
- python-cffi
repo_makedepends:
- python-setuptools
- python-pycparser
- python-wheel
update_on:
- alias: python
- alpm: python-pynacl
  source: archpkg
