destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-argon2-cffi-bindings'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends: []
update_on:
- alpm: python-argon2-cffi-bindings
  source: archpkg
