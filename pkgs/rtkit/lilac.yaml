destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rtkit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- dbus
- polkit
- systemd
repo_makedepends:
- git
- meson
- vim
update_on:
- alpm: rtkit
  source: archpkg
