destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''asciiquarium'')

  '
repo_depends:
- perl
- perl-term-animation
update_on:
- alias: perl
- alpm: asciiquarium
  source: archpkg
