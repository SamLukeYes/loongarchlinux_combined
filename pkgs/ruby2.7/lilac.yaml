destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby2.7'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxcrypt
- gdbm
- openssl
- libffi
- libyaml
- gmp
- zlib
repo_makedepends:
- tk
update_on:
- alpm: ruby2.7
  source: archpkg
