destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libtirpc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- krb5
update_on:
- alpm: libtirpc
  source: archpkg
