destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''shairport-sync'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libsoxr
- pulseaudio: libpulse
- alsa-lib
- mosquitto
- avahi
- popt
- libconfig
- openssl
update_on:
- alpm: shairport-sync
  source: archpkg
