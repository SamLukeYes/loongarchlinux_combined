destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-applets'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gnome-panel
- linux-tools: cpupower
- libgtop
- tracker3
- upower
update_on:
- alpm: gnome-applets
  source: archpkg
