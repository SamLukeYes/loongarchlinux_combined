destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pysol_cards'')

  '
repo_depends:
- python-pbr
update_on:
- alpm: python-pysol_cards
  source: archpkg
