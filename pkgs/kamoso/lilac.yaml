destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kamoso'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- purpose
- kirigami2
- gstreamer: gst-plugins-good
- gstreamer: gst-plugins-bad
update_on:
- alpm: kamoso
  source: archpkg
