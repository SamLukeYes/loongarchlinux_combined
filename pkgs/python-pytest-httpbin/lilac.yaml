destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytest-httpbin'')

  '
repo_checkdepends:
- python-requests
repo_depends:
- python-pytest
- httpbin
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-pytest-httpbin
  source: archpkg
