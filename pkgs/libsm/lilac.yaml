destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libsm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libice
- util-linux: util-linux-libs
repo_makedepends:
- xorg-util-macros
- xtrans
- xorgproto
update_on:
- alpm: libsm
  source: archpkg
