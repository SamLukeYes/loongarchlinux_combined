destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''synapse'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- xdg-utils
- zeitgeist
- rest
- libgee
- libkeybinder3
- libnotify
- json-glib
- hicolor-icon-theme
update_on:
- alpm: synapse
  source: archpkg
