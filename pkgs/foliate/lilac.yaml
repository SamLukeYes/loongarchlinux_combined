destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''foliate'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gjs
- webkit2gtk
- iso-codes
update_on:
- alpm: foliate
  source: archpkg
