destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''schismtracker'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libx11
- libxv
- sdl2
- hicolor-icon-theme
repo_makedepends:
- git
- python
update_on:
- alpm: schismtracker
  source: archpkg
