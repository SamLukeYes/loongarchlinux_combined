destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''quota-tools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libnl
- dbus
- libtirpc
repo_makedepends:
- rpcsvc-proto
update_on:
- alpm: quota-tools
  source: archpkg
