destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''haruna'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kfilemetadata
- kio
- mpv
- kirigami2
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: haruna
  source: archpkg
