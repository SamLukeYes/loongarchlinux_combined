destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''redshift'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libdrm
- libxcb
- libxxf86vm
repo_makedepends:
- intltool
- python
update_on:
- alpm: redshift
  source: archpkg
