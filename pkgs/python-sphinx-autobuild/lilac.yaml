destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sphinx-autobuild'')

  '
repo_depends:
- python-sphinx
- python-livereload
- python
update_on:
- alias: python
- alpm: python-sphinx-autobuild
  source: archpkg
