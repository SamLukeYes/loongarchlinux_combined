destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''svgpart'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kparts
update_on:
- alpm: svgpart
  source: archpkg
