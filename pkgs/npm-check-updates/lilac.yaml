destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''npm-check-updates'')

  '
repo_checkdepends: []
repo_depends:
- nodejs-nopt
- npm
- semver
repo_makedepends:
- git
update_on:
- alpm: npm-check-updates
  source: archpkg
