destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-sphinx-argparse-cli'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-sphinx
- python-docutils-stubs
repo_makedepends:
- python-build
- python-installer
- python-hatchling
- python-hatch-vcs
update_on:
- alias: python
- alpm: python-sphinx-argparse-cli
  source: archpkg
