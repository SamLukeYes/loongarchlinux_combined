destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''yubico-pam'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- json-c
- yubikey-personalization
- yubico-c-client
- pam
update_on:
- alpm: yubico-pam
  source: archpkg
