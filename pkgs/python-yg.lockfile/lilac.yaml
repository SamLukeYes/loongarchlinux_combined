destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-yg.lockfile'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-zc.lockfile
- python-tempora
- python-jaraco.functools
repo_makedepends:
- git
- python-setuptools-scm
update_on:
- alpm: python-yg.lockfile
  source: archpkg
