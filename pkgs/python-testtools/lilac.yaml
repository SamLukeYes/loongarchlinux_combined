destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-testtools'')

  '
repo_checkdepends:
- python-testscenarios
repo_depends:
- python-pbr
- python-fixtures
repo_makedepends:
- git
- python-setuptools
update_on:
- alpm: python-testtools
  source: archpkg
