destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''visidata'')

  '
repo_depends:
- python-importlib-metadata
- python-dateutil
- python
- python-setuptools
update_on:
- alias: python
- alpm: visidata
  source: archpkg
