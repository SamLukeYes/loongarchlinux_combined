destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-http-daemon'')

  '
repo_depends:
- perl-http-message
- perl-lwp-mediatypes
- perl
- perl-http-date
update_on:
- alias: perl
- alpm: perl-http-daemon
  source: archpkg
