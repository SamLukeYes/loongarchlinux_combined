destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-aiorpcx'')

  '
repo_checkdepends: []
repo_depends:
- python-attrs
repo_makedepends:
- python-setuptools
- python-websockets
update_on:
- alpm: python-aiorpcx
  source: archpkg
