destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-litedram'')

  '
repo_depends:
- python-migen
- python-litex
- python-yaml
update_on:
- alpm: python-litedram
  source: archpkg
