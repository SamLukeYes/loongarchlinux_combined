destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''hwloc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- libtool
- systemd: systemd-libs
- libpciaccess
repo_makedepends:
- cairo
- libxml2
- pciutils
- systemd
- libx11
update_on:
- alpm: hwloc
  source: archpkg
