destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libwebp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- giflib
- libpng
- libtiff
repo_makedepends:
- cmake
- ninja
update_on:
- alpm: libwebp
  source: archpkg
