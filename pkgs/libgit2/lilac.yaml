destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libgit2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- pcre2
- zlib
- libssh2
- openssl
- glibc
- http-parser
update_on:
- alpm: libssh2
  provides: libssh2.so
  source: alpm
- alias: libcrypto
- alias: libssl
- alpm: pcre2
  provides: libpcre2-8.so
  source: alpm
- alpm: libgit2
  source: archpkg
