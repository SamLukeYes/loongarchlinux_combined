destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gpsd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb
- bluez: bluez-libs
- python
- pps-tools
update_on:
- alias: python
- alpm: gpsd
  source: archpkg
