destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''garcon'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxfce4ui
repo_makedepends:
- intltool
- python
- gobject-introspection
update_on:
- alpm: garcon
  source: archpkg
