destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dvisvgm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- texlive-bin
- woff2
- ghostscript
repo_makedepends:
- python
update_on:
- alpm: dvisvgm
  source: archpkg
