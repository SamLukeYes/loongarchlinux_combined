destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vsftpd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libnsl
- libcap
- openssl
- glibc
- pam
update_on:
- alpm: vsftpd
  source: archpkg
