destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libfishsound'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- speex
- libvorbis
- flac
update_on:
- alpm: libfishsound
  source: archpkg
