destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xterm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxft
- libxaw
- ncurses
- luit
- xbitmaps
- libutempter
- libxkbfile
repo_makedepends: []
update_on:
- alpm: xterm
  source: archpkg
