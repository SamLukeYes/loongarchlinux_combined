destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ansible-core'')

  '
repo_depends:
- python-paramiko
- python-cryptography
- python-jinja
- python-packaging
- python-resolvelib
- python
update_on:
- alias: python
- alpm: ansible-core
  source: archpkg
