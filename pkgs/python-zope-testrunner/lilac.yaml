destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-zope-testrunner'')

  '
repo_depends:
- python-zope-exceptions
- python-zope-interface
- python-setuptools
update_on:
- alpm: python-zope-testrunner
  source: archpkg
