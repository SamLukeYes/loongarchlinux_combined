destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libmatroska'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- cmake
- git
- ninja
update_on:
- alpm: libebml
  provides: libebml.so
  source: alpm
- alpm: libmatroska
  source: archpkg
