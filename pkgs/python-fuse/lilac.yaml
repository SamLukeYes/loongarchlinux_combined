destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fuse'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fuse2
- python
repo_makedepends:
- python-setuptools
- fuse2
update_on:
- alias: python
- alpm: python-fuse
  source: archpkg
