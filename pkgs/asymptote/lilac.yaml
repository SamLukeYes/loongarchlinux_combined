destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''asymptote'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- freeglut
- libsigsegv
- gsl
- gc
- fftw
update_on:
- alpm: gsl
  source: alpm
  strip_release: true
- alpm: asymptote
  source: archpkg
