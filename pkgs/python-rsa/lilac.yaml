destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-rsa'')

  '
repo_depends:
- python-pyasn1
update_on:
- alpm: python-rsa
  source: archpkg
