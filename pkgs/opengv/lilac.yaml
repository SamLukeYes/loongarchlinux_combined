destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opengv'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pybind11
- python-numpy
- eigen
update_on:
- alpm: opengv
  source: archpkg
