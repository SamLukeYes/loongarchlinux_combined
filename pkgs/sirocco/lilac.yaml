destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sirocco'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- mpfr
update_on:
- alpm: sirocco
  source: archpkg
