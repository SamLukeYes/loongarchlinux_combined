destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''firefox-extension-mailvelope'')

  '
repo_depends:
- firefox
- gpgme
update_on:
- alpm: firefox-extension-mailvelope
  source: archpkg
