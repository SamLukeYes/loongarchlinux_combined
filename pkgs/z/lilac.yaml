destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''z'')

  '
repo_checkdepends: []
repo_depends:
- bash
repo_makedepends: []
update_on:
- alpm: z
  source: archpkg
