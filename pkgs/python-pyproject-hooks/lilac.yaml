destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyproject-hooks'')

  '
repo_checkdepends:
- python-pytest
- python-setuptools
- python-testpath
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-flit-core
- python-wheel
update_on:
- alias: python
- alpm: python-pyproject-hooks
  source: archpkg
