destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-subunit'')

  '
repo_checkdepends:
- python-fixtures
- python-hypothesis
- python-testscenarios
repo_depends:
- python-extras
- python-testtools
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-subunit
  source: archpkg
