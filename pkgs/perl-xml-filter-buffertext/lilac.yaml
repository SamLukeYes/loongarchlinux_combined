destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-xml-filter-buffertext'')

  '
repo_depends:
- perl-xml-sax
update_on:
- alpm: perl-xml-filter-buffertext
  source: archpkg
