destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''timezonemap'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gtk3
- libsoup
repo_makedepends:
- gobject-introspection
update_on:
- alpm: timezonemap
  source: archpkg
