destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-gmpy2'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- libmpc
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-gmpy2
  source: archpkg
