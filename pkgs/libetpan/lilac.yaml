destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libetpan'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gnutls
- libgcrypt
- libsasl
- zlib
update_on:
- alpm: libetpan
  source: archpkg
