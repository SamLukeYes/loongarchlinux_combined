destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ttf-jetbrains-mono'')

  '
update_on:
- alpm: ttf-jetbrains-mono
  source: archpkg
