destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''protobuf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- gcc: gcc-libs
- glibc
- zlib
- protobuf
- python
repo_makedepends:
- cmake
- gtest
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: protobuf
  source: archpkg
