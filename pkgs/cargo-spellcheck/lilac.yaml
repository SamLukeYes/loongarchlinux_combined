destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cargo-spellcheck'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- hunspell
repo_makedepends:
- clang
update_on:
- alpm: cargo-spellcheck
  source: archpkg
