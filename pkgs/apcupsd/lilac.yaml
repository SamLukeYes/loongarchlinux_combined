destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''apcupsd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libusb-compat
- gcc: gcc-libs
- systemd: systemd-sysvcompat
update_on:
- alpm: apcupsd
  source: archpkg
