destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cuda'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- nvidia-utils: opencl-nvidia
- nvidia-utils
- python
- gcc12
- cuda
- nss
repo_makedepends: []
update_on:
- alias: python
- alpm: cuda
  source: archpkg
