destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nyx'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-stem
- net-tools
- procps-ng
- lsof
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: nyx
  source: archpkg
