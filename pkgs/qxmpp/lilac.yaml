destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qxmpp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gstreamer
- qca: qca-qt6
- qca: qca-qt5
update_on:
- alpm: qxmpp
  source: archpkg
