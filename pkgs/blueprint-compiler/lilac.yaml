destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''blueprint-compiler'')

  '
repo_depends: []
update_on:
- alpm: blueprint-compiler
  source: archpkg
