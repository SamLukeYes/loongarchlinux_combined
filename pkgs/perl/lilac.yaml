destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- db5.3
- libxcrypt
- db
- glibc
update_on:
- alpm: libxcrypt
  provides: libcrypt.so
  source: alpm
- alpm: perl
  source: archpkg
