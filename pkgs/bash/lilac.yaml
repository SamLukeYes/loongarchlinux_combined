destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bash'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- readline
update_on:
- manual: 1
  source: manual
- alpm: readline
  provides: libreadline.so
  source: alpm
- alpm: bash
  source: archpkg
