destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jsonpatch'')

  '
repo_checkdepends:
- python-coverage
repo_depends:
- python-jsonpointer
repo_makedepends:
- python-setuptools
- python-pypandoc
update_on:
- alpm: python-jsonpatch
  source: archpkg
