destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''clazy'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- clang
repo_makedepends:
- cmake
- llvm
update_on:
- alias: clang
- alpm: clazy
  source: archpkg
