destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-schemdraw'')

  '
repo_checkdepends:
- python-pytest-runner
- python-nbval
- python-matplotlib
- python-ziafont
- python-ziamath
- python-latex2mathml
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-schemdraw
  source: archpkg
