destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''modem-manager-gui'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libnotify
- gdbm
- modemmanager
- gtkspell3
- gtk3
update_on:
- alpm: modem-manager-gui
  source: archpkg
