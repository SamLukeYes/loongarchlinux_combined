destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''netcf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxslt
- libxml2
- libnl
repo_makedepends:
- gcc
update_on:
- alpm: netcf
  source: archpkg
