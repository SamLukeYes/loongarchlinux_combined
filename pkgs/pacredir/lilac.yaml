destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pacredir'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- systemd: systemd-libs
- avahi
- curl
- iniparser
- darkhttpd
- libmicrohttpd
repo_makedepends:
- systemd
- discount
update_on:
- alpm: pacredir
  source: archpkg
