destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-hist'')

  '
repo_depends:
- python-histoprint
- python-typing_extensions
- python-boost-histogram
update_on:
- alpm: python-hist
  source: archpkg
