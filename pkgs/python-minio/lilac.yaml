destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-minio'')

  '
repo_depends:
- python-urllib3
- python
- python-certifi
update_on:
- alias: python
- alpm: python-minio
  source: archpkg
