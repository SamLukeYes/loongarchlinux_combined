destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''camlp5'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- camlp-streams
- ocaml
update_on:
- alpm: ocaml
  source: alpm
  strip_release: true
- alpm: camlp5
  source: archpkg
