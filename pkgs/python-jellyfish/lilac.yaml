destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jellyfish'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
repo_makedepends:
- git
- maturin: python-maturin
- python-build
- python-installer
update_on:
- alias: python
- alpm: python-jellyfish
  source: archpkg
