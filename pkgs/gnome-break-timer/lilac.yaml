destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-break-timer'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gsound
- gtk3
- libnotify
repo_makedepends:
- meson
- git
- vala
update_on:
- alpm: gnome-break-timer
  source: archpkg
