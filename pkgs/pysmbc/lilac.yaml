destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pysmbc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python
- samba: smbclient
repo_makedepends:
- python
- samba: smbclient
- python-setuptools
update_on:
- alias: python
- alpm: pysmbc
  source: archpkg
