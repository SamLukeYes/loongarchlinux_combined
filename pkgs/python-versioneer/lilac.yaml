destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-versioneer'')

  '
repo_depends:
- python-tomli
- python
- python-setuptools
update_on:
- alias: python
- alpm: python-versioneer
  source: archpkg
