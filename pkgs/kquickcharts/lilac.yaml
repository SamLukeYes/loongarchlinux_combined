destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kquickcharts'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-quickcontrols2
update_on:
- alpm: kquickcharts
  source: archpkg
