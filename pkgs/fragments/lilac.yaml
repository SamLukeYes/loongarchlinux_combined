destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fragments'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libadwaita
- transmission: transmission-cli
repo_makedepends:
- git
- meson
- rust
update_on:
- alpm: fragments
  source: archpkg
