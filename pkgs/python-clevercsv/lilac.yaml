destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-clevercsv'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest-runner
- python-pandas
- python-tabview
- python-wilderness
- python-cchardet
- python-termcolor
repo_depends:
- python-chardet
- python-regex
- python-packaging
repo_makedepends:
- git
- python-setuptools
update_on:
- alpm: python-clevercsv
  source: archpkg
