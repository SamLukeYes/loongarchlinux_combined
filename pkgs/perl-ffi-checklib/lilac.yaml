destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-ffi-checklib'')

  '
repo_checkdepends:
- perl-file-which
- perl-test2-suite
- perl-test2-tools-process
repo_depends:
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-ffi-checklib
  source: archpkg
