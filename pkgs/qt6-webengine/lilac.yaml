destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''qt6-webengine'')

  add_into_array(''arch'', [''loong64''])

  '
update_on:
- alpm: qt6-webengine
  source: archpkg
