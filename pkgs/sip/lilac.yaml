destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sip'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- python-toml
- python-packaging
- python-setuptools
- python-ply
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alpm: sip
  source: archpkg
