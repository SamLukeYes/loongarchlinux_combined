destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-devel-checkcompiler'')

  '
repo_checkdepends:
- perl-test-requires
repo_depends:
- perl
repo_makedepends:
- perl-module-build
update_on:
- alias: perl
- alpm: perl-devel-checkcompiler
  source: archpkg
