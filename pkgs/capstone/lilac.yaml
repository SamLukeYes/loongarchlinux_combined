destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''capstone'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- capstone
- python
repo_makedepends:
- glibc
- python
- python-setuptools
update_on:
- alias: python
- alpm: capstone
  source: archpkg
