destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''aalib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ncurses
- gpm
- libx11
- glibc
- slang
update_on:
- alpm: aalib
  source: archpkg
