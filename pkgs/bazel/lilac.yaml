destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bazel'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libarchive
- zip
- unzip
repo_makedepends:
- git
- protobuf
- python
- gcc12
update_on:
- alpm: bazel
  source: archpkg
