destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ksshaskpass'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kwallet
repo_makedepends:
- extra-cmake-modules
- kdoctools
update_on:
- alpm: ksshaskpass
  source: archpkg
