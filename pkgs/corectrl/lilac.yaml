destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''corectrl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-base
- fmt
update_on:
- alpm: qt5-base
  source: alpm
  strip_release: true
- alpm: fmt
  provides: libfmt.so
  source: alpm
- alpm: corectrl
  source: archpkg
