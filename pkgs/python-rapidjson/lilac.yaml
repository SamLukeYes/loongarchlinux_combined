destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-rapidjson'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest-runner
- python-pytest-benchmark
- python-pytz
repo_depends:
- python
repo_makedepends:
- python-setuptools
- rapidjson
update_on:
- alias: python
- alpm: python-rapidjson
  source: archpkg
