destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-outcome'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-asyncio
- python-pytest-cov
repo_depends:
- python
- python-attrs
repo_makedepends:
- git
- python-setuptools
update_on:
- alias: python
- alpm: python-outcome
  source: archpkg
