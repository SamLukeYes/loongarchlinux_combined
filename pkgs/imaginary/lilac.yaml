destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''imaginary'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- libvips
- lcms2
- openjpeg2
- poppler: poppler-glib
- openslide
- libjxl
repo_makedepends:
- go
update_on:
- alpm: imaginary
  source: archpkg
