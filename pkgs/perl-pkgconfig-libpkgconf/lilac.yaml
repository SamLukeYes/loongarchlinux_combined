destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-pkgconfig-libpkgconf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pkgconf
- perl
update_on:
- alias: perl
- alpm: perl-pkgconfig-libpkgconf
  source: archpkg
