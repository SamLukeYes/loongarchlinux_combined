destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''icmake'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libbobcat
- libunwind
repo_makedepends:
- icmake
- yodl
update_on:
- alpm: icmake
  source: archpkg
