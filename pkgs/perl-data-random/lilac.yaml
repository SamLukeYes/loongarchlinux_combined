destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-data-random'')

  '
repo_depends:
- perl-yaml-tiny
- perl-gd
update_on:
- alpm: perl-data-random
  source: archpkg
