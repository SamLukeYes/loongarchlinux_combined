destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libkolabxml'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- xerces-c
- boost: boost-libs
repo_makedepends:
- cmake
- boost
- xsd
- swig
update_on:
- alpm: libkolabxml
  source: archpkg
