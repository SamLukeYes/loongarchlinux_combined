destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''skopeo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gpgme
- lvm2: device-mapper
- containers-common
repo_makedepends:
- go
- go-md2man
- btrfs-progs
- git
- systemd
update_on:
- alpm: skopeo
  source: archpkg
