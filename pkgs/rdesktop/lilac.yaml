destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rdesktop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libao
- libsamplerate
- xorg-xrandr
- pcsclite
- libgssglue
- libxcursor
repo_makedepends: []
update_on:
- alpm: rdesktop
  source: archpkg
