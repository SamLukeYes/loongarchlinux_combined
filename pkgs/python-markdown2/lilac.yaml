destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-markdown2'')

  '
repo_checkdepends:
- python-tox-current-env
repo_depends:
- python
- python-pygments
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-markdown2
  source: archpkg
