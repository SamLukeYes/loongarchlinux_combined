destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vimiv'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- python-pillow
- gtk3
- pygobject: python-gobject
- glibc
- python
update_on:
- alias: python
- alpm: vimiv
  source: archpkg
