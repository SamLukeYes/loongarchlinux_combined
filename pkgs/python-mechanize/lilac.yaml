destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mechanize'')

  '
repo_depends:
- python-html5lib
update_on:
- alpm: python-mechanize
  source: archpkg
