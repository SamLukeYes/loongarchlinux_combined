destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-warlock'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-jsonschema
- python-jsonpatch
repo_makedepends:
- python-build
- python-installer
- python-poetry-core
update_on:
- alpm: python-warlock
  source: archpkg
