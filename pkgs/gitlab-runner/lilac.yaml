destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gitlab-runner'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- git
- ca-certificates
- curl
- tar
- glibc
update_on:
- alpm: gitlab-runner
  source: archpkg
