destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libktorrent'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kio
- qca: qca-qt5
update_on:
- alpm: libktorrent
  source: archpkg
