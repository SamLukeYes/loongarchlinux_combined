destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''maui-clip'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- taglib
- mauikit-filebrowsing
- ffmpeg
update_on:
- alpm: maui-clip
  source: archpkg
