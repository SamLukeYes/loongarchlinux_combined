destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-kajiki'')

  '
repo_checkdepends:
- python-babel
- python-pytest
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-sphinx
- python-wheel
update_on:
- alias: python
- alpm: python-kajiki
  source: archpkg
