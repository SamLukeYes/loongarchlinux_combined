destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ibus-sunpinyin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sunpinyin
- sunpinyin-data
- ibus
- gtk3
- pygobject: python-gobject
update_on:
- alpm: ibus-sunpinyin
  source: archpkg
