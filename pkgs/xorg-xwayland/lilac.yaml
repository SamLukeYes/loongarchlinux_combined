destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xorg-xwayland'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libepoxy
- xorg-server: xorg-server-common
- libxfont2
- libxcvt
- mesa
- pixman
- nettle
- systemd: systemd-libs
update_on:
- alpm: xorg-xwayland
  source: archpkg
