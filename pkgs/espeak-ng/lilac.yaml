destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''espeak-ng'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- pcaudiolib
- libsonic
repo_makedepends:
- ruby-ronn-ng
update_on:
- alpm: espeak-ng
  source: archpkg
