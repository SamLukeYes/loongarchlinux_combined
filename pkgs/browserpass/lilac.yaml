destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''browserpass'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gnupg
- glibc
repo_makedepends:
- go
- git
update_on:
- alpm: browserpass
  source: archpkg
