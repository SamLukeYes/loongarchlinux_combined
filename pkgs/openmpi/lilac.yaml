destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openmpi'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openpmix
update_on:
- alpm: openpmix
  provides: libpmix.so
  source: alpm
- alpm: openmpi
  source: archpkg
