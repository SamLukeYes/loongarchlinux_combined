destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-editdistance'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-nose
repo_depends:
- python
repo_makedepends:
- python-setuptools
- cython
update_on:
- alias: python
- alpm: python-editdistance
  source: archpkg
