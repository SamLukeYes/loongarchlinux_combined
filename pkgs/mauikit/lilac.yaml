destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mauikit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- knotifications
- kirigami2
- mauiman
- ki18n
- qt5-svg
update_on:
- alpm: mauikit
  source: archpkg
