destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''avr-gcc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- avr-binutils
- libisl
- libmpc
- zstd
update_on:
- alpm: avr-gcc
  source: archpkg
