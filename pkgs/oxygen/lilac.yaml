destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''oxygen'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- frameworkintegration
- hicolor-icon-theme
- kdecoration
- kwayland
update_on:
- alpm: oxygen
  source: archpkg
