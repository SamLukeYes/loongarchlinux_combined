destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nftables'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libmnl
- ncurses
- jansson
- libnftnl
- readline
- gmp
update_on:
- alias: readline
- alpm: nftables
  source: archpkg
