destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pytrie'')

  '
repo_checkdepends:
- python: python-tests
repo_depends:
- python
- python-sortedcontainers
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-pytrie
  source: archpkg
