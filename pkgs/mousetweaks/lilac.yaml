destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mousetweaks'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libxtst
- gtk3
- gsettings-desktop-schemas
repo_makedepends:
- python
- git
update_on:
- alpm: mousetweaks
  source: archpkg
