destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''glew1.10'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxi
- libx11
- libxext
- glibc
- libxmu
update_on:
- alpm: glew1.10
  source: archpkg
