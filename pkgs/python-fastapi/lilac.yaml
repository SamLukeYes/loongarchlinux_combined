destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-fastapi'')

  '
repo_depends:
- python-starlette
- python-anyio
- python-pydantic
- python
update_on:
- alias: python
- alpm: python-fastapi
  source: archpkg
