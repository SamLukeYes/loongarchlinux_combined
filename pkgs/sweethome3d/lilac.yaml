destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sweethome3d'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- shared-mime-info
- desktop-file-utils
- libxext
- libxrender
- libnsl
repo_makedepends:
- ant
- strip-nondeterminism
update_on:
- alpm: sweethome3d
  source: archpkg
