destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mygui'')

  add_into_array(''arch'', [''loong64''])

  '
update_on:
- alpm: mygui
  source: archpkg
