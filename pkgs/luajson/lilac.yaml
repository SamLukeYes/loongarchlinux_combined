destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''luajson'')

  '
repo_checkdepends: []
repo_depends:
- lpeg: lua52-lpeg
repo_makedepends:
- lpeg: lua52-lpeg
update_on:
- alpm: luajson
  source: archpkg
