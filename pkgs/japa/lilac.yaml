destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''japa'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- clxclient
- zita-alsa-pcmi
- clthreads
- fftw
- libx11
- glibc
update_on:
- alpm: japa
  source: archpkg
