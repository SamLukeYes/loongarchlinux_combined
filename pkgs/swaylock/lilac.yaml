destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''swaylock'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxkbcommon
- gdk-pixbuf2
- wayland
- cairo
- pam
update_on:
- alpm: swaylock
  source: archpkg
