destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fcft'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- ttf-dejavu
repo_depends:
- fontconfig
- freetype2
- harfbuzz
- libutf8proc
- pixman
repo_makedepends:
- meson
- scdoc
- tllist
update_on:
- alpm: fcft
  source: archpkg
