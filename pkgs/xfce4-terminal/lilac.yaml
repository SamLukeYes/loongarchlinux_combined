destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''xfce4-terminal'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- hicolor-icon-theme
- libxfce4ui
- vte3
update_on:
- alpm: xfce4-terminal
  source: archpkg
