destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''bumblebee'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- virtualgl
- glib2
repo_makedepends:
- help2man
update_on:
- alpm: bumblebee
  source: archpkg
