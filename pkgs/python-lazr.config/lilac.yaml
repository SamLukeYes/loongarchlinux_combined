destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-lazr.config'')

  '
repo_depends:
- python-lazr.delegates
- python-zope-interface
- python
- python-setuptools
update_on:
- alias: python
- alpm: python-lazr.config
  source: archpkg
