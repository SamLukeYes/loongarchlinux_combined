destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-flask'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-werkzeug
- python-jinja
- python-itsdangerous
- python-click
repo_makedepends:
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alpm: python-flask
  source: archpkg
