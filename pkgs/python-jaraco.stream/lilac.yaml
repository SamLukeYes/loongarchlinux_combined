destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-jaraco.stream'')

  '
repo_checkdepends:
- python-more-itertools
- python-pytest
repo_depends:
- python
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-setuptools-scm
- python-toml
- python-wheel
update_on:
- alias: python
- alpm: python-jaraco.stream
  source: archpkg
