destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opensearch-dashboards-security-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- opensearch-dashboards
update_on:
- alpm: opensearch-dashboards-security-plugin
  source: archpkg
