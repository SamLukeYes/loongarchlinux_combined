destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vapoursynth'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zimg
- python
update_on:
- alpm: zimg
  provides: libzimg.so
  source: alpm
- alias: python
- alpm: vapoursynth
  source: archpkg
