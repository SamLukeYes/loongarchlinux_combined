destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''coin-or-clp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- coin-or-asl
- suitesparse
- coin-or-osi
update_on:
- alpm: coin-or-clp
  source: archpkg
