destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''uget'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libnotify
- curl
- gtk3
- gstreamer
update_on:
- alpm: uget
  source: archpkg
