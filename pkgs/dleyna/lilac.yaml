destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dleyna'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gupnp-dlna
- libsoup3
- dbus
- libxml2
- gupnp
- gupnp-av
update_on:
- alpm: dleyna
  source: archpkg
