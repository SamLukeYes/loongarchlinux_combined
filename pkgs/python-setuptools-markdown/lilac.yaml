destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-setuptools-markdown'')

  '
repo_checkdepends: []
repo_depends:
- python-setuptools
- python-pypandoc
repo_makedepends: []
update_on:
- alpm: python-setuptools-markdown
  source: archpkg
