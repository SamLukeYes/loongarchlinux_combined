destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''zutty'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libglvnd
- freetype2
- xorg-fonts-misc
- libxmu
update_on:
- alpm: zutty
  source: archpkg
