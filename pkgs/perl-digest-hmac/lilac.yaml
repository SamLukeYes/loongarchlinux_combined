destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-digest-hmac'')

  '
repo_checkdepends: []
repo_depends:
- perl-digest-sha1
- perl
repo_makedepends: []
update_on:
- alias: perl
- alpm: perl-digest-hmac
  source: archpkg
