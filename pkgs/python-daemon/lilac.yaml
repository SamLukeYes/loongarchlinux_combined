destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-daemon'')

  '
repo_depends:
- python-lockfile
- python
- python-setuptools
update_on:
- alias: python
- alpm: python-daemon
  source: archpkg
