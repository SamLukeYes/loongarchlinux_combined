destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''coin-or-mp'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- coin-or-cbc
update_on:
- alpm: coin-or-mp
  source: archpkg
