destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''wgetpaste'')

  '
repo_depends:
- wget
update_on:
- alpm: wgetpaste
  source: archpkg
