destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''singularity'')

  '
repo_depends:
- python-pygame
- python-numpy
- python
update_on:
- alias: python
- alpm: singularity
  source: archpkg
