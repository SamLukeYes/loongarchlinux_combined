destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-dulwich'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- gpgme: python-gpgme
- python-paramiko
repo_depends:
- python-urllib3
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alpm: python-dulwich
  source: archpkg
