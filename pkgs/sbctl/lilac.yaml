destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sbctl'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- binutils
- util-linux
repo_makedepends:
- go
- git
- asciidoc
update_on:
- alpm: sbctl
  source: archpkg
