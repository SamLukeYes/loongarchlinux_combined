destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opensearch-index-management-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- opensearch
- opensearch-notifications-plugin
repo_makedepends:
- unzip
update_on:
- alpm: opensearch-index-management-plugin
  source: archpkg
