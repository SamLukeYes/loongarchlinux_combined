destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mysql++'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- mariadb: mariadb-libs
repo_makedepends: []
update_on:
- alpm: mysql++
  source: archpkg
