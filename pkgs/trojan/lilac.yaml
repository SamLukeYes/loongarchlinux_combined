destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''trojan'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- openssl
- python
- curl
repo_depends:
- boost: boost-libs
- openssl
- mariadb: mariadb-libs
repo_makedepends:
- cmake
- boost
- openssl
- mariadb: mariadb-libs
update_on:
- alpm: trojan
  source: archpkg
