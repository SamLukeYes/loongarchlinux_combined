destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kdav'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kio
repo_makedepends:
- extra-cmake-modules
- doxygen
- qt5-tools
update_on:
- alpm: kdav
  source: archpkg
