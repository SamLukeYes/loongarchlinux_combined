destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libspatialite'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- librttopo
- geos
- libfreexl
- proj
- libxml2
- zlib: minizip
- sqlite
update_on:
- alpm: libspatialite
  source: archpkg
