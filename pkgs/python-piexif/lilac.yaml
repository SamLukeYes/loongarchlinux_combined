destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-piexif'')

  '
repo_checkdepends:
- python-pytest
- python-pillow
- libwebp
- python-sphinx
repo_depends:
- python
repo_makedepends:
- python-setuptools
update_on:
- alias: python
- alpm: python-piexif
  source: archpkg
