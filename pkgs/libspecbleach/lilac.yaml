destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libspecbleach'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- fftw
update_on:
- alpm: fftw
  provides: libfftw3f.so
  source: alpm
- alpm: libspecbleach
  source: archpkg
