destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-rtree'')

  '
repo_checkdepends:
- python-pytest-runner
- python-pytest-cov
- python-numpy
repo_depends:
- python
- spatialindex
repo_makedepends:
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-rtree
  source: archpkg
