destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-litesata'')

  '
repo_checkdepends: []
repo_depends:
- python-migen
- python-litex
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-litesata
  source: archpkg
