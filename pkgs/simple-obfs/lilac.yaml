destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''simple-obfs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libev
repo_makedepends:
- asciidoc
- xmlto
update_on:
- alpm: simple-obfs
  source: archpkg
