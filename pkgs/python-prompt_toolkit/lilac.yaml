destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-prompt_toolkit'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- python-pygments
- python-wcwidth
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools
update_on:
- alias: python
- alpm: python-prompt_toolkit
  source: archpkg
