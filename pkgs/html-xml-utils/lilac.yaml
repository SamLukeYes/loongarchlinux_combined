destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''html-xml-utils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- curl
- libidn2
update_on:
- alpm: html-xml-utils
  source: archpkg
