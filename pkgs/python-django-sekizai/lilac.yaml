destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-django-sekizai'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- django: python-django
- python-django-classy-tags
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-django-sekizai
  source: archpkg
