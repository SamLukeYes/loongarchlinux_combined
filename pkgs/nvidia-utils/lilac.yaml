destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nvidia-utils'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- egl-wayland
- dkms
- zlib
- libglvnd
- xorg-server
update_on:
- alpm: nvidia-utils
  source: archpkg
