destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sudo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- pam
- zlib
- openssl
- glibc
- openldap: libldap
update_on:
- alias: libcrypto
- alias: libssl
- alpm: sudo
  source: archpkg
