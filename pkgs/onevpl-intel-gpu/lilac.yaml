destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''onevpl-intel-gpu'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- intel-media-driver
- libdrm
- libva
repo_makedepends:
- cmake
update_on:
- alpm: onevpl-intel-gpu
  source: archpkg
