destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''golang-golang-x-image'')

  '
repo_depends:
- golang-golang-x-text
- go
update_on:
- alpm: golang-golang-x-image
  source: archpkg
