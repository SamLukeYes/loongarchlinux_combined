destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plymouth'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glibc
- pango
- systemd: systemd-libs
- filesystem
- libdrm
- bash
- cairo
- glib2
- cantarell-fonts
- libpng
update_on:
- alpm: plymouth
  source: archpkg
