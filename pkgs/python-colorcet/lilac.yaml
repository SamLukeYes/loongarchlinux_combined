destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-colorcet'')

  '
repo_depends:
- python-pyct
update_on:
- alpm: python-colorcet
  source: archpkg
