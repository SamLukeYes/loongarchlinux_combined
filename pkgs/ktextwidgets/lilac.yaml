destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ktextwidgets'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-speech
- kcompletion
- sonnet
- kconfigwidgets
update_on:
- alpm: ktextwidgets
  source: archpkg
