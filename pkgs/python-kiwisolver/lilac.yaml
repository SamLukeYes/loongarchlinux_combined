destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-kiwisolver'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest-runner
repo_depends:
- python
repo_makedepends:
- git
- python-cppy
- python-setuptools-scm
- python-wheel
update_on:
- alias: python
- alpm: python-kiwisolver
  source: archpkg
