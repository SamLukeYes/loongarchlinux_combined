destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vim-ansible'')

  '
repo_checkdepends: []
repo_depends:
- python-jinja
- vim
repo_makedepends:
- ansible
- python
update_on:
- alpm: vim-ansible
  source: archpkg
