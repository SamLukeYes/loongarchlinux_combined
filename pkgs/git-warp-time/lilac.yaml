destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''git-warp-time'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- gcc: gcc-libs
- glibc
repo_makedepends:
- git
- jq
update_on:
- alpm: julia
  provides: libgit2.so
  source: alpm
- alpm: git-warp-time
  source: archpkg
