destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-return-multilevel'')

  '
repo_checkdepends:
- perl-test-fatal
repo_depends:
- perl-data-munge
repo_makedepends: []
update_on:
- alpm: perl-return-multilevel
  source: archpkg
