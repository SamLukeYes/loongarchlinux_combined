destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sx'')

  '
repo_depends:
- xorg-server
- xorg-xauth
update_on:
- alpm: sx
  source: archpkg
