destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libretro-scummvm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libretro-core-info
repo_makedepends:
- git
update_on:
- alpm: libretro-scummvm
  source: archpkg
