destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''phonegap'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- cordova
- nodejs-nopt
- semver
repo_makedepends:
- npm
- python
update_on:
- alpm: phonegap
  source: archpkg
