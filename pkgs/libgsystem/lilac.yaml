destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libgsystem'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib2
- systemd: systemd-libs
- attr
update_on:
- alpm: libgsystem
  source: archpkg
