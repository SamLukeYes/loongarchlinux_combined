destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libksieve'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- kidentitymanagement
- pimcommon
- kmailtransport
repo_makedepends:
- extra-cmake-modules
- kdoctools
- qt5-tools
- doxygen
update_on:
- alpm: libksieve
  source: archpkg
