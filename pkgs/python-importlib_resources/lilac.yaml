destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-importlib_resources'')

  '
repo_checkdepends:
- python-pytest
- python: python-tests
repo_depends:
- python-zipp
repo_makedepends:
- python-build
- python-installer
- python-setuptools-scm
- python-toml
- python-wheel
update_on:
- alpm: python-importlib_resources
  source: archpkg
