destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''limine'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- nasm
- mtools
- llvm
- lld
- clang
update_on:
- alpm: limine
  source: archpkg
