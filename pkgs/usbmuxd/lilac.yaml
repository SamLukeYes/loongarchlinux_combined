destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''usbmuxd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libimobiledevice
- libusb
- libplist
repo_makedepends:
- git
- systemd
update_on:
- alpm: usbmuxd
  source: archpkg
