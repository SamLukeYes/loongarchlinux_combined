destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vint'')

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python-ansicolor
- python-chardet
- python-setuptools
- python-yaml
repo_makedepends:
- python-build
- python-installer
- python-wheel
update_on:
- alpm: vint
  source: archpkg
