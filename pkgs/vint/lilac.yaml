destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vint'')

  '
repo_depends:
- python-setuptools
- python-chardet
- python-yaml
- python-ansicolor
update_on:
- alpm: vint
  source: archpkg
