destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''signal-desktop'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libvips
- alsa-lib
- libxss
- gtk3
- hicolor-icon-theme
- openjpeg2
update_on:
- alpm: signal-desktop
  source: archpkg
