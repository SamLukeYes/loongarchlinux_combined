destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''openxr'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- jsoncpp
- libx11
- vulkan-icd-loader
- libglvnd
- libxrandr
- libxxf86vm
repo_makedepends:
- cmake
- vulkan-headers
- python
- ninja
- wayland
- mesa
update_on:
- alias: jsoncpp
- alpm: openxr
  source: archpkg
