destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sdl2_ttf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- sdl2
- freetype2
update_on:
- alpm: sdl2_ttf
  source: archpkg
