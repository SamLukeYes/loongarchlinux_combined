destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libxfce4util'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glib2
repo_makedepends:
- intltool
- gobject-introspection
- vala
update_on:
- alpm: libxfce4util
  source: archpkg
