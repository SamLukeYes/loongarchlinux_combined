destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gsoap'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- openssl
- zlib
- gcc: gcc-libs
repo_makedepends:
- autoconf
- automake
update_on:
- alpm: gsoap
  source: archpkg
