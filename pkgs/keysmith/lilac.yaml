destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''keysmith'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- kirigami2
- kdbusaddons
- libsodium
- ki18n
- qt5-svg
- kwindowsystem
update_on:
- alpm: keysmith
  source: archpkg
