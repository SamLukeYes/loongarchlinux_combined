destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libtiff'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libjpeg-turbo
- xz
- zlib
- zstd
- glibc
update_on:
- alpm: darktable
  provides: libjpeg.so
  source: alpm
- alpm: libtiff
  source: archpkg
