destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libretro-parallel-n64'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libretro-core-info
update_on:
- alpm: libretro-parallel-n64
  source: archpkg
