destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''mate-session-manager'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- dbus-glib
- libsm
- gettext
- gtk3
- mate-desktop
update_on:
- alpm: mate-session-manager
  source: archpkg
