destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''sqlite'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- zlib
- tcl
- readline
- glibc
update_on:
- alias: readline
- alpm: sqlite
  source: archpkg
