destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-cairo-gobject'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glib-perl
- cairo-perl
update_on:
- alpm: perl-cairo-gobject
  source: archpkg
