destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plasma-nm'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- modemmanager-qt
- networkmanager-qt
- plasma-workspace
update_on:
- alpm: plasma-nm
  source: archpkg
