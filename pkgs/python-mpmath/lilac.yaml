destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mpmath'')

  '
repo_depends:
- python-gmpy2
update_on:
- alpm: python-mpmath
  source: archpkg
