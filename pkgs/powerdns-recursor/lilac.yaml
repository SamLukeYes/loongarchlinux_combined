destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''powerdns-recursor'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- net-snmp
- fstrm
- luajit
- libsodium
- openssl
- boost: boost-libs
- systemd: systemd-libs
update_on:
- alpm: curl
  provides: libcurl.so
  source: alpm
- alpm: boost
  provides: libboost_context.so
  source: alpm
- alpm: powerdns-recursor
  source: archpkg
