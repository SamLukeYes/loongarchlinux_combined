destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''lib2geom'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gsl
update_on:
- manual: 1
  source: manual
- alpm: gsl
  source: alpm
  strip_release: true
- alpm: lib2geom
  source: archpkg
