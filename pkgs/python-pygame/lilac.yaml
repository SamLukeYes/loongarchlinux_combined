destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pygame'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- glibc
- libpng
- python
- sdl2
- sdl2_mixer
- sdl2_ttf
- sdl2_image
- portmidi
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
- sdl2
update_on:
- alias: python
- alpm: python-pygame
  source: archpkg
