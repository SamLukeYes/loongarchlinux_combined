destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opensearch'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- java11-openjdk: jre11-openjdk-headless
- libxml2
- systemd
update_on:
- alpm: opensearch
  source: archpkg
