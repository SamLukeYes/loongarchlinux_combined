destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''vim'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- acl
- zlib
- perl
- libgcrypt
- libcanberra
- libxt
- gtk3
- glibc
- gpm
update_on:
- alias: perl
- alias: perl
- alpm: vim
  source: archpkg
