destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''typescript'')

  '
repo_checkdepends: []
repo_depends:
- nodejs
repo_makedepends:
- npm
- rsync
update_on:
- alpm: typescript
  source: archpkg
