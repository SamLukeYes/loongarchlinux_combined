destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-doublex'')

  '
repo_checkdepends:
- python-nose2
repo_depends:
- python-pyhamcrest
- python-six
repo_makedepends:
- python-setuptools
update_on:
- alpm: python-doublex
  source: archpkg
