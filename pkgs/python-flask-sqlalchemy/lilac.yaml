destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-flask-sqlalchemy'')

  '
repo_checkdepends:
- python-pytest
- python-blinker
- python-mock
repo_depends:
- python-flask
- python-sqlalchemy
repo_makedepends:
- python-setuptools
- python-sphinx
- python-pallets-sphinx-themes
- python-sphinx-issues
update_on:
- alpm: python-flask-sqlalchemy
  source: archpkg
