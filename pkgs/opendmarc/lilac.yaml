destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opendmarc'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libbsd
- perl-json
- libidn
- libspf2
update_on:
- alpm: opendmarc
  source: archpkg
