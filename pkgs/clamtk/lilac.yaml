destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''clamtk'')

  '
repo_depends:
- perl
- python
update_on:
- manual: 1
  source: manual
- alias: perl
- alias: python
- alpm: clamtk
  source: archpkg
