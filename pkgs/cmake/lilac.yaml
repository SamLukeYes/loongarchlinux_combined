destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cmake'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- curl
- libarchive
- hicolor-icon-theme
- jsoncpp
- libuv
- rhash
repo_makedepends:
- qt6-base
- python-sphinx
- emacs
update_on:
- alias: jsoncpp
- alpm: jsoncpp
  provides: libjsoncpp.so
  source: alpm
- alpm: cmake
  source: archpkg
