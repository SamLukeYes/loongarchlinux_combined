destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pysmf'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
repo_depends:
- python
- libsmf
repo_makedepends:
- git
- python-build
- python-installer
- python-wheel
- python-setuptools
- cython
update_on:
- alias: python
- alpm: python-pysmf
  source: archpkg
