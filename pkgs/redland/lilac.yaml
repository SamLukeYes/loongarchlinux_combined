destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''redland'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- redland
- mariadb: mariadb-libs
- redland
- postgresql: postgresql-libs
- redland
- unixodbc
- redland
repo_makedepends:
- rasqal
- raptor
- postgresql: postgresql-libs
- mariadb: mariadb-libs
- unixodbc
- sqlite
update_on:
- alpm: redland
  source: archpkg
