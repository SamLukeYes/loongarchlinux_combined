destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-user-share'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- mod_dnssd
- glib2
- dconf
- systemd
update_on:
- alpm: gnome-user-share
  source: archpkg
