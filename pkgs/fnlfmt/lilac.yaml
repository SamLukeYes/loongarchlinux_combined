destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fnlfmt'')

  '
repo_checkdepends: []
repo_depends:
- lua
repo_makedepends:
- git
update_on:
- alias: lua
- alpm: fnlfmt
  source: archpkg
