destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-zita-audiotools'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- gcc: gcc-libs
- libsndfile
- zita-resampler
- glibc
- python-numpy
update_on:
- alpm: python-zita-audiotools
  source: archpkg
