destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libphonenumber'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- icu
- protobuf
update_on:
- alpm: icu
  provides: libicui18n.so
  source: alpm
- alpm: protobuf
  provides: libprotobuf.so
  source: alpm
- alpm: libphonenumber
  source: archpkg
