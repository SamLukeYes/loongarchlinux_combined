destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''flobopuyo'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- glu
- sdl_mixer
- sdl_image
update_on:
- alpm: flobopuyo
  source: archpkg
