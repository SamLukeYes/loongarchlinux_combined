destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''evemu'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- libevdev
repo_makedepends:
- asciidoc
- python
- xmlto
update_on:
- alpm: evemu
  source: archpkg
