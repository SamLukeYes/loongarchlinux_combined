destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''pacman-contrib'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- fakeroot
- pacman
- perl
repo_makedepends:
- asciidoc
- git
update_on:
- alias: perl
- alpm: pacman-contrib
  source: archpkg
