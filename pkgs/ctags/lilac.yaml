destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ctags'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- jansson
- libseccomp
- pcre2
- libyaml
- libxml2
- glibc
update_on:
- alpm: libseccomp
  provides: libseccomp.so
  source: alpm
- alpm: ctags
  source: archpkg
