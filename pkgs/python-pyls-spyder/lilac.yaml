destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-pyls-spyder'')

  '
repo_depends:
- python-lsp-server
update_on:
- alpm: python-pyls-spyder
  source: archpkg
