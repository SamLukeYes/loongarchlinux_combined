destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''fastd'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libmnl
- gcc: gcc-libs
- json-c
- libsodium
- libcap
- openssl
update_on:
- alpm: fastd
  source: archpkg
