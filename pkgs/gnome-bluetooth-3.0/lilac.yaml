destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''gnome-bluetooth-3.0'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- bluez
- gtk4
- libadwaita
- libnotify
- pulseaudio: pulseaudio-bluetooth
- upower
- gsound
- systemd
update_on:
- alpm: gnome-bluetooth-3.0
  source: archpkg
