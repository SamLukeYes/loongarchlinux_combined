destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-enrich'')

  '
repo_checkdepends:
- python-pytest
- python-pytest-mock
- python-pytest-xdist
repo_depends:
- python-rich
repo_makedepends:
- git
- python-build
- python-installer
- python-setuptools
- python-setuptools-scm
- python-wheel
update_on:
- alpm: python-enrich
  source: archpkg
