destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-erb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- ruby-rake
- ruby-rake-compiler
repo_depends:
- ruby
- ruby-cgi
repo_makedepends:
- tree
update_on:
- alias: ruby
- alpm: ruby-erb
  source: archpkg
