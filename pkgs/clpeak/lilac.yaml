destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''clpeak'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- ocl-icd
- gcc: gcc-libs
repo_makedepends:
- cmake
- opencl-headers
- ninja
- git
update_on:
- alpm: clpeak
  source: archpkg
