destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''elixir'')

  '
repo_checkdepends:
- git
repo_depends:
- erlang-nox
repo_makedepends: []
update_on:
- alpm: elixir
  source: archpkg
