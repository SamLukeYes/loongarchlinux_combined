destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''plasma-browser-integration'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- purpose
- kfilemetadata
- plasma-workspace
repo_makedepends:
- extra-cmake-modules
update_on:
- alpm: plasma-browser-integration
  source: archpkg
