destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''rocs'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-xmlpatterns
- grantlee
- qt5-script
- ktexteditor
- hicolor-icon-theme
update_on:
- alpm: rocs
  source: archpkg
