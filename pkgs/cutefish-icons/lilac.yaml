destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''cutefish-icons'')

  '
repo_checkdepends: []
repo_depends: []
repo_makedepends:
- extra-cmake-modules
- ninja
update_on:
- alpm: cutefish-icons
  source: archpkg
