destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''btrbk'')

  '
repo_depends:
- perl
- btrfs-progs
update_on:
- alias: perl
- alpm: btrbk
  source: archpkg
