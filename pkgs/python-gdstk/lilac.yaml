destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-gdstk'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest-runner
repo_depends:
- python-numpy
repo_makedepends:
- git
- python-setuptools
- cmake
update_on:
- alpm: python-gdstk
  source: archpkg
