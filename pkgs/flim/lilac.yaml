destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''flim'')

  '
repo_depends:
- emacs
update_on:
- alpm: emacs
  source: alpm
  strip_release: true
- alpm: flim
  source: archpkg
