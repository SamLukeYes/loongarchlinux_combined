destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''nut'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- openssl
- gcc: gcc-libs
- net-snmp
- libusb-compat
- libtool
- neon
- glibc
update_on:
- alpm: nut
  source: archpkg
