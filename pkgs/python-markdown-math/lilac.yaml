destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-markdown-math'')

  '
repo_depends:
- python-markdown
update_on:
- alpm: python-markdown-math
  source: archpkg
