destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''libime'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- boost: boost-libs
- fcitx5
repo_makedepends:
- boost
- extra-cmake-modules
- ninja
- python
update_on:
- alpm: libime
  source: archpkg
