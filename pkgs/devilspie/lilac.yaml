destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''devilspie'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libwnck3
update_on:
- alpm: devilspie
  source: archpkg
