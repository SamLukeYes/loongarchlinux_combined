destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ffmpeg2theora'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libkate
- ffmpeg4.4
update_on:
- alpm: ffmpeg2theora
  source: archpkg
