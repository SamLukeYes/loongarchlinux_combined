destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''luakit'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- lua-filesystem: lua51-filesystem
- webkit2gtk
- luajit
update_on:
- alpm: luakit
  source: archpkg
