destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-adb-shell'')

  '
repo_checkdepends: []
repo_depends:
- python
- python-cryptography
- python-pyasn1
- python-rsa
repo_makedepends:
- python-build
- python-installer
- python-setuptools
- python-wheel
update_on:
- alias: python
- alpm: python-adb-shell
  source: archpkg
