destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''kvantum'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- qt5-svg
- hicolor-icon-theme
- kwindowsystem
update_on:
- alpm: kvantum
  source: archpkg
