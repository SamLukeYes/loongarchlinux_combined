destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''apparmor'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- dejagnu
- perl-locale-gettext
- python-notify2
- python-psutil
repo_depends:
- audit
- bash
- gcc: gcc-libs
- glibc
- pam
- python
repo_makedepends:
- apache
- libxcrypt
- python-setuptools
- ruby
- swig
update_on:
- alias: python
- alpm: apparmor
  source: archpkg
