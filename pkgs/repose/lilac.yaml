destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''repose'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- python-pytest
- python-cffi
- python-pytest-xdist
repo_depends:
- pacman
- libarchive
- gnupg
repo_makedepends:
- ragel
update_on:
- alpm: repose
  source: archpkg
