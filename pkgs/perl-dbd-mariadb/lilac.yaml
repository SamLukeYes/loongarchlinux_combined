destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''perl-dbd-mariadb'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends:
- mariadb
- perl-test-deep
repo_depends:
- mariadb: mariadb-libs
- perl-dbi
repo_makedepends:
- perl-devel-checklib
update_on:
- alpm: perl-dbd-mariadb
  source: archpkg
