destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''python-mediafile'')

  '
repo_depends:
- python-mutagen
- python-six
update_on:
- alpm: python-mediafile
  source: archpkg
