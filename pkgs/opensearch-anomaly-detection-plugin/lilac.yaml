destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''opensearch-anomaly-detection-plugin'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- opensearch
repo_makedepends:
- unzip
update_on:
- alpm: opensearch-anomaly-detection-plugin
  source: archpkg
