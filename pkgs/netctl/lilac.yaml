destsubdir: core
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''netctl'')

  '
repo_depends:
- iproute2
- coreutils
update_on:
- alpm: netctl
  source: archpkg
