destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''dante'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- libxcrypt
- pam
- krb5
update_on:
- alpm: dante
  source: archpkg
