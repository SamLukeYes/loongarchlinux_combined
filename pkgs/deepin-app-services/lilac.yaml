destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''deepin-app-services'')

  add_into_array(''arch'', [''loong64''])

  '
repo_checkdepends: []
repo_depends:
- dtkwidget
repo_makedepends:
- cmake
- gtest
- ninja
update_on:
- alpm: deepin-app-services
  source: archpkg
