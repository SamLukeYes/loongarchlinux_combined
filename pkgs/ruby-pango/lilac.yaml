destsubdir: extra
post_build_script: 'git_add_files(_G.files)

  git_commit()

  '
pre_build_script: '_G.files = download_official_pkgbuild(''ruby-pango'')

  add_into_array(''arch'', [''loong64''])

  '
repo_depends:
- ruby
- ruby-cairo-gobject
- ruby-cairo
- pango
- ruby-gobject-introspection
- gobject-introspection
update_on:
- alias: ruby
- alpm: ruby-pango
  source: archpkg
